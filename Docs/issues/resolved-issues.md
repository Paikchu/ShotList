# 分镜助手 · 已修复记录

更新日期：2026-09-22。已修复问题保留编号、详情、复现步骤、验证结果和修复提交以便追溯；经实测无需修复而关闭的条目同样保留在此，标 ☒。按优先级及编号排序。未关闭问题见 [open-issues](open-issues.md)。

## 汇总 Checklist

| 完成 | 编号 | 问题标题 |
|:--:|---|---|
| ☑ | P0-4 | [打开镜头面板会自动修改并保存描述](#p0-4) |
| ☑ | P0-5 | [元数据读取失败后被当成空库，旧数据可能被覆盖](#p0-5) |
| ☑ | P0-6 | [元数据写入失败仍报告保存成功，重启后片段失去关联](#p0-6) |
| ☑ | P1-8 | [长按菜单清空片段未经确认直接删除](#p1-8) |
| ☑ | P1-9 | [空状态下分镜页整条导航栏的触摸被吞，右上角按钮点不动](#p1-9) |
| ☑ | P2-16 | [导出过程中素材缺失会重新编号，片段序号与界面不一致](#p2-16) |
| ☑ | P2-17 | [导出缺少空间预检，空间不足时仅显示系统错误](#p2-17) |
| ☑ | P2-18 | [素材变化后旧导出包仍可分享，未标记过期](#p2-18) |
| ☑ | P2-20 | [缩略图失效后在途任务仍回写旧图并清掉新任务](#p2-20) |
| ☑ | P2-21 | [工程默认关闭代码签名，阻断默认真机分发流程](#p2-21) |
| ☑ | P2-24 | [历史页的「还没拍」与补拍提示使用不同口径](#p2-24) |
| ☑ | P2-25 | [前一次导入完成会清空后一次相册选择的目标](#p2-25) |
| ☑ | P2-26 | [导入目标被删除后仍返回成功，并残留临时文件](#p2-26) |
| ☑ | P2-27 | [保存回看片段期间仍能重拍，导致源文件被提前删除](#p2-27) |
| ☑ | P2-28 | [同时间戳片段交换镜头顺序后，文件名编号错位](#p2-28) |
| ☑ | P2-29 | [删除失败仍移除记录，孤儿清理虚报成功数量](#p2-29) |
| ☑ | P2-30 | [历史卡片显示当天素材，VoiceOver 却朗读全量最新素材](#p2-30) |
| ☑ | P2-31 | [缩略图解码失败后一直显示加载动画](#p2-31) |
| ☑ | P2-32 | [主素材文件名预览默认取最后一条，可能与实际导出不一致](#p2-32) |
| ☑ | P2-33 | [大视频同步复制占用主线程，保存和导入可能卡住界面](#p2-33) |
| ☑ | P2-34 | [相册导入触发系统整段转码，导入耗时长且画质被降](#p2-34) |
| ☑ | P2-35 | [一次只能选一段视频，多段素材要重复走一遍导入流程](#p2-35) |
| ☑ | P2-36 | [选择 4K 或 60 fps 后单条录制会更早触到 600 MB 上限，停止时不给任何说明](#p2-36) |
| ☑ | P2-37 | [缩放上限被硬编码为 8×，不随设备与格式变化](#p2-37) |
| ☑ | P2-38 | [有镜头的列表状态下点导航栏标题无反应，改名弹窗不出现](#p2-38) |
| ☑ | P2-39 | [镜头面板把没改过的编号也当成一次编辑写回，面板打开期间编号被重排后，只改描述也会把镜头挪回旧位置](#p2-39) |
| ☒ | P2-40 | [重排、插入、删除分镜时在主协程上逐条复制片段文件，素材多时界面卡住且短时占用双份磁盘](#p2-40) |
| ☑ | P2-41 | [历史页进度环按磁盘口径、右侧三项统计按记录口径，同一屏两个数字会互相矛盾](#p2-41) |
| ☑ | P2-42 | [拍摄设置里分辨率不可用时写成「该分辨率下不支持」，说的不是这一行的对象](#p2-42) |
| ☑ | P2-43 | [保存拍摄与相册导入都会对已经属于本应用的临时文件再整份复制一次](#p2-43) |
| ☑ | P2-44 | [历史页影片下拉面板不滚动，影片较多时底部的影片与「重制」落到屏幕外点不到](#p2-44) |
| ☑ | P2-45 | [任何一次分镜编辑都会立刻删掉已生成的导出包，分享还没完成时会被中断](#p2-45) |
| ☑ | P2-46 | [删除补偿日志清理失败时整个应用停在「无法读取分镜记录」，标题与实际原因不符](#p2-46) |
| ☑ | P2-47 | [影片条的点击收起层用已在 iOS 26 废弃的 UIScreen.main 取屏幕尺寸，是全项目仅有的编译告警](#p2-47) |
| ☑ | P2-48 | [删除补偿日志本身损坏时应用永远停在「删除尚未完成」，重试无法自愈也没有出口](#p2-48) |
| ☑ | P2-49 | [体积上限随画质放宽后磁盘可能被录满，而写满时仍然只是「自己停了」且没有任何说明](#p2-49) |
| ☑ | P2-50 | [录制失败时残缺的临时视频不会被删除，一直占着磁盘到下次启动](#p2-50) |
| ☑ | P2-51 | [打包途中输入变化使结果被丢弃后，切换影片仍显示「需重新打包」，而新影片从未打过包](#p2-51) |
| ☑ | P2-52 | [片段文件已不在磁盘上时点播放，进入一块没有任何出口的黑屏，只能强杀应用](#p2-52) |
| ☑ | P2-53 | [应用内没有任何删除整部影片的入口，影片记录只增不减](#p2-53) |
| ☑ | P2-55 | [快速拍摄在相机页改走「导入」后取消选片，新建的空镜头不会被撤销](#p2-55) |
| ☑ | P2-56 | [批量添加入口移除后，ShotStore.addShots(count:) 只剩测试在调用](#p2-56) |
| ☑ | P2-58 | [选片器关闭时的快速拍摄收尾依赖两个 onChange 的执行顺序，顺序颠倒时导入会被静默丢弃](#p2-58) |

☑ 修复并验证通过；☒ 经实测无需修复而关闭（没有修复提交，条目内写明依据与重开条件）。

<a id="p0-4"></a>

### P0-4 · 打开镜头面板会自动修改并保存描述

**验证状态：** 模拟器实测

**代码位置：** ShotList/Views/ClipOptionsSheet.swift · .task / scheduleDraftSave / flushDraft

**预期结果：** 仅浏览或关闭面板不改描述和 JSON；真实输入仍自动保存，不回填猜测的原始描述。

**问题详情**

面板中的 `.task` 等待约 0.9 秒后，无条件追加「（探针追加）」。描述变化会触发自动保存；每次重新打开都会重复追加，污染原始描述、导出文件名和分镜指南。等待任务忽略取消错误，提前关闭也不能作为可靠保护。

**复现方法（修复前）**

1. 在测试数据中新建一个有描述的镜头。
2. 打开镜头面板，不做任何输入，等待约 1.5 秒后关闭。
3. 再次打开，检查描述及 shots.json：文本已被追加「（探针追加）」。重复打开会继续追加。

**修复状态：** 已修复

**修复说明：** 删除延迟追加描述的临时探针，取消后不再抢焦点，未编辑草稿不触发首尾空白裁剪。

**验证结果：** iOS 26.5 / iPhone 17 Pro Max，独立 bundle com.max.ShotList.DebugFixes：输入 Original note 后正常持久化；连续三次打开面板各等待超过 2 秒并关闭/重开，shots.json 字节与基线完全一致。Build iOS Apps 构建运行通过。

**修复 commit：** 3bbfc42c49f474368e4f806f82525b651752b34f

<a id="p0-5"></a>

### P0-5 · 元数据读取失败后被当成空库，旧数据可能被覆盖

**验证状态：** 隔离存储测试及模拟器实测

**代码位置：** ShotList/Models/ShotStore.swift · load / applySnapshot / writeMetadata

**预期结果：** 无法读取记录时显示错误，禁止新增/改写/删除及孤儿清理；恢复有效记录后可重试，首次启动正常建库。

**问题详情**

`load` 用 `try?` 吞掉读取和解码错误，无法区分首次启动、暂时不可读和 JSON 损坏。失败后以空分镜列表计算孤儿文件，原素材会被列为「未使用文件」；新增操作还会覆盖原元数据。用户若继续清理，视频文件也会被删除。

**复现方法（修复前）**

1. 在隔离目录创建 ShotStore，添加一个镜头及一个片段。
2. 将该目录的 shots.json 改为无效 JSON，再用同一目录重新创建 ShotStore。
3. 观察结果：shots=0、orphans=1；原视频仍存在，却被当成未使用文件。
4. 新增一个镜头后检查 shots.json，原文件内容已被新列表覆盖。

**修复状态：** 已修复

**修复说明：** 区分首次启动与读取/解码失败；失败时阻止全部仓库写操作和孤儿清理，显示独立错误页及重新读取。元数据缺失但素材存在也进入保护状态。新增隔离目录单元测试和测试 scheme。

**验证结果：** Build iOS Apps test_sim：4/4 通过，覆盖损坏 JSON、元数据为目录、有素材但元数据缺失、首次启动及有效空库。损坏状态逐项调用增删改/排序/清理/视频保存均不覆盖原文件，恢复后可重读。iOS 26.5 模拟器实测错误页、失败重试保留原字节、恢复备份后回到原分镜。

**修复 commit：** 20ccc251fd885f09d9d624d06bd97f849c3eda28

<a id="p0-6"></a>

### P0-6 · 元数据写入失败仍报告保存成功，重启后片段失去关联

**验证状态：** 隔离存储测试及模拟器实测

**代码位置：** ShotList/Models/ShotStore.swift · addClip / commit / persist / writeMetadata / syncClipFileNames

**预期结果：** 元数据提交失败必须向调用方报告错误，保留源视频和旧记录/文件；成功重试并重启后仍能找到新片段。

**问题详情**

保存流程先复制视频、删除源文件，再写入元数据。`writeMetadata` 忽略写入错误，因此 `addClip` 仍正常返回，界面会提示成功并退出回看。重启后新片段没有元数据引用，成为孤儿文件。重编号后的元数据提交失败也可能造成文件名与记录不一致。

**复现方法（修复前）**

1. 在隔离目录创建一个镜头及待保存的临时视频。
2. 把 shots.json 替换为同名目录，强制后续元数据写入失败。
3. 调用 addClip：方法无错误返回，源文件被删除，内存片段数为 1。
4. 重新创建 ShotStore，片段数变为 0；目标视频仍在磁盘，但已失去分镜关联。

**修复状态：** 已修复

**修复说明：** 元数据编码和原子写入改为抛错；仓库保存失败恢复已提交快照并清理本次新文件，源视频在成功提交后才删除。重编号先复制再提交、提交后清理旧文件，删除操作也后置到元数据成功之后。非 throwing 操作返回失败并显示内联提示；描述草稿失败时保留面板，完成/拍摄/导入/播放前检查保存结果。

**验证结果：** Build iOS Apps test_sim（iOS 26.5）：8/8 通过，其中新增写入失败重试、编码失败、增删改与重编号回滚、磁盘校正提交失败测试。模拟器实际相册导入在 shots.json 为目录时显示导入失败、源视频保留且无新增目标文件；恢复后再次导入成功，重新启动仍有关联。描述编辑失败保留草稿和面板，恢复后点击完成正常保存。相机回看使用同一 addClip 抛错路径并由现有 catch 保留 reviewURL；未执行真机录制及真实低空间测试。

**修复 commit：** ffcd00ce7ee1e4320be58d558797d933651d09ce

<a id="p1-8"></a>

### P1-8 · 长按菜单清空片段未经确认直接删除

**验证状态：** 模拟器实测

**代码位置：** ShotList/Views/ShotListView.swift · contextMenu

**预期结果：** 长按清空先说明删除范围并确认；取消不改变记录和文件，确认后只删除片段、保留分镜。

**问题详情**

长按菜单的「清空片段（保留分镜）」直接调用 `removeAllClips`，没有二次确认，也没有 destructive 角色标记。同一操作在镜头面板中有确认对话框，两处入口行为不一致，误触会立即删除全部片段。

**复现方法（修复前）**

1. 准备一个含多条测试片段的镜头。
2. 长按分镜卡片，选择「清空片段（保留分镜）」。
3. 观察到没有确认对话框，片段记录及文件直接被删除。

**修复状态：** 已修复

**修复说明：** 长按菜单改为 destructive 操作，仅设置待确认镜头；确认弹层说明数量、不可恢复及保留分镜，确认后才清空。

**验证结果：** iOS 26.5 独立 App：含两条真实测试视频的卡片长按清空后出现确认，JSON 和两文件保持；点击弹层外取消后卡片仍显示两条；再次长按并明确确认后两文件及片段引用删除，分镜 ID、描述和编号保留。构建运行通过。

**修复 commit：** bad4378f6aa59bfe542e285dfa3fbc6fd3490833

<a id="p1-9"></a>

### P1-9 · 空状态下分镜页整条导航栏的触摸被吞，右上角按钮点不动

**验证状态：** 模拟器实测

**代码位置：** ShotList/Views/ShotListView.swift · emptyState / toolbarContent（`ToolbarItem(placement: .largeTitle)`）

**预期结果：** 空状态下右上角的加号、三点与标题按钮与列表状态一样可点：加号弹出镜头面板，三点打开影片菜单，点标题弹出改名弹窗。

**问题详情**

分镜页的标题改名按钮挂在 `largeTitle` 位、占住整条标题行（该写法来自 d4a5c54，因为标签根页上的 `navigationTitle` 不是可点按钮）。当根页内容不是滚动容器时——空状态把 `ContentUnavailableView` 直接铺在根上——整条导航栏的触摸区域被吃掉：

- 在导航栏上真实触摸加号、三点，菜单与镜头面板都不出现，也不弹改名弹窗，即点击既没有到达目标按钮，也没有被标题按钮接走，没有任何反馈；
- 同一时刻内容区的「添加分镜」按钮正常，说明不是全局模态或整窗失效；
- 列表状态（有镜头）没有这个问题，因为那一份内容本身就是 `List`；空状态是任意一部没有镜头的影片，包括未命名影片、改名后的空影片，以及「重制」刚开出来的空白影片——也就是用户截图里那一屏。

定位依据（iOS 26.5 / iPhone 17 Pro Max，同一份数据下逐个变体实测，记录在探针日志中）：空状态改用 `ScrollView` 承载、或去掉标题按钮，导航栏触摸都立即恢复；只去掉背景、只去掉 `frame`、把标题按钮的 `maxWidth: .infinity` 去掉，仍然点不动。可见触发条件是「`largeTitle` 位的自定义标题按钮 + 根页非滚动内容」，与影片是否命名无关（有名 / 未命名的空影片表现一致）。

影响：空状态下加号、三点（切换影片 / 改标题 / 重制）与点标题改名全部不可用。加镜头还有内容区按钮可用，影片菜单要绕到历史页，因此定为 P1 而不是 P0。

**复现方法**

1. 在测试数据中准备一部**没有镜头**的影片（未命名或已命名均可），把它设为当前影片。
2. 进入「分镜」标签页，确认显示「这部影片还没有分镜」空状态、导航栏标题为该影片名。
3. 点击右上角加号：观察没有镜头面板出现、空状态也不变。
4. 点击右上角三点：观察影片菜单不弹出。
5. 点击导航栏标题：观察改名弹窗不出现。
6. 对照：切到任意有镜头的影片（列表状态），重复第 3–5 步，三个按钮均正常响应。

**修复状态：** 已修复

**修复说明：** 空状态改为与列表状态一致的写法——用 `ScrollView` 承载 `ContentUnavailableView`，并用 `containerRelativeFrame(.vertical)` 让它仍然占满一屏；配 `.scrollBounceBehavior(.basedOnSize)`，内容不满一屏时不回弹。根因（自定义标题按钮与非滚动根内容的相互作用）不改动标题按钮本身，避免影响「点标题改名」这条既有设计。

**验证结果：** iOS 26.5 / iPhone 17 Pro Max，App 真实安装后由 XCUITest 探针按下真实触摸（探针工程放在 /tmp，不进入仓库）：修复前，未命名空影片与有名空影片的空状态下点加号、三点均无任何反应（菜单 / 面板 / 改名弹窗都不出现）；修复后，两条空影片用例的加号均弹出镜头面板、三点均打开影片菜单、点标题弹出「影片标题」改名弹窗，有镜头的列表状态用例无回归。空状态上滑位移为 0（不出现回弹）。版式与改前逐像素比对，差异像素数 0（阈值 12，1320×2868）。构建运行通过。

**修复 commit：** 69dcd1cbeb338be44063061c3d45faccb18035c4

<a id="p2-16"></a>

### P2-16 · 导出过程中素材缺失会重新编号，片段序号与界面不一致

**验证状态：** 隔离测试实测

**代码位置：** ShotList/Support/ExportPackageBuilder.swift · build / available.enumerated

**预期结果：** 缺失素材不改变其余片段的原始条号；文件名、CSV 和指南保持同一对应关系。

**问题详情**

界面按 `shot.clips` 的位置显示「第 N 条」，导出却先过滤磁盘上不存在的文件，再重新从 1 编号。正常前台校正已缩小触发范围，但导出取得快照后文件消失仍会触发，导致文件名、CSV 和指南的片段号与原界面不一致。

**复现方法（修复前）**

1. 准备一个含三条片段的镜头，记录界面的第 1、2、3 条及对应文件。
2. 在隔离测试中先取得 shots 快照，再删除第 1 条文件；不调用 refreshStorageStats。
3. 用该快照调用 ExportPackageBuilder.build。
4. 检查导出文件名及 CSV：原第 2 条被标成第 1 条，原第 3 条被标成第 2 条。

**修复状态：** 已修复

**修复说明：** 先按原 clips 数组枚举条号再过滤缺失文件；文件名、CSV 和指南使用原始条号及原总数，主素材从仍可用的片段中选择。

**验证结果：** Build iOS Apps test_sim：隔离目录实际打包四组数据（全量、第1条缺失、仅第3条、最新条缺失），验证目标文件字节、CSV 条号、指南主/备用路径与快照一致，全部通过。

**修复 commit：** 569643ead51b5b63bd3d679f3c77f19eaa9aaceb

<a id="p2-17"></a>

### P2-17 · 导出缺少空间预检，空间不足时仅显示系统错误

**验证状态：** 隔离故障测试及模拟器实测

**代码位置：** ShotList/Support/ExportPackageBuilder.swift · build / zip

**预期结果：** 在复制前尽可能提示空间不足；导出过程中耗尽空间也给出可执行提示，清理失败产物并保留源素材。

**问题详情**

导出先复制素材，再生成和复制压缩包，需要额外临时空间；旧导出包可能也仍存在。流程没有检查可用空间，直到复制或打包失败才显示系统错误，没有说明空间不足及可采取的措施。实际峰值取决于复制和压缩行为，不能固定按素材大小估算。

**复现方法（修复前）**

1. 在专用测试设备或受限测试卷中准备较大素材，使剩余空间不足以完成导出。
2. 点击「生成导出包」。
3. 检查是否在复制/打包阶段才失败，且提示仅包含系统错误，没有明确的空间不足说明。

**修复状态：** 已修复

**修复说明：** 导出前估算素材工作副本、临时及最终 zip 与文本开销，读取重要用途可用容量并给出释放空间提示；容量未知时继续并保留运行时保护。保留 Cocoa/POSIX 及底层磁盘满错误类型，失败清理工作目录和不完整 zip。

**验证结果：** 隔离测试覆盖预检容量为0（复制次数0）、容量未知/充足、复制视频和复制zip时注入 ENOSPC、嵌套Cocoa错误及非空间错误，全部通过；源素材字节不变，失败无工作目录/残缺zip。模拟器独立测试App临时注入可用容量0，点击生成导出包显示预计空间/当前空间/释放空间提示，探针已移除。未填满真实设备磁盘。

**修复 commit：** 43e0a9c392b23541de39ab784497c623e87cd39d

<a id="p2-18"></a>

### P2-18 · 素材变化后旧导出包仍可分享，未标记过期

**验证状态：** 隔离时序测试及模拟器实测

**代码位置：** ShotList/Views/ExportView.swift、ShotList/Support/ExportSession.swift · build / invalidate / resultSection

**预期结果：** 输入变化后旧包不能再从页面分享；在途旧结果不回显，重新生成后才恢复分享入口。

**问题详情**

导出结果保存在独立的 `package` 状态中，追加片段、改描述、换序后不会失效。界面继续显示「刚刚生成的导出包」并允许分享，用户可能发出旧内容。生成期间清空分镜，迟到的后台结果也可能重新显示旧包。

**复现方法（修复前）**

1. 生成一个包含现有素材的导出包。
2. 返回分镜页追加一条片段，或修改描述、调整顺序。
3. 回到导出页，不重新生成，直接分享旧包。
4. 解压检查：包内仍是修改前的内容，界面没有过期提示。

**修复状态：** 已修复

**修复说明：** ExportSession 跟踪输入代次并持有结果；分镜/素材/范围变化撤下分享入口并提示重建，结果回传再次核对当前输入和代次。每次打包使用独立目录，由会话回收自身旧文件，迟到任务不再全局清理新包。

**验证结果：** 6项导出相关测试通过，其中受控挂起 builder 验证生成中失效后的迟到包被删除且不回显，并验证回调前输入检查和重建恢复。模拟器实际生成后改描述，返回导出页显示过期提示且无旧包分享按钮；重新生成成功，切换导出范围后再次失效。

**修复 commit：** 919f983828cf0bd377b558acd48770c789a4f88f

<a id="p2-20"></a>

### P2-20 · 缩略图失效后在途任务仍回写旧图并清掉新任务

**验证状态：** 隔离时序测试及模拟器正常路径实测

**代码位置：** ShotList/Support/Haptics.swift · ThumbnailLoader.thumbnail / invalidate / removeAll

**预期结果：** 失效前的解码结果不能写回缓存、返回旧图或清除新请求；同代并发读者仍复用一次解码。

**问题详情**

`invalidate` 只清除缓存和在途任务表，不阻止旧解码任务完成。旧任务恢复后仍无条件写入缓存，并把同路径的 inFlight 项清空；如果该路径已经发起新任务，新任务记录也会被误删。视图的取消检查无法保护加载器缓存。

**复现方法（修复前）**

1. 在隔离测试中启动路径 A 的缩略图解码，用可控等待暂停结果返回。
2. 调用 invalidate(for: A)，将 A 替换为另一段视频，再发起同路径的新请求。
3. 让旧任务先完成、新任务继续等待。
4. 检查缓存及 inFlight：旧图被写回，新任务记录被清掉；后续请求可能读到旧图或重复解码。

**修复状态：** 已修复

**修复说明：** 每个解码请求和缓存结果携带独立ID，完成时核对当前在途或已缓存结果ID；失效取消并移除请求。旧结果返回nil，不能写缓存或清掉新请求，同代并发请求继续复用解码。

**验证结果：** Build iOS Apps test_sim：针对单路径 invalidate / 全量 removeAll，各覆盖旧任务先完成和新任务先完成四种受控时序；验证旧结果nil、新缓存身份、无第三次解码；另测并发读者共享一次解码。2项测试全部通过。模拟器构建运行及正常视频缩略图显示通过。

**修复 commit：** 7bf815383d8304fefb29f6c73a40d354a390775f

<a id="p2-21"></a>

### P2-21 · 工程默认关闭代码签名，阻断默认真机分发流程

**验证状态：** 真机目标签名归档及IPA导出实测

**代码位置：** project.yml · targets.ShotList.settings.base；ShotList.xcodeproj/project.pbxproj

**预期结果：** 默认真机构建启用自动签名，不需额外覆盖签名开关即可生成可校验的签名归档和IPA。

**问题详情**

`CODE_SIGNING_ALLOWED=NO` 和 `CODE_SIGNING_REQUIRED=NO` 被设置为工程默认值，影响 Debug 和 Release。即使配置了开发团队，也不会按通常的自动签名流程生成可安装、可分发的签名产物，需要额外覆盖这些设置。

**复现方法（修复前）**

1. 打开工程，选择真机或通用 iOS 设备目标，保留仓库默认设置。
2. 检查 Debug/Release 的有效 Build Settings，确认 CODE_SIGNING_ALLOWED 为 NO。
3. 在具备证书和真机的测试环境执行安装或归档导出，检查签名产物及安装/分发结果。此步骤尚未实测。

**修复状态：** 已修复

**修复说明：** 移除 App target 对 CODE_SIGNING_ALLOWED/REQUIRED 的 NO 覆盖，保留已有团队和 Automatic 签名；同步 XcodeGen 源配置与生成工程。

**验证结果：** Debug/Release 的 iphoneos 有效设置均为 CODE_SIGNING_ALLOWED=YES、CODE_SIGNING_REQUIRED=YES。未额外覆盖签名设置执行 xcodebuild archive（Release、generic/platform=iOS）成功；codesign --verify --deep --strict 通过，团队匹配且非adhoc。使用 debugging 方法本地导出IPA成功，解包后签名验证通过且包含 provisioning profile。全套16项模拟器测试通过。未安装到真机，未上传商店。

**修复 commit：** 7041d030d89fb2cb04e7569d650834acadf1625b

<a id="p2-24"></a>

### P2-24 · 历史页的「还没拍」与补拍提示使用不同口径

**验证状态：** 模拟器实测（隔离测试数据）

**代码位置：** ShotList/Views/HistoryView.swift · neverShot / statRow / footnote；ShotList/Views/RootTabView.swift · badge

**预期结果：** 历史待拍筛选与所选日期补拍口径一致，从未拍单独展示。

**问题详情**

「还没拍」统计块和「未拍」筛选表示从未拍过；今天的「下一个」与标签徽标表示今天没有拍过，包含以前拍过的镜头。各自数字自洽，但同屏语义不明确，按补拍提示去筛选会找不到对应镜头。

**复现方法（修复前）**

1. 准备镜头 A：昨天有片段、今天没有；镜头 B：从未有片段。
2. 进入历史页并选中今天。
3. 观察「还没拍」只有 1 个，但今日待拍徽标为 2，「下一个」可能指向 A。
4. 点击「还没拍」或「未拍」，列表只显示 B，找不到提示需要补拍的 A。

**修复状态：** 已修复

**修复说明：** 将未拍口径明确标为从未拍，增加按选中日期计算的待拍筛选；今日补拍提示可点击，直接显示全部当日待拍镜头。保留日历展开收起功能。

**验证结果：** iOS 26.5 独立模拟器App构造昨天拍过A、从未拍B、今天拍过C：从未拍数量1；今日待拍提示2，点击后显示A/B且待拍筛选选中；点击从未拍仅显示B。构建及实际界面验证通过。

**修复 commit：** 69365b5a690d1a7c38e919e1a036ace0584273b7

<a id="p2-25"></a>

### P2-25 · 前一次导入完成会清空后一次相册选择的目标

**验证状态：** 模拟器实测（隔离测试数据）

**代码位置：** ShotList/Views/ShotFlowModifier.swift · pickerItem.onChange / drainQueue / importMovie

**预期结果：** 异步导入结束不改写后续选择目标，重叠导入期间加载状态正确。

**问题详情**

多个导入共用 `pickerTarget` 和 `isImporting`。前一个任务结束时无条件清空目标并隐藏进度，可能覆盖后一个操作的状态；随后选择的视频因 target 为 nil 被直接忽略，也没有错误提示。

**复现方法（修复前）**

1. 为镜头 A 开始导入，通过可控延迟让导入保持等待。
2. 导入未结束时，为镜头 B 打开相册选择器，暂不选择视频。
3. 让 A 完成，再在选择器中选择 B 的视频。
4. 检查 B：视频没有导入，因为 A 的 defer 已将 pickerTarget 清空。若两个任务已启动，先完成的一次还会提前隐藏进度。

**修复状态：** 已修复

**修复说明：** 相册选择完成时同步取出并清空本次目标；导入任务结束不再触碰后续选择目标，进度按activeImports计数，最后一个任务结束才隐藏。

**验证结果：** iOS 26.5系统PhotosPicker实测：对A导入延迟25秒，期间为B打开选择器；确认A已新增片段且B选择器仍打开，再选择B视频，B正确新增片段。临时延迟已移除。

**修复 commit：** 71ac26d4a562b440602d483ef59eaf0fb9ba49b2

<a id="p2-26"></a>

### P2-26 · 导入目标被删除后仍返回成功，并残留临时文件

**验证状态：** 模拟器实测（隔离测试数据）

**代码位置：** ShotList/Models/ShotStore.swift · addClip；ShotList/Views/ShotFlowModifier.swift · importMovie

**预期结果：** 目标删除时明确失败，相册自有中转文件回收。

**问题详情**

异步导入结束时目标镜头可能已被删除。`addClip` 找不到目标便直接 return，不抛错，调用方仍执行成功反馈。素材没有加入任何镜头，中转文件也没有在本次操作结束时清理。

**复现方法（修复前）**

1. 在隔离目录创建镜头和临时视频，记录镜头 ID。
2. 删除该镜头，再用原 ID 调用 addClip。
3. 观察方法无错误返回，但 clips=0、source_exists=true。
4. 对应界面路径是：视频导入等待期间删除目标镜头或清空全部分镜，之后等待导入完成。

**修复状态：** 已修复

**修复说明：** addClip找不到目标时抛出targetMissing；ImportedMovie.save统一管理本次中转文件，在成功、失败或取消后回收，错误经现有导入提示向用户展示。

**验证结果：** 9项存储测试通过；目标删除后调用addClip确实抛错且不触碰调用方源文件，ImportedMovie.save失败后清理其拥有的源文件，无新片段/孤儿。模拟器实际PhotosPicker导入后注入删除目标，显示目标分镜已被删除提示，tmp中无import-*残留；探针已移除。

**修复 commit：** 108a2e9630d0df6475bb903426faa6d317a6e7c3

<a id="p2-27"></a>

### P2-27 · 保存回看片段期间仍能重拍，导致源文件被提前删除

**验证状态：** 模拟器实测（隔离测试数据）

**代码位置：** ShotList/Views/CameraCaptureView.swift · reviewScreen / save / retake

**预期结果：** 保存完成前不能重拍删除源文件，意外关闭后安全收尾。

**问题详情**

保存按钮受 `isSaving` 保护，「重拍」按钮没有保护。保存任务等待时长读取时，用户仍可触发 retake，删除正在保存的 reviewURL 并恢复取景；旧保存任务恢复后复制源文件失败，新录制也可能已经开始。

**复现方法（修复前）**

1. 录制一条测试视频，进入回看页。
2. 在测试构建中延迟 VideoMetadata.duration 的返回。
3. 点击「使用这条」后，趁正在保存点击「重拍」。
4. 恢复时长读取，检查旧保存任务是否因源文件已被删除而报错。

**修复状态：** 已修复

**修复说明：** 保存期间禁用重拍与交互关闭，动作入口再次检查保存状态；页面意外消失时由保存任务完成后回收源文件，避免提前删除和恢复已关闭的相机。

**验证结果：** iOS 26.5 隔离模拟器以合成视频进入真实回看页面，并临时延迟保存 15 秒：保存期间重拍和两个保存按钮退出可操作目标，源文件仍存在；完成后片段由 2 增至 3，回到列表且源文件回收。移除全部临时探针后 build_run_sim 通过。模拟器不支持真实摄像头，未验证真机录制。

**修复 commit：** 078d7e419852e0f49b7ed41bd0d0cb150beb1b08

<a id="p2-28"></a>

### P2-28 · 同时间戳片段交换镜头顺序后，文件名编号错位

**验证状态：** 隔离存储测试

**代码位置：** ShotList/Models/ShotStore.swift · normalize / syncClipFileNames

**预期结果：** 排序及重载后文件名前缀匹配当前编号，冲突不覆盖内容，写入失败保留旧关联。

**问题详情**

不同镜头可以拥有相同时间戳 token。交换镜头顺序时，两个文件的目标名互相占用，重命名逻辑因目标存在全部跳过。内存编号已更新，后续 normalize 不会再尝试改名，文件名与镜头编号长期不一致。

**复现方法（修复前）**

1. 在隔离目录准备两个镜头，分别引用镜头01_20260915_120000.mov 和镜头02_20260915_120000.mov。
2. 加载 ShotStore，交换两个镜头的顺序。
3. 检查记录：镜头 1 仍引用镜头02_…，镜头 2 仍引用镜头01_…。
4. 重新加载，确认错位仍存在；原文件内容没有被覆盖。

**修复状态：** 已修复

**修复说明：** 冲突时选择带正确编号的唯一文件名，保留旧文件直至 JSON 原子提交；normalize 同步检查文件名，使历史错位记录也能修复。

**验证结果：** iOS 26.5 隔离存储 XCTest：同时间戳双片段交换后验证编号前缀、片段 ID、文件内容和重载一致；注入 JSON 写入失败验证原文件与引用保留、无暂存泄漏；历史错位自动修复且二次 normalize 无变化。ShotStoreTests 10/10 通过。

**修复 commit：** 76bbf4574941727a18ee3a506501f303b8743412

<a id="p2-29"></a>

### P2-29 · 删除失败仍移除记录，孤儿清理虚报成功数量

**验证状态：** 隔离存储测试及模拟器实测

**代码位置：** ShotList/Models/ShotStore.swift · removeClip / removeAllClips / removeFile / removeOrphanFiles

**预期结果：** 删除失败保留剩余片段关联并可重试，清理数量只计实际成功，恢复失败不丢追踪。

**问题详情**

文件删除错误被 `try?` 吞掉，内存和 JSON 中的引用仍被移除，未删掉的视频变成孤儿文件。`removeOrphanFiles` 返回尝试删除的数量而非成功数量，调用方也没有得到失败反馈。

**复现方法（修复前）**

1. 在隔离测试中注入 FileManager，让指定视频的 removeItem 抛出权限错误。
2. 对包含该视频的镜头调用 removeAllClips。
3. 检查结果：clips=0、orphans=1，文件没有删除但已失去引用。
4. 继续调用 removeOrphanFiles：返回 1，实际剩余孤儿文件仍为 1。

**修复状态：** 已修复

**修复说明：** 删除前原子写入关联恢复日志，JSON 提交后再删除素材；失败文件恢复原分镜和片段顺序，恢复写入失败时保留日志并暂停变更，重载恢复。孤儿清理只计成功数量，失败通过页面内提示反馈。

**验证结果：** iOS 26.5：ShotStoreTests 12/12 通过，覆盖单条/全部片段/分镜/全库删除权限失败、部分孤儿清理、解除故障重试，以及恢复 JSON 写入失败后重启恢复。隔离模拟器在真实片段删除入口注入失败：操作未完成提示可见，原 3 段片段保持，未失去引用。移除临时探针后 build_run_sim 通过。

**修复 commit：** ce8ecad32bdaf429caadecd031f5cffd46cf98c9

<a id="p2-30"></a>

### P2-30 · 历史卡片显示当天素材，VoiceOver 却朗读全量最新素材

**验证状态：** 模拟器实测（隔离测试数据）

**代码位置：** ShotList/Views/HistoryView.swift · card；ShotList/Views/ShotCardView.swift · accessibilityLabel

**预期结果：** 历史卡片无障碍描述与卡片显示的日期范围、次数和时长一致。

**问题详情**

历史卡片通过 displayClip、takeCountOverride 等参数显示选中日期的素材，但无障碍标签仍直接使用 shot.accessibilityDescription。朗读使用整个镜头的最新片段与总条数，与当前卡片的时间、数量和时长不一致。

**复现方法（修复前）**

1. 同一镜头准备昨天 1 条、今天 2 条片段，各条时长设为不同值。
2. 在历史页选择昨天并开启 VoiceOver。
3. 聚焦该卡片，对比视觉内容与朗读内容。
4. 视觉应只显示昨天的素材，当前朗读逻辑却使用今日状态、共 3 条和今天最新时长。

**修复状态：** 已修复

**修复说明：** 无障碍标签改用与可见卡片相同的 effectiveClip / effectiveTakeCount / effectiveTotalDuration，朗读所示片段日期、时长与当前统计条数。

**验证结果：** iOS 26.5 模拟器运行时无障碍树：镜头3昨天1条7秒、今天2条11/13秒；选择昨天，标签为共1段、昨天、0:07，与界面一致；切回今天为共2段、0:13、总时长24秒。未进行真人听读评估。

**修复 commit：** ecdc4c3e8b5a28193150347dd535d7ea5e829874

<a id="p2-31"></a>

### P2-31 · 缩略图解码失败后一直显示加载动画

**验证状态：** 模拟器实测（隔离测试数据）

**代码位置：** ShotList/Views/ClipThumbnailView.swift · picture / loadImage；ShotList/Support/Haptics.swift · decodeThumbnail

**预期结果：** 缩略图解码失败后显示失败占位，结束加载状态。

**问题详情**

视图用 image=nil 同时表示加载中和加载失败。视频存在但无法解码时，加载器返回 nil，视图仍因 URL 非空显示 ProgressView，没有失败占位或重试状态。

**复现方法（修复前）**

1. 在隔离测试中准备一个包含非视频字节的 .mov 文件，并让测试镜头引用它。
2. 打开包含该镜头的列表，等待缩略图任务结束。
3. 确认解码已返回 nil 后，观察卡片仍持续显示加载动画。

**修复状态：** 已修复

**修复说明：** 将解码失败从加载中状态分离；返回nil后显示video.slash占位，URL变化时重置失败状态，成功结果仍显示缩略图。

**验证结果：** iOS 26.5 独立App用非视频字节的mov文件复现：加载结束后显示灰色失败占位且无转圈；同屏两段正常视频继续显示首帧、播放标志和时长。构建及截图检查通过。

**修复 commit：** 5876bff0a6f34ab85cfef53638f50bf1c197871c

<a id="p2-32"></a>

### P2-32 · 主素材文件名预览默认取最后一条，可能与实际导出不一致

**验证状态：** 模拟器实测（隔离测试数据）

**代码位置：** ShotList/Views/ClipOptionsSheet.swift · previewFileName；ShotList/Views/ExportView.swift · exampleMainFileName；ShotList/Models/Shot.swift · latestByRecordedAt

**预期结果：** 主素材文件名预览与实际导出的最新片段序号一致。

**问题详情**

两处预览重复使用 clipCount 作为主素材条号，实际导出却按 recordedAt 选择最新片段。时钟回拨等情况会让追加到末尾的片段时间更早，此时主素材不在数组末尾，预览条号与实际导出条号不同。

**复现方法（修复前）**

1. 构造同一镜头的两条片段：数组第 1 条记录为 12:00，第 2 条记录为 11:00。保持该内存顺序，避免重新加载时排序。
2. 打开镜头面板或导出页，记录主素材文件名预览。
3. 用同一份数据生成导出包。
4. 预览使用第 2 条编号，实际根目录主素材是第 1 条；对比两者文件名。

**修复状态：** 已修复

**修复说明：** 在Shot中统一计算latestTakeIndex，两处预览使用该条号，保持时间戳选主素材的规则与导出一致。

**验证结果：** 4项导出测试通过；新增时钟回拨数据（第1条时间晚于第2条），验证预览01-1与实际生成指南主素材一致。模拟器用临时逆序测试数据检查镜头面板显示第1条为最新，文件名03-1一致；探针已移除。

**修复 commit：** f921b003f797ae90b31e2c7ee883fdc4f8590105

<a id="p2-33"></a>

### P2-33 · 大视频同步复制占用主线程，保存和导入可能卡住界面

**验证状态：** 隔离线程测试及模拟器实测；真机耗时未测

**代码位置：** ShotList/Models/ShotStore.swift · addClip；ShotList/Views/CameraCaptureView.swift · save；ShotList/Views/ShotFlowModifier.swift · importMovie

**预期结果：** 素材复制离开主线程，复制期间界面可响应，状态变化或取消不产生错误归属和孤儿素材。

**问题详情**

`ShotStore` 是 MainActor 类型，addClip 内同步执行 copyItem。调用方放在 Task 中不会把这段复制自动移到后台。时长读取虽已后台化，大视频复制仍占用主线程；具体延迟取决于文件大小、文件系统和设备，尚无真机耗时结论。

**复现方法（修复前）**

1. 在真机测试环境准备较大的视频，从相册导入或保存录制回看。
2. 用 Instruments Time Profiler 记录保存阶段，观察主线程调用栈。
3. 检查 addClip → FileManager.copyItem 是否在主线程执行，以及复制期间滚动、点击和进度动画的响应。

**修复状态：** 已修复

**修复说明：** 相册中转与素材入库共用显式后台复制器；在独立临时文件完成复制后回主协程重新确认目标、当前编号与取消状态，再移动并提交元数据。失败和取消回收半成品，调用者源文件保留至提交成功。

**验证结果：** iOS 26.5：隔离 XCTest 在 copyItem 内断言非主线程并阻塞复制，验证主协程仍可排序，复制后使用新编号；目标删除、取消、JSON 写入失败均保持源文件且无暂存泄漏。模拟器真实相册导入时以 25 秒同步复制延迟探针验证仍可切换历史页，完成后当天片段由 2 增至 3。探针已移除，完整测试 24/24 通过。未测真机大文件耗时，未给出性能倍数结论。

**修复 commit：** 7ece9e6bdeb92aa8ef30426b864e74ae01a760bd

<a id="p2-34"></a>

### P2-34 · 相册导入触发系统整段转码，导入耗时长且画质被降

**验证状态：** 隔离测试实测及模拟器实测；真机导入耗时与转码耗时未测

**代码位置：** ShotList/Views/ShotFlowModifier.swift · photosPicker；ShotList/Support/MediaTranscode.swift · export；ShotList/Support/ExportPackageBuilder.swift · build / copyOrTranscode；ShotList/Support/ExportSession.swift · build / invalidate

**预期结果：** 导入与拍摄只负责拿到原文件，不在这一步转码；需要更小体积或更好兼容性时在导出页按需选择格式，且可反复改、反复导。

**问题详情**

`PhotosPicker` 未指定 `preferredItemEncoding`，系统按默认的「自动」策略在把文件交给 App 之前先整段转一次码。这段转码发生在系统侧：App 期间收不到任何回调，界面上只剩一个转圈；耗时与素材时长正相关（一段两分钟的 4K 素材要等数分钟），画质还会被降一档。原文件已经兼容时，这层转码纯属浪费——同卷直接交付是 APFS 克隆，实测复制 800MB 文件耗时不足 0.01 秒。转码时机本身也错位：用户此刻的诉求是「先拿到素材」，格式诉求到导出时才明确。

**复现方法（修复前）**

1. 在测试设备相册放入一段较长的高分辨率视频（例如 2 分钟 4K）。
2. 进入「分镜」页，为某个镜头打开相册选择器并选中该视频。
3. 观察选中到写入片段之间的等待：等待期间无进度回调，界面只有转圈，时长随素材时长增长。
4. 对照：在同一卷上直接复制等体积文件（APFS 克隆）几乎瞬时完成，说明耗时不来自 App 内的拷贝。

**修复状态：** 已修复

**修复说明：** 相册拾取显式声明 `preferredItemEncoding: .current`，直接取相册原片；转码整体后移到导出阶段，导出页新增「视频格式」选项（原片 / H.264 · 1080p / HEVC · 1080p，走 `AVAssetExportSession` 的 1920x1080 预设，属上限语义、小素材不放大），逐条转码并回报进度，选「原片」时保持原有复制路径。格式选择存在偏好里，镜头面板的文件名预览与导出读同一个扩展名函数，不会出现预览与产物不一致。打包期间改动范围或格式即置取消标记，在途导出在下一条素材前停手。

**验证结果：** XCTest 30/30 通过（打包 8、会话 5、仓库 15、缩略图 2），其中新增 4 项导出测试（转码档改扩展名、转码替换复制且编号文件名同步、转码失败指出文件名并提示改回原片、取消后不再启动转码）与 2 项会话测试（失效向在途打包传取消、进度在打包中发布并在结束后清空）。iOS 26.5 模拟器用 HEVC 占位素材走真实导出链路：产物为 H.264 的 `.mov`，包内主素材、备用片段、CSV、指南与说明的编号命名一致。真机 4K 长素材的实际导入耗时与转码耗时未实测；PhotosPicker 选片无法自动化，`.current` 的收益依据是系统不再插入转码以及同卷复制的实测耗时。临时探针已移除，移除后重新构建与全量测试通过。

**修复 commit：** 99763b8ffaffda8131cebff97002677568fa7651

<a id="p2-35"></a>

### P2-35 · 一次只能选一段视频，多段素材要重复走一遍导入流程

**验证状态：** 隔离测试实测及模拟器实测；选中多段后的端到端落地未实测

**代码位置：** ShotList/Views/ShotFlowModifier.swift · photosPicker / onChange(pickerItems) / importMovies；ShotList/Views/ClipOptionsSheet.swift · 从相册导入按钮；ShotList/Support/MediaImport.swift · MovieImporter

**预期结果：** 一次可以从相册选中多段视频，按选择顺序连续导入同一个镜头，中途不必退出弹层重来。

**问题详情**

选择器绑定单个 `PhotosPickerItem`，系统在按下第一段视频时即完成本次选择并收起。N 段素材因此要走 N 遍「打开镜头面板 → 点导入 → 等面板关闭 → 等选择器 → 选中 → 等导入」，每遍都要重新定位同一个镜头。导入本身的等待已经不长（见 P2-34），这里耗掉的时间主要是弹层往返。同一件事还带出两个连带问题：镜头面板入口写的是「从相册再添加**一条**」，与实际能接受的素材量语义不符；导入提示固定为「正在导入视频…」，多段导入时看不出进行到第几段。

**复现方法（修复前）**

1. 在「分镜」页点开一个镜头，点「从相册导入」。
2. 试着连点两段视频：按下第一段后选择器立即收起并开始导入，第二段没有被选中。
3. 想再导入第二段，只能重新打开镜头面板、重新点导入，重新定位到同一个镜头。

**修复状态：** 已修复

**修复说明：** 选择绑定改为 `[PhotosPickerItem]`，选择器进入多选形态（出现确认按钮与选中计数）。一次选中的多段按选择顺序逐段导入同一个镜头；每段落地时取当前时间，因此后选的更新，与「最新一条是主素材」一致。推进逻辑抽到 `MovieImporter.run`：逐段执行、单段失败不打断后面的、失败原因按发生顺序收集。导入提示带序号（「正在导入 3 / 5」），只有一段时不显示数字；结束时按单段失败 / 部分失败 / 全部失败给三种文案。进度状态携带批次 id，只有发起这一批的任务能改进度、能收回指示器，避免上一批的收尾动到下一批（P2-25 同类）。镜头面板入口文案去掉「一条」。

**验证结果：** XCTest 35/35 通过（打包 8、会话 5、批量导入推进 4、仓库 16、缩略图 2）。新增 `MovieImporterTests` 4 项覆盖顺序不变、中途失败不中断、失败原因按序收集、空选择不触发；新增仓库测试验证连续落三段后的顺序与时长、最后一段即主素材，以及**同一秒内连落三段的文件名不互相覆盖**（批量导入必然撞上时间戳 token 相同）、扩展名各随源文件。iOS 26.5 模拟器实测：相册选择器确实进入多选形态（右上出现确认按钮，单选形态下没有该按钮），导入提示渲染为「正在导入 3 / 5」。模拟器无法合成点击，**「选中多段后连续落地」只验证到选择器形态与推进逻辑，没有在真实相册里端到端跑过**；真机相册未实测。

**修复 commit：** 206fc18781b5fe0570249d9cdf1fa68e74269973

<a id="p2-36"></a>

### P2-36 · 选择 4K 或 60 fps 后单条录制会更早触到 600 MB 上限，停止时不给任何说明

**验证状态：** 代码级确认（未在真机实测触发）；用户于 2026-09-21 指示按已修复收口，未验证项以用户的验收结论为准

**代码位置：** ShotList/Camera/CameraRecorder.swift · maximumFileSize / configureIfNeeded、fileOutput(_:didFinishRecordingTo:from:error:)

**预期结果：** 单条录制因体积或时长上限结束时，向用户说明「这条已达上限并被保存」，并且在选到高码率格式时不至于在远未到 10 分钟时就被截断。

**问题详情**

`maxRecordedFileSize` 固定为 600 MB，与选中的分辨率、帧率无关。录制设置放开之后，同样的时长在高分辨率/高帧率下会产生成倍的字节数：

- 4K 与 60 fps 的码率远高于 1080p 30 fps，因此体积上限会先于时长上限生效，拍摄在远未到 10 分钟时就自行停止；
- `fileOutput(_:didFinishRecordingTo:from:error:)` 只区分「成功返回」与「失败」，达到上限时 AVFoundation 会带着 `AVErrorMaximumFileSizeReached` / `AVErrorMaximumDurationReached` 并把 `recordingSuccessfullyFinished` 置为 true，现有代码据此判为成功，于是界面直接进入回看，用户看到的是「突然自己停了」而没有任何解释；
- `activeVideoMinFrameDuration` 由本应用设置为目标帧率，因此上限何时生效可确定由应用选择的格式决定，不是随机现象。

定级依据：正常使用（默认 1080p 30 fps）下一般碰不到；选择 4K/60 fps 并拍摄较长素材时会稳定触发，且素材被提前截断而用户无从判断原因。触发后素材仍在、可重拍，有绕行方式（改用较低画质或分段拍），因此定为 P2 而不是 P1。未在真机实测，具体在几分钟处停止取决于设备与编码器，本清单不给出未经测量的分钟数。

**复现方法**

1. 在真机上进入取景页，右上角打开拍摄设置，选择「4K」+「60 fps」。
2. 连续拍摄一段超过 1 分钟的素材（具体时长取决于设备码率）。
3. 观察：录制会在远未到 10 分钟时自行停止并直接进入回看，界面没有任何关于上限的说明；「回看第 N 条」正常，素材可用。
4. 对照 code 位置确认该次停止来自 `maxRecordedFileSize`，而不是时长上限或中断。

**修复状态：** 已修复

**修复说明：**

根因有两处，均在 `ShotList/Camera/CameraRecorder.swift`：

1. `fileOutput(_:didFinishRecordingTo:from:error:)` 只按 `recordingSuccessfullyFinished` 分「成功／失败」两类，撞上限（`error` 非空但素材完好）与用户主动停止（`error` 为 nil）被合并成同一个 `.success(URL)`，界面因此拿不到「是不是撞了上限」的信息。
2. `configureIfNeeded` 把 `movieOutput.maxRecordedFileSize` 设成写死的 `600 * 1024 * 1024`，且只在初次配置时赋值一次，不随分辨率、帧率变化。

修复：

- 新增 `CameraRecorder.StopReason`（`userRequested` / `reachedLimit`）与 `RecordingOutput`（`url` + `stopReason`），`startRecording(completion:)` 的回调类型从 `Result<URL, Error>` 改为 `Result<RecordingOutput, Error>`。
- `fileOutput(...)` 判定成功后，新增 `stopReason(matching:)`：把 `error` 转成 `AVError`，`.maximumFileSizeReached` / `.maximumDurationReached` 记为 `.reachedLimit`，其余情况（`error` 为 nil，或虽非 nil 但不是这两种 code，例如中断、磁盘写满）一律记为 `.userRequested`——不会把「来电中断但素材完好」误报成「撞了上限」。
- `CameraCaptureView.toggleRecording()` 读取 `output.stopReason`；`.reachedLimit` 时置位新的 `@State reachedRecordingLimit`，驱动一条新增的短提示 alert「已达录制上限」／「这段已保存，可以回看或重拍。」，`enterReview(output.url)` 两种情况下都照常执行，视频本身不受影响。
- 新增 `CameraRecorder.updateMaximumFileSize()`，在 `applyFormatLocked(...)` 每次成功套用格式后调用（覆盖初次进相机、设置面板改分辨率/帧率、切前后摄像头三个入口）：读 `movieOutput.outputSettings(for:)` 里 `AVVideoCompressionPropertiesKey/AVVideoAverageBitRateKey`——这是 AVFoundation 针对当前 `activeFormat` 实际会用的平均码率，不是自己按宽高猜的——按「码率 × 10 分钟时长上限 × 1.15 余量」换算成字节数，与原来的固定 600 MB 取较大值写回 `maxRecordedFileSize`。取不到码率（连接不存在、`AVVideoAverageBitRateKey` 缺失或为 0）时退回固定 600 MB。用 `max(...)` 兜底确保低码率格式下限不变，只对高码率格式放宽。
- 顺带修正了同一根因下两处现在已经过期的说明文案：`ShotList/Views/CameraSettingsSheet.swift` 设置面板底部说明（原文断言「4K 或 60 fps 会更早碰到体积上限」，修复后不再准确），以及 `README.md` 两处（功能列表一行 + 「已知限制」段落，原文同样断言 600 MB 是固定值）。

**验证结果：** - 构建：`xcodebuild -project ShotList.xcodeproj -scheme ShotList -destination 'generic/platform=iOS Simulator' -configuration Debug build` → `** BUILD SUCCEEDED **`；核对完整日志无新增警告，唯一出现的 `appintentsmetadataprocessor` 提示（"Metadata extraction skipped, no AppIntents.framework dependency found"）是本项目固有提示，与本次改动无关。
- 隔离的代码级验证：把 `fileOutput(...)` 与 `stopReason(matching:)` 的判定逻辑原样搬到仓库外的独立 Swift 脚本，用真实 `AVError`/`NSError`（`AVFoundationErrorDomain`）构造样本，`swift <script>.swift` 直接执行，覆盖 6 种分支：① 用户主动停止（`error` 为 nil）→ `success(userRequested)`；② 撞 `.maximumFileSizeReached` 且 `recordingSuccessfullyFinished=true` → `success(reachedLimit)`；③ 撞 `.maximumDurationReached` 且 `recordingSuccessfullyFinished=true` → `success(reachedLimit)`；④ 其他「素材完好」但不属于这两种 code 的错误（用 `.diskFull` 代表）→ `success(userRequested)`，确认不会被误判成撞上限；⑤ `recordingSuccessfullyFinished=false` → `failure`；⑥ `userInfo` 完全不带该 key → `failure`。6 项全部通过。验证后已删除该临时脚本，未写入任何单元测试文件、未纳入提交。
- 待验证（需要真机摄像头，本环境沙盒无真机、模拟器无摄像头，无法执行）：`updateMaximumFileSize()` 在真实设备上从 `outputSettings(for:)` 实际读到的 `AVVideoAverageBitRateKey` 数值是否符合预期量级；4K / 60 fps 实拍时体积上限是否确实不再远早于 10 分钟触发、具体在第几分钟触发；撞上限后回看页新增的「已达录制上限」提示的真实弹出时机、文案排版与交互手感；`updateMaximumFileSize()` 在 `switchCamera()` 路径下前后摄像头来回切换时的实际取值表现。

用户验收（2026-09-21）：用户指示将本条移入已修复。真机阶段未能覆盖的项——① `updateMaximumFileSize()` 在真机上从 `outputSettings(for:)` 读到的平均码率；② 4K / 60 fps 实拍时体积上限是否不再远早于 10 分钟触发；③ 撞上限后「已达录制上限」提示的真实弹出时机；④ 前后摄像头来回切换时的取值——以用户的验收结论为准；用户侧的设备与逐步观察未在此逐项记录。

**修复 commit：** `bdbbba6d337055543dcc7aea6f355f880aaf7063`

**编号说明：** 本条原登记为 P2-34。同一编号同时被 main 上并行登记的「相册导入触发系统整段转码，导入耗时长且画质被降」占用——本条登记于 2026-09-16 00:47，该条登记于同日 00:56，本条在先，但 main 为主干且其条目已先行合入，故由本条让号，改编号为 P2-36（当时 P2 前缀最大序号 35 加一）。原编号 P2-34 不再复用。

<a id="p2-37"></a>

### P2-37 · 缩放上限被硬编码为 8×，不随设备与格式变化

**验证状态：** 隔离单元测试；真机上的系统推荐区间未测

**代码位置：** ShotList/Camera/CameraRecorder.swift · zoomScale(for:)（原 maximumZoomFactor 常量）；ShotList/Camera/CaptureFraming.swift · ZoomRangePolicy

**预期结果：** 界面给出的可滑倍率区间与当前格式下系统推荐的区间一致；设备收窄可用范围时不会给出到不了的档位。

**问题详情**

界面可滑到哪由 `min(maxAvailableVideoZoomFactor, 8)` 决定，8 是应用自己拍的固定值，既不随设备变，也不随当前格式变，两头都会偏：

- 设备把镜头切换行为设为锁定或受限时，`minAvailableVideoZoomFactor` 会被抬到当前那颗镜头的换镜头倍率（见 AVCaptureDevice.h 中 `AVCapturePrimaryConstituentDeviceSwitchingBehaviorLocked` 的说明），`maxAvailableVideoZoomFactor` 同步收窄。界面仍按固定上限画档位与吸附点，会给出设备此刻到不了的倍率。
- 系统在 `activeFormat` 上提供 `systemRecommendedVideoZoomRange`，这正是内建相机缩放控件使用的区间，回答的是「该让用户滑到哪」。硬编码的 8× 会把这个推荐整个盖掉：推荐区间比 8× 窄时界面给出系统并不推荐的档位，比 8× 宽时又拿不到系统认为可用的区间。

定级依据：不影响录制结果，只在部分设备状态下让档位与吸附点偏出可用范围，且双指捏合时仍会被 `setZoom` 收进设备真实范围，有绕行方式，因此定为 P2 而不是 P1。

**复现方法**

模拟器上不可复现：没有摄像头时 `configureIfNeeded` 先返回失败，取景页走降级分支，`refreshDeviceCapabilities` 与 `zoomScale(for:)` 都不执行。可用代码级验证：

1. 向 `ZoomRangePolicy.range` 传入推荐区间与可用区间，观察结算结果。`CaptureFramingTests` 已覆盖推荐更窄、推荐更宽、推荐为 nil、取交为空、可用上下界颠倒五种情形，以及区间收窄后档位随之减少。
2. 真机验证：进入取景页，换到可用范围被收窄的设备状态（如锁定镜头切换），观察焦距胶囊与吸附点是否仍停在固定上限上。

**修复状态：** 已修复

**修复说明：** 区间改由 `ZoomRangePolicy.range` 结算：优先取 `activeFormat.systemRecommendedVideoZoomRange`，并与 `minAvailable/maxAvailable` 取交集——不能直接采用推荐值，头文件写明可用区间会在推荐区间**之内**再被收窄，否则仍会给出到不了的档位。系统未提供推荐值时（该属性允许为 nil）退回「可用区间压到 8×」的老口径。取交为空、或设备上报的上下界颠倒时退回实际可用区间，避免 `ClosedRange` 越界构造崩溃。删除 CameraRecorder 中的固定上限常量，区间随格式变化重新结算（换分辨率/帧率与切前后摄都会走一次）。

**验证结果：** iOS 26.5 / iPhone 17 Pro Max 模拟器：`xcodebuild test` 58 项全过（原 49 项 + 新增 9 项），构建无警告新增；应用可启动，取景页验收页 `-chromeDemo 1` 正常渲染。**未验证**：模拟器没有摄像头，`zoomScale(for:)` 这条路径整体不执行，真机上 `systemRecommendedVideoZoomRange` 的具体上下界、以及新口径下焦距胶囊的实际排布（尤其是否会因此丢掉 0.5× 档）均未实测；本轮截图未目视核对，仅确认渲染成功。

**修复 commit：** 0485917f13b094059ddbd0a578a8a63fe22993e0

**编号说明：** 本条原登记为 P2-35，与 main 上并行登记的「一次只能选一段视频，多段素材要重复走一遍导入流程」重号（该条登记于 2026-09-16 01:06，先于本条 09:18），合并时由本条让号，改编号为 P2-37。修复提交 `0485917` 与登记提交 `5e22847` 的提交信息沿用原编号，追踪以 SHA 为准；原编号 P2-35 不再复用。

<a id="p2-38"></a>

### P2-38 · 有镜头的列表状态下点导航栏标题无反应，改名弹窗不出现

**验证状态：** 模拟器实测

**代码位置：** ShotList/Views/ShotListView.swift · `filmTitleButton` / `toolbarContent`（`ToolbarItem(placement: .largeTitle)`）、`beginTitleEdit()`

**预期结果：** 分镜页导航栏标题可点，点按弹出「影片标题」改名弹窗——这是该按钮唯一的存在理由。空状态下同一个按钮已按此预期工作，列表状态不应有区别。

**问题详情**

标题改名按钮挂在 `ToolbarItem(placement: .largeTitle)`，由 `Button(action: beginTitleEdit)` 把 `isEditingTitle` 置位，再由挂在根页内容上的 `.filmActionDialogs(...)` 打开 `.alert("影片标题", isPresented:)`。当根页内容是 `List`（当前影片有镜头）时，这个按钮收不到触摸：

- 真实触摸标题文字正中（原生 3x 像素 264,255 → 点 88,85；落在标题墨迹 cols 64..461 / rows 212..305 之内）没有任何反应；同一行上沿、下沿与左右两端共 5 个点扫描同样全部无反应；
- **同一时刻、同一条导航栏**上的加号与三点正常：三点能弹出影片菜单，加号能弹出镜头面板。导航栏本身没有整体失效；
- 「触摸没到按钮」与「触摸到了但弹窗不弹」两者已分开验证：把该按钮的 action 临时换成 `addShot`（会弹出「镜头 N」面板）后，点标题同样什么也不弹；而同一判据用在加号上立刻能测出面板 → **触摸根本没有到达按钮**，不是弹窗链路的问题；
- 弹窗链路另行单独验证：走「影片菜单 → 修改标题」能正常弹出同一个「影片标题」弹窗（按钮为「取消」「保存」），而该菜单项与 `beginTitleEdit` 写入的是同一个 `isEditingTitle`；
- 该按钮在无障碍树里也不作为按钮暴露：`app.navigationBars.buttons` 只有「添加分镜」「影片菜单」，导航栏本身以 `identifier: "夏日vlog"`（标题文本）暴露。

与已修复的 P1-9 对照，触发条件正好相反：P1-9 是「`largeTitle` 位自定义标题按钮 + 根页**非**滚动内容」把**整条**导航栏的触摸吃掉（内容改为 `ScrollView` 承载后恢复）；本条是根页为 `List` 时**只有**标题按钮被吃掉，加号与三点不受影响。空状态（P1-9 修复后的 `ScrollView` 承载 `ContentUnavailableView`）下同一个按钮可点、弹窗正常——本次在同一设备、同一构建下先后实测两种状态，结果分别为「不弹」与「弹窗弹出」，因此本条的范围限定在列表状态。

定级依据：点标题改名是代码注释里明确写出的既有设计（`filmTitleButton`：「系统 `navigationTitle` 在标签根页上是纯文本，点了没反应」，所以自己往 `largeTitle` 位放按钮），用户会去点它，而点了没有任何反馈。但改名有可用的绕行入口——右上三点「影片菜单 → 修改标题」，所以定为 P2 而不是 P1。根因（列表状态下这个按钮的触摸被谁接走）尚未定位，不在本清单中给出未经证实的推断。

**复现方法**

1. 前置：当前影片至少有一个镜头。模拟器可用 `Tools/seed-simulator.py <UDID>` 灌入演示数据，「夏日vlog」有 5 个镜头。
2. 进入「分镜」标签页，确认导航栏大标题为影片名、下方是镜头卡片列表。
3. 点导航栏标题文字：观察「影片标题」改名弹窗不出现，界面无任何变化。
4. 对照（同一屏、同一条导航栏）：点右上三点，「影片菜单」正常弹出；点右上加号，镜头面板正常弹出。
5. 对照（弹窗链路）：三点 →「修改标题」，同一个「影片标题」弹窗正常弹出，说明弹窗本身可用。
6. 对照（另一状态）：三点 →「重制」→「开一部空白影片」，进入空状态后点标题，改名弹窗正常弹出，说明按钮本身可用，只有列表状态收不到触摸。

真实触摸由 XCUITest 探针按下（探针工程放在 /tmp，不进入仓库）。

**根因（修复时定位）**

触发条件实测收敛为「标题按钮挂在 `.largeTitle` 槽 **且** 根页内容是 `List`」：该组合下槽位交给内容的可点矩形只剩底部约 10pt 一条（y=104/108 可触发），而绘制矩形仍覆盖整块文字（墨迹 y 70.7–102），绘制区与命中区错开。空状态（`ScrollView` 承载）下同一槽位两者重合，所以正常。是布局的哪一步把它们错开的没有继续深挖——修复改成不依赖该槽位的写法。

逐个变体实测（iOS 26.5 / iPhone 17 Pro Max，同一份数据、同一构建）：改 `.topBarLeading` 有效；改 action 为 `addShot`、换 `onTapGesture`、换 `simultaneousGesture`、换真 UIKit 控件、改 `.principal`、加大 `frame(height:)` 都无效。

**修复状态：** 已修复

**修复说明：** 标题按钮改挂 `.topBarLeading`——该槽位下内容的命中矩形与绘制矩形重合，列表与空两种状态都可点。配套三处必要改动：① 去掉 `.navigationTitle(filmTitle)`，留在标题行的那份系统大标题会叠在按钮上并吃掉点击；② 宽度从 `maxWidth:` 改为显式 `frame(width:)`，交给槽位协商时它只提案 36pt 宽，`minimumScaleFactor` 会把 34pt 的字压成一小团；③ 删掉 `largeTitle` 时代用于纵向顶位的 `filmTitleLift` 常量与上下等对边距。另外工具栏槽会给项套一层 iOS 26 玻璃胶囊、尺寸跟着这个 230pt 的框走，标题右侧会拖出一大片空白，用 `sharedBackgroundVisibility(.hidden)` 只关背景，按钮本身与无障碍语义保留。空状态那一层的滚动承载容器（P1-9 的修复）保持不动。

**验证结果：** iOS 26.5 / iPhone 17 Pro Max 模拟器，App 真实安装后由 XCUITest 探针按下真实触摸（探针工程放在 /tmp，不进入仓库）。四条用例全过：

- 列表状态：标题命中区 x 40/88/140/152 × y 70/78/84/92/100 共 20 个点全部弹出「影片标题」；y=106 落在墨迹（行 70.67–102.00）之下、不触发——命中区与绘制区一致；
- 无回归：内容区 y=120/150/200 仍照常打开镜头面板，右上加号弹出镜头面板、三点弹出影片菜单；
- 改名链路：弹窗 → 输入「验收改名」→ 保存，标题确实变成「验收改名」；
- 空状态：y=70/84/96 均可触发。

版式（原生 3x 像素）：改后标题墨迹 行 70.67–102.00、列 25.33..157.67，墨宽 132.67pt、墨高 31.67pt。与本次修复前的归档截图（`Docs/导航栏标题对齐-改前改后.png` 中间一行，即 012d8b8 之后的状态）逐项对比：列 21.33..153.67、墨宽 132.67pt、墨高 31.67pt——字号与纵向位置完全一致，只有左边缘右移 4.0pt（比历史页系统大标题「历史记录」的 21.00pt 右 4.3pt），属工具栏槽位的标准前导留白，成因与「为什么不补常量」写在 `filmTitleButton` 的注释里。

滚动与页面版式也一并核对：把镜头补到 35 个再滚到底，标题仍以纯文本停在导航栏上，不被列表内容压住或裁切；标题下方首张卡片的顶边为 124.67pt，与修复前的同页截图一致（修复前 124.67pt），换槽位没有挪动页面版式。**未验证**：特大辅助字号下的排布、超长影片名（>230pt，走 `minimumScaleFactor` 0.7 再截断）的实际观感。

**修复 commit：** 2f7c422d202afb5383b141079212e5d525faf40e

<a id="p2-39"></a>

### P2-39 · 镜头面板把没改过的编号也当成一次编辑写回，面板打开期间编号被重排后，只改描述也会把镜头挪回旧位置

**验证状态：** 模拟器实测（隔离测试数据 + 临时探针注入删除补偿）

**代码位置：** ShotList/Views/ClipOptionsSheet.swift · `draftNumber` 初始化、`flushDraft()`；ShotList/Models/ShotStore.swift · `update(_:)`、`recoverPendingDeletions()`（行号为修复前；修复后对应 `numberEdit` / `displayedNumber` / `flushDraft()`）

**问题详情**

**预期行为：** 用户只改了分镜描述（或字幕、角标），落盘就只改这三样文字，镜头在列表中的位置不动。

**实际行为：** `flushDraft()` 无条件把 `edited.number = clamped` 一起写回，`clamped` 来自 `@State draftNumber`——它在面板**打开的那一刻**由 `shot.number` 初始化，之后只有 Stepper 会更新它。因此只要面板打开期间这个镜头的编号被别的路径改过，用户接着敲一个字，`store.update(_:)` 就会按这个过期编号把镜头 `remove` + `insert` 回旧位置，并连带把它后面所有镜头重新编号、把磁盘上的片段文件改名。

**根因证据：**

- `ClipOptionsSheet.swift:62` `_draftNumber = State(initialValue: shot.number)`——`@State` 的初值只在视图身份建立时生效一次，面板打开后不会再跟随 `live.number`；
- `ClipOptionsSheet.swift:340-351` 的 guard 把「编号变了」和「文字变了」并成一个条件，任一命中就整条写回，其中包含 `edited.number = clamped`；
- `ShotStore.swift:427-431` 拿 `updated.number` 当目标位置做移动，不区分这个编号是用户改的还是过期快照。

**面板打开期间能改到编号的路径：** `ShotStore.commit()` 每次落盘都会调 `recoverPendingDeletions()`；一旦它真的找回过东西（`restored == true`），第 912–917 行会把**全部影片的全部镜头**按下标重新编号，`mergeRecovered` 还可能在中间插回一条整镜（第 967 行），使当前镜头的编号整体后移。此时面板里的 `draftNumber` 已经过期。触发前提是此前有过「JSON 已提交、文件没删成功」的残留日志，因此触发窗口窄，定为 P2 而不是 P1；但根因是「把未修改的字段也当成编辑写回」，与触发概率无关。

**同时澄清一项此前存疑的问题：** 「切换分镜顺序时分镜描述回填到别的镜头上」这一条，在当前代码里**不成立**。`flushDraft()` 是全应用唯一写 `note` / `caption` / `badgeText` 的地方（已按 `\.note = ` / `\.caption = ` / `\.badgeText = ` 全仓检索确认），它先取 `live = store.shot(withID: shot.id)` 再改字段，`ShotStore.update(_:)` 又按 `edited.id` 定位目标，两道都按 id 走，描述不会落到别的镜头上。拖拽排序、上移 / 下移、Stepper 改编号三条路都经 `normalize()` 按下标连续重编号，`update(_:)` 的「先摘后插」在上移与下移两个方向上的落点也都核对过（5 个镜头把第 2 个改成第 4 个 → 落在下标 3；把第 4 个改成第 2 个 → 落在下标 1），没有差一位。

**复现方法**

1. 隔离测试数据：新建一部影片，加 3 个镜头，都写上可区分的描述（A / B / C）。
2. 代码级验证（不破坏真实素材）：在 `ShotStore.recoverPendingDeletions()` 的 `if restored {` 分支入口临时插一条探针，确认进入该分支时会执行第 913–917 行的整库重编号。
3. 构造残留日志：用隔离目录的 `pending-deletions.json` 让恢复流程真的找回一条镜头（不要对用户真实的 Application Support 目录操作）。
4. 打开镜头 B 的面板，不动 Stepper，只在「分镜描述」里追加一个字，等 400 毫秒。
5. 观察分镜列表：镜头 B 被移回打开面板时的那个位置，后续镜头编号整体变化，磁盘上的片段文件名跟着改名。预期正确结果是只有 B 的描述变了，顺序不动。
6. 验证完成后移除全部探针与隔离数据。

**修复状态：** 已修复

**修复说明：** 根因是 `ClipOptionsSheet` 打开时把 `shot.number` 抄进 `@State draftNumber`，而 `flushDraft()` 把「编号变了」和「文字变了」并成一个条件、命中任一就整条写回（含编号），过期副本因此被当成一次用户编辑。修复把 `draftNumber` 换成只记录用户拨动的 `numberEdit: Int?`：没拨过（`nil`）时 `displayedNumber` 取 `live.number`，Stepper 与导出文件名预览都跟随仓库，`flushDraft()` 写回的编号就是仓库当前值，`ShotStore.update(_:)` 不会触发移动；拨过则夹进 `numberRange` 并按用户目标移动；落盘成功、或拨回与仓库一致后清回 `nil`，之后仓库编号再变也不会被旧编辑盖回；落盘失败保留用户拨的值，沿用原有重试行为。`ShotStore.update(_:)` 的移动语义不动，改动仅在 `ClipOptionsSheet.swift`。

**验证结果：** iOS 26.5 / iPhone 17 Pro Max 模拟器，演示数据由 `Tools/seed-simulator.py` 灌入（「夏日vlog」5 个镜头），操作与观察均在模拟器里完成，磁盘状态读容器内 `films.json`。

模拟器里没有别的路径能在面板开着时触发删除补偿（面板是模态；`commit()` 开头还会用已提交快照覆盖 `pending-deletions.json`，外部放的日志只在启动读取时生效），所以用临时探针：`ShotStore.init` 里起一个轮询任务，发现容器里出现 `probe-trigger` 文件就执行真实的 `recoverPendingDeletions()`；补偿日志由隔离脚本放进模拟器容器（在当前影片最前多一个磁盘上仍有片段文件的「被删」镜头 X），不触碰任何真实数据。探针与脚本不入库，探针已在提交前移除（提交只含 `ClipOptionsSheet.swift`）。

- **修复前（复现）：** 打开「手冲壶」镜头 03 面板（Stepper=3）→ 触发补偿：标题变「镜头 04」，Stepper 仍停在 3 → 只在描述末尾敲一个字，等 400 毫秒：`films.json` 里该镜头被挪回第 3 位、排到「手持稳定器」之前，后续片段文件同时改名。
- **修复后（同一路径）：** 触发补偿后 Stepper 跟着变成 4；只敲一个字，该镜头仍是第 4 位、在「手持稳定器」之后，只有描述变了。
- **回归（同一构建）：** Stepper「−」单独拨 4→3，400 毫秒后落盘，标题与文件名预览（夏日vlog-03.mov）同步；「+」后立刻点「完成」，4→5 落盘，列表顺序与磁盘一致；拨过并落盘后再触发一次补偿，Stepper 跟到 4，再敲一个字顺序不变（验证 `numberEdit` 落盘后清回 `nil`）。
- **干净构建（无探针，仅含修复）：** 打开面板不动、等待后点「完成」，`films.json` 的 md5 与打开前一致（不多写盘）；Stepper「−」3→2 落盘；只敲一个字，位置不动、只有描述变化。构建成功，无新增告警（既有的 `FilmBar` 中 `UIScreen.main` 告警属 P2-47）。

**未验证：** 真机；Stepper 与文字落在同一个 400 毫秒窗口内一起写盘（工具点击间隔超过 400 毫秒，代码上是同一个 `edited`）；400 毫秒内把 Stepper 拨过又拨回原值那条分支（仅代码级）；面板开着时由真实的「文件删失败」自然触发补偿（探针执行的是同一段恢复代码，不是自然故障）。

**修复 commit：** 51eb320e54e75d63ec3ba325da5050c38373e09b

<a id="p2-40"></a>

### P2-40 · 重排、插入、删除分镜时在主协程上逐条复制片段文件，素材多时界面卡住且短时占用双份磁盘

**验证状态：** 模拟器实测——按复现方法走完，「界面卡住」「双份磁盘」两个症状均未复现（见「验证结果」）；真机未验证

**代码位置：** ShotList/Models/ShotStore.swift · `syncClipFileNames()`（第 1022–1061 行）、`normalize()`（第 445–455 行）及其调用点（第 359、371、393、434、461、471、620 行）

**问题详情**

**预期行为：** 调整分镜顺序、在中间插入或删除一个镜头，是纯粹的列表操作，应当立即完成。

**实际行为：** 编号即位置，所以插入 / 删除 / 重排会让插入点之后**所有**镜头的编号 +1 或 −1，`normalize()` 随即调 `syncClipFileNames()`，对每一条受影响的片段执行 `fileManager.copyItem(at:to:)` 把整个视频复制成新文件名，旧文件要等 `commit()` 成功后才删。`ShotStore` 是 `@MainActor`，`syncClipFileNames()` 是同步调用，因此这些文件系统操作全部压在主线程上。

**根因证据：**

- `ShotStore.swift:1051` `try fileManager.copyItem(at: source, to: destination)`——是复制不是改名，选择复制是为了「JSON 提交失败时旧记录仍可读」（第 1015 行注释），代价就是这一步；
- 每条片段在复制前还要至少一次 `fileExists`（第 1040 行），重名时在 `while` 里继续探测（第 1043 行）；
- 调用链上没有任何一处把它挪出主协程：`insertShot` → `normalize()` → `syncClipFileNames()` 全在 `@MainActor` 上同步执行。

**影响范围：** 在一个 12 个镜头、每镜一条 200 MB 素材的影片里，在第 1 个镜头下方插入一个新镜头会触发 12 次整片复制，期间这些文件在磁盘上各存在两份。定级依据：这是主路径上的常规操作（右键「在下方插入新镜头」「上移一位」都会触发），但不丢数据、可恢复，且小影片上不明显，因此定为 P2。**未实测**：APFS 同卷复制在部分情况下会被系统优化成克隆，实际耗时与实际占用需要真机测量，本清单不给出未经测量的秒数与字节数。

**修正（修复时实测后）：** 上面「未实测」的那一点已经测了，结论与本条标题的假设相反——`FileManager.copyItem` 在 APFS 同卷上就是 COW 克隆，耗时与占用都不随文件大小增长。「一个 12 镜头、每镜 200 MB 的影片里，在第 1 个镜头下方插入」这个原文场景，整个 `insertShot` 在主线程上约 10 ms（热态），卷可用空间零变化。代码层面的事实（复制而非改名、同步跑在 `@MainActor`）仍然成立，但它们造成的用户可见后果在模拟器上没有出现。详见「验证结果」。

**复现方法**

1. 隔离测试数据：新建一部影片，加 12 个镜头，每个镜头用相册导入一段体积较大的视频（不要使用用户真实素材目录）。
2. 在 `ShotStore.syncClipFileNames()` 里临时加一条计数与耗时探针（记录复制次数与总耗时）。
3. 回到分镜页，对第 1 个镜头长按选「在下方插入新镜头」。
4. 观察：探针报出的复制次数应与受影响的片段条数一致；插入动作完成前界面无响应。同时在「文件」App 的「分镜视频」目录下观察提交前后的文件数量变化。
5. 预期正确结果：插入立即完成，磁盘上不出现同一条素材的两份副本。
6. 验证完成后移除探针与隔离数据。

**修复状态：** 无需修复（已关闭）

**修复说明：** 本轮没有改代码。原登记的三个可选方向都是为了去掉「物理复制」的代价，而这个代价在 APFS 上实测并不存在，动数据路径换不来可测量的收益：

- `moveItem` + 失败回滚：会拆掉「JSON 提交失败时旧记录仍可读」这层保险。`commit()` 失败时的回滚只是删掉已暂存的新文件，旧文件若已被移走就退不回去；进程在移动之后、`films.json` 写盘之前被杀的话，重启时记录仍指向旧文件名，`reconcileClipsWithDisk` 会把这些片段记录清掉、文件则变成孤儿（代码级推演，未实测）。
- 把重命名挪到后台：要引入「记录已改、文件还没改」的中间态，与全应用「磁盘口径」的统计（`existingClipFileNames`）冲突，复杂度远大于当前收益。
- 编号与文件名解耦：改动面最大，会牵动导出命名与「文件」App 里的可读性，同样没有可测量的收益作依据。

**关闭结论：** 按「无需修复」关闭，没有代码修复提交。重开条件：日后若在真机上测出 `copyItem` 退化成真实拷贝，再重开本条；届时首选保持现有的「先暂存新名字、提交后再删旧名字」结构，只把暂存这一步换成 `linkItem`（硬链接，同样零占用、保留失败回滚语义），而不是改成 `moveItem`。

**验证结果：** 未走「真机」；模拟器与宿主机上的实测如下，探针均为临时代码，已在提交前移除，隔离设备与测试数据已删除。

环境：iOS 26.5 / iPhone 17 Pro 模拟器（专门新建的隔离设备，容器在宿主 APFS 卷上），Debug 构建。数据：1 部影片、12 个镜头、每镜 1 条 200 MB 的**互不相同**的随机内容文件（逻辑总量 2.4 GB）。探针记录 `syncClipFileNames()` 的 `copyItem` 次数、耗时、所在线程与卷可用空间变化，并给 `insertShot` / `delete` 整个调用计时。操作是走 UI 真实触发的：长按第 1 个镜头 →「在下方插入新镜头」，以及长按第 1 个镜头 →「删除分镜」。

| 操作（12 镜头 × 200 MB） | `copyItem` 次数 / 逻辑体积 | 复制段耗时 | 整个调用耗时 | 卷可用空间变化 |
|---|---|---|---|---|
| 第 1 镜下方插入（首次，冷） | 11 次 / 2200 MB | 16.09 ms | 未计时（早一版探针只计复制段） | −1 MB（噪声，无增长） |
| 第 1 镜下方插入（重新灌数据后） | 11 次 / 2200 MB | 3.72 ms | `insertShot` 9.90 ms | −1 MB（噪声，无增长） |
| 删除第 1 镜（其后 11 条片段全部改名） | 11 次 / 2200 MB | 4.95 ms | `delete` 15.63 ms | 0 MB |

- 探针确认全部在主线程（`Thread.isMainThread == true`），复制次数与受影响的片段条数一致，与本条根因描述吻合；但热态下整个调用只有约 10–16 ms，量级是一两帧的时间，不是原条目设想的「界面无响应」。首次（冷态）只计了复制段（16 ms），整个调用没有计时，可能略高；屏幕上是否掉帧没有单独测量。
- 磁盘上没有出现两份副本：插入后按 `films.json` 逐条核对过，12 个文件与 12 条片段记录的文件名一一对应；删除后目录里剩 11 个文件，与预期的 11 条片段一致，`pending-deletions.json` 已被消费，没有残留。
- 独立对照（宿主 macOS APFS，5 个 200 MB 随机文件）：`copyItem` 合计 1.0 ms、`linkItem` 合计 1.1 ms，卷可用空间均零变化——`copyItem` 被系统做成了 COW 克隆，与 `linkItem` 同为元数据操作，不随文件大小计费。

**未验证：** 真机。模拟器不实现数据保护（`NSFileProtection`），克隆在真机上是否照常成立没有实测；代码层面能确认的只有：全仓没有设置任何文件保护类、没有数据保护相关配置，素材文件都落在系统默认保护类下，同类文件之间不该阻止克隆。另外，若素材目录落在不支持克隆的卷上（应用容器里不会出现）才会退化成真实拷贝，本次未构造。

**修复 commit：** 无——按无需修复关闭，没有代码修复提交。

<a id="p2-41"></a>

### P2-41 · 历史页进度环按磁盘口径、右侧三项统计按记录口径，同一屏两个数字会互相矛盾

**验证状态：** 模拟器实测（隔离模拟器 + 演示数据 + 目录权限故障注入；未在真机验证）

**代码位置：** ShotList/Views/HistoryView.swift · `progressCard`、`recordedShots` / `pendingShots`、`stats`；ShotList/Views/ExportView.swift · `recordedShots`、`exampleShot`；ShotList/Models/ShotStore.swift · `progress`、`availableShotCount`、`applySnapshot`、新增的 `isRecorded(_:)`（行号为修复前，已省略；修复后历史页与导出页直接读 `store.recordedShots` / `store.pendingShots`）

**问题详情**

**预期行为：** 同一屏上的「完成百分比」与「已拍 / 未拍 / 全部」三个数字出自同一个口径，不会互相矛盾。

**实际行为：** `ProgressRing(progress: store.progress)` 的分子是 `availableShotCount`——`applySnapshot` 按**磁盘上真的有文件**算出来的；而右侧 `statColumn` 用的是 `store.shots.filter(\.hasClip).count`——JSON 记录口径。两者在「记录还在、文件已经不在磁盘上」时会给出不同的数字，例如进度环显示 40%（2/5），右边「已拍」却写 3。

**根因证据：**

- `HistoryView.swift:71` `store.shots.filter(\.hasClip)`，`Shot.hasClip` 只看 `clips.isEmpty`（Shot.swift:181），不问磁盘；
- `ShotStore.swift:200-202` `progress` 用 `recordedCount`，即 `availableShotCount`；
- `ShotStore.swift:143-151` 的注释已经写明设立磁盘口径就是为了避免这类「界面显示已拍、实际给不出文件」，历史页这一半没有跟上。

**影响范围：** `reconcileClipsWithDisk` 会在每次切回前台时把失效记录清掉，所以两个数字对不上的窗口很窄——需要素材在应用位于前台期间失效、或目录枚举失败（`snapshot.isComplete == false`，此时 `applySnapshot` 保留旧统计而记录不变）。定级依据：显示不一致、不影响数据，且窗口窄，定为 P2。同一口径问题也出现在导出页的「单独分享某个镜头」一节（ExportView.swift:25 `recordedShots` 同样用 `hasClip`），属同一根因，一并在此登记，不另开编号。

**复现方法**

1. 隔离测试数据：准备一部含 5 个镜头、其中 3 个已拍的测试影片（使用隔离的 Documents 目录，不要动用户真实素材）。
2. 代码级验证：在 `HistoryView.recordedShots` 与 `ShotStore.progress` 各加一条探针，打印两个口径的计数。
3. 让 `diskSnapshot()` 返回 `.unreadable`（临时把 `clipsDirectory` 指向一个不可读路径），使 `applySnapshot` 保留旧统计、`reconcileClipsWithDisk` 不动记录。
4. 进入历史页，观察进度环百分比与右侧「已拍」数字。
5. 预期正确结果：两处读同一个口径，数字一致；实际会出现进度环与「已拍」不一致。
6. 验证完成后移除探针与隔离数据。

**修复状态：** 已修复

**修复说明：** 根因是「已拍」有两套判据。进度环、影片条的「N/M 已拍」、导出页概览读的是 `applySnapshot` 按磁盘枚举结果算出的缓存；历史页右侧三项统计与筛选列表、导出页「单独分享某个镜头」读的是 `Shot.hasClip`，只看 JSON 记录，不问磁盘。

修复在 `ShotStore` 里只留一个判据 `isRecorded(_:)`：至少有一条片段的文件名在最近一次成功枚举的磁盘文件名集合里（与导出包筛镜头同一条规则），并据此给出 `recordedShots` / `pendingShots`。`applySnapshot` 里每部影片的「已拍镜头数」也改调同一个函数，不再单独维护一份；历史页统计与筛选、导出页「单独分享」与示例文件名取镜头（`exampleShot`）都改读它。已拍 + 未拍恒等于全部。`Shot.hasClip` 保留——删除确认文案、镜头面板里确实要按记录判断的地方仍用它——注释里注明它只看记录。

**未改：** 分镜页时间线节点（`ShotListView.swift` 的 `NumberBadge(isRecorded: shot.hasClip)`）仍按记录判断。故障注入下它显示「已拍」而缩略图为空（模拟器实测，基线截图），但该页没有计数与之并列，不构成本条的「同屏两个数字」，不在此改。

**验证结果：** iOS 26.5 / iPhone 17 模拟器（为本次验证新建的独立设备，不影响其他会话的模拟器）；演示数据由 `Tools/seed-simulator.py` 灌入（「夏日vlog」5 个镜头、3 个已拍）；页面用 `-preselectTab` 直达；截图取自 `xcrun simctl io screenshot`。**当前环境没有点击 / 滑动自动化，所以没有点按筛选条与卡片。**

故障注入不需要改代码：把容器里的 `Documents/分镜视频` 目录 `chmod 000`，`contentsOfDirectory` 抛错，`diskSnapshot()` 返回 `.unreadable`，`applySnapshot` 保留旧统计、`reconcileClipsWithDisk` 不动记录，正是复现步骤 3 的状态。

- **修复前（基线构建，同一份数据）：** 历史页进度环 **0% 已完成**、影片条 **0/5 已拍**，右侧却是 **已拍 3 / 未拍 2 / 全部 5**——同屏三处数字互相矛盾，与问题描述一致。
- **修复后（同一份数据，目录仍不可读）：** 历史页进度环 0%、影片条 0/5，右侧 **已拍 0 / 未拍 5 / 全部 5**，三处一致；导出页概览「已拍 0 / 5 个镜头」0%，「单独分享某个镜头」一节显示「还没有拍好的镜头。」（该节在页面下部、无法滑动到达，验收时临时把它提到列表最前截图，探针已在提交前移除）。
- **正常路径（恢复目录权限）：** 历史页 60%、影片条 3/5、已拍 3 / 未拍 2 / 全部 5；导出页概览「已拍 3 / 5 个镜头」60%，「单独分享」列出镜头 1、2、3，各带分享按钮。
- **磁盘缺一个文件（枚举成功，走 `reconcileClipsWithDisk` 校正）：** 删掉镜头 3 的片段文件后重启，历史页 40%、影片条 2/5、已拍 2 / 未拍 3 / 全部 5，镜头 3 卡片变为未拍占位，三处一致。
- **构建：** Debug 构建 `BUILD SUCCEEDED`，输出里没有来自 `ShotStore` / `HistoryView` / `ExportView` / `Shot.swift` 的告警。

**未验证：** 真机；点按「已拍 / 未拍」筛选后的列表（无点击自动化，仅代码级：列表与三项统计读同一个 `store.recordedShots` / `store.pendingShots`）；写盘失败回滚（`commit()` 的 catch 分支）自然触发的那条不一致路径（仅代码级）。**代码级推断、未实测：** 目录持续不可读时切换影片，进度环分子 `availableShotCount` 仍是上一次成功枚举时「上一部影片」的缓存，而列表按当前影片的记录派生，两者仍可能不一致；这是 `applySnapshot`「枚举失败时保留旧值」的既有取舍，iOS 上 `Documents` 目录持续不可读的情形罕见，本次不改。

**修复 commit：** d61bd44adc2d7db75dd2e6b0ac9df385ec6cb01c

<a id="p2-42"></a>

### P2-42 · 拍摄设置里分辨率不可用时写成「该分辨率下不支持」，说的不是这一行的对象

**验证状态：** 模拟器实测（借助已有的 Debug 验收入口 `-chromeDemo 4`；未在真机切前置摄像头验证）

**代码位置：** ShotList/Views/CameraSettingsSheet.swift · `resolutionUnavailableNote` / `frameRateUnavailableNote`（修复后，第 135–138 行）、分辨率分节（第 35–50 行）、帧率分节（第 52–67 行）

**问题详情**

**预期行为：** 某档分辨率在当前摄像头上不可用时，这一行右侧应写「这台摄像头不支持」；某档帧率在当前分辨率下不可用时，才写「该分辨率下不支持」。

**实际行为：** 两个分节共用同一个 `unavailableNote`，它只按 `availableFrameRates.isEmpty` 二选一。`availableFrameRates` 由调用方按当前选中分辨率算出（CameraCaptureView.swift:243），正常情况下非空，于是**分辨率**行的不可用说明也会写成「该分辨率下不支持」——一行分辨率选项右侧写着「该分辨率下不支持」，指代不明。

**根因证据：** `CameraSettingsSheet.swift:39` 与 `:56` 都取同一个 `unavailableNote`，而它的判据（`availableFrameRates` 是否为空）与「这一行是分辨率还是帧率」无关。

**影响范围：** 切到前置摄像头时 4K 通常不可用，这一行就会出现这句话。只影响文案可读性，不影响拍摄，定为 P2。

**复现方法**

1. 真机上进入任意镜头的取景页，切到前置摄像头（前置多数不支持 4K）。
2. 点右上角「拍摄设置」。
3. 观察「分辨率」一节里 4K 那一行右侧的说明文字。
4. 实际显示「该分辨率下不支持」；预期正确结果是「这台摄像头不支持」。

**修复状态：** 已修复

**修复说明：** 根因是两个分节共用同一个 `unavailableNote`，其判据（`availableFrameRates.isEmpty`）与「这一行是分辨率还是帧率」无关：`availableFrameRates` 由调用方按当前选中分辨率算出，正常情况下非空，导致分辨率行的不可用说明总是落到「该分辨率下不支持」分支。修复：拆成两个专用文案——`resolutionUnavailableNote`（「这台摄像头不支持」，分辨率行用）与 `frameRateUnavailableNote`（「该分辨率下不支持」，帧率行用），两处 `trailing:` 参数各自取用对应文案，删除原先共用且判据错位的 `unavailableNote`。`row(...)` 的无障碍标签 / 值直接读同一个 `trailing` 参数，随之自动更正，无需单独改动。

**验证结果：** iOS 26.5 / iPhone 17 Pro 模拟器，Debug 构建。`CameraSettingsSheet` 设计为纯值视图（可不可用由调用方算好传入），借助已有的 Debug 验收入口 `CameraChromeDemoScreen`（`-chromeDemo 4` 本就会弹出拍摄设置面板）临时把该入口里的 `availableResolutions` 改为 `[.hd1080, .hd720]`（去掉 `.uhd4K`，`availableFrameRates` 保持 `[.fps24, .fps30]` 非空，构造「分辨率不可用但帧率列表非空」的条件）；`xcrun simctl launch <UDID> com.max.ShotList -chromeDemo 4` 启动后截图：「分辨率」一节「4K」行显示「这台摄像头不支持」，「帧率」一节「60 fps」行显示「该分辨率下不支持」，两行文案分别对应各自的对象，不再指代不清。验证完成后已用 `git checkout` 把该临时改动从 `CameraChromePreviewScreen.swift` 还原，最终提交只包含 `CameraSettingsSheet.swift` 的修复。构建命令 `xcodebuild -project ShotList.xcodeproj -scheme ShotList -destination 'generic/platform=iOS Simulator' -configuration Debug build` 执行 `BUILD SUCCEEDED`，输出中唯一的 `warning:` 是既有的、与本次改动无关的 `appintentsmetadataprocessor` 提示，没有新增编译告警。**未验证：** 真机切前置摄像头的真实路径（环境无真机、模拟器无摄像头，本次通过纯值视图直接构造等价数据代替）；无障碍朗读走系统检查器逐条听读确认（本次工具环境的 `inspect` 不可用，改为代码级核对 `accessibilityLabel` 与界面文案同源于同一个 `trailing` 字符串）。

**修复 commit：** d193179de895efc8fe687d34ff6c1e9d5d6a2e18

<a id="p2-43"></a>

### P2-43 · 保存拍摄与相册导入都会对已经属于本应用的临时文件再整份复制一次

**验证状态：** 隔离测试实测（iPhone 17 Pro · iOS 26.5 模拟器，应用内探针走真实 `addClip`；相册选择器界面与相机录制未走，真机沙盒下的 `linkItem` 未测）

**代码位置：** ShotList/Models/ShotStore.swift · `addClip(from:duration:to:)`（第 482–511 行，第 486 行的 `temporaryCopy`）；ShotList/Support/MediaImport.swift · `ImportedMovie.transferRepresentation`（第 27–34 行）、`MediaFileCopy.temporaryCopy`（第 96–110 行）；ShotList/Views/CameraCaptureView.swift · `save(_:continuing:)`（第 534–568 行）；修复后 `addClip` 拆出 `clipTarget(for:sourceURL:)`，行号以现状为准

**问题详情**

**预期行为：** 一段素材从来源落到「分镜视频」目录，整条链路上只做必要的一次搬运。

**实际行为：** `addClip` 的第一步就是 `mediaFileCopy.temporaryCopy(of: sourceURL)`，把源文件再复制一份到临时目录，然后 `moveItem` 到目的地，最后 `try? removeItem(at: sourceURL)` 删掉源。但它的**两个调用方给进来的 `sourceURL` 本来就是本应用自己的临时文件**：

- 相册导入：`ImportedMovie` 的 `importing:` 已经调过一次 `MediaFileCopy.shared.temporaryCopy(of: received.file)`，交给 `addClip` 的已经是 `import-<uuid>.<ext>`；于是同一段素材在临时目录里被复制两次；
- 相机拍摄：`CameraRecorder` 直接把视频录到 `tmp/shot-<uuid>.mov`，`save(_:)` 把它交给 `addClip`，又复制一次。

**根因证据：** `ShotStore.swift:485-486` 的注释写的是「大文件复制显式离开主协程，暂存文件不进入素材枚举和孤儿清理范围」——这两个目的在源文件已经是应用私有临时文件时都已经满足（它本来就不在 `clipsDirectory` 里），这一步是冗余的。

**影响范围与维护成本：** 多出一份临时文件、一次可能失败的 I/O（`addClip` 因此多一条失败路径与一条取消路径）。定为 P2。

**已实测：`copyItem` 确实被优化成 COW 克隆，这一步不按文件大小收费。** 用 iPhoneSimulator SDK 编译探针、`simctl spawn` 在模拟器里以 iOS Foundation 运行，在真实 app 容器内对一个 600 MB 文件从 `tmp/` 复制到 `Documents/分镜视频/`：`FileManager.copyItem` 耗时 0.000 秒，`volumeAvailableCapacityForImportantUsage` 零变化（`linkItem` 同样结果）。同时确认容器的 `tmp` 与 `Documents` 同卷（`stat -f %d` 相同），这是克隆能成立的前提。

据此修正原先的影响判断：**「设备空间紧张时会让本可成功的保存失败」不成立**——克隆不预先占用空间。剩下的仍然是可核实的成本：多一份临时文件条目、多一条可失败的 I/O 与一条取消路径。**未证实**：真机的数据保护（`NSFileProtection`）分级是否会让跨保护类的 `copyItem` 退化成真实拷贝，模拟器不实现数据保护，测不出来。

**关于建议修复方案的一处约束（新增）：** 直接把源文件 `moveItem` 到目的地会**破坏**「提交 JSON 前源视频可重试」这条约束——`commit()` 失败时的回滚是删掉已暂存的目标文件（`ShotStore.swift` 的 `stagedFileNames` 分支），源文件此时已经被移走，就再也退不回去了。中转的那一份克隆正是这条约束的实现方式。要去掉中转，必须同时把回滚从「删除目标」改成「把目标移回源位置」，这会动到崩溃安全那套机制。修复时二选一：要么按上面的方式改回滚，要么保留克隆（实测代价近似为零）只做清理性重构。

**复现方法**

1. 隔离测试数据：准备一段体积较大的测试视频，不要使用用户真实素材。
2. 在 `MediaFileCopy.temporaryCopy` 里临时加一条探针，打印每次调用的源路径与目的路径。
3. 走一次「从相册导入」，观察探针：同一段素材会打印两次，第二次的源路径是第一次的目的路径（`tmp/import-*`）。
4. 真机上走一次「用相机拍摄 → 使用这条」，观察探针：源路径是 `tmp/shot-*`，仍然被复制了一次。
5. 预期正确结果：源文件已是应用私有临时文件时，直接 `moveItem`，探针只应出现在相册导入的第一跳。
6. 验证完成后移除探针与隔离数据。

**修复状态：** 已修复

**修复说明：** 根因：`addClip` 无条件把来源复制成 `import-*` 再 `moveItem` 进「分镜视频」；这一跳是为「真复制」设计的（离开主协程、暂存文件不进素材枚举），但两个调用方给进来的本来就是本应用临时目录里的完整文件，属冗余。最小修复：`addClip` 先用 `FileManager.linkItem` 把来源硬链接到目标名——O(1)、不复制、不等待，来源名原样保留；`commit()` 失败时回滚只删目标名，「提交 JSON 前源视频可重试」不受影响；提交成功后再删来源名。链接不成（跨卷等）才退回原有的「`temporaryCopy` → 重新解析目标 → `moveItem`」路径。取舍：不用 `moveItem`，是为了不动 `commit()` 的回滚（崩溃安全机制）；不用同目录 `copyItem`，是因为它在无法克隆时会退化成整份复制，放在主协程上会卡界面，而硬链接要么立刻完成、要么立刻失败。入口补了一句 `Task.checkCancellation()`，保持「已取消的任务不落片」（原先由 `temporaryCopy` 首行保证）。两个调用点（`ImportedMovie.save`、`CameraCaptureView.save`）无需改动；`syncClipFileNames` 的重命名暂存同样用 `copyItem`，不在本条范围。测试辅助 `BlockingCopyFileManager` 让 `linkItem` 抛错，使三条复制路径用例继续覆盖退路。

**验证结果：** iOS 26.5 / iPhone 17 Pro 模拟器（单独新建的 P2-43-verify，验证后删除），Debug 构建。因模拟器控制未获授权，改用临时应用内探针（真实 app 容器的 `tmp/` 与 `Documents/`）复用真实 `MediaFileCopy`、`ImportedMovie.save`、`ShotStore.addClip`，依次走：A 相册导入链（`importing:` 那一跳 + `save` → `addClip`）、B 相机链（`tmp/shot-*` → `addClip`）、C 提交 JSON 失败后重试、D 600 MB 素材。修复前 `MediaFileCopy.temporaryCopy` 的调用次数：A 两次（第二次源路径正是第一次的目的路径 `import-*`）、B 一次（`shot-*` → `import-*`）、C 每次尝试各一次、D 一次。修复后：A 仅第一跳一次，B / C / D 均为 0 次；C 首次失败时源文件仍在、内容逐字节一致、片段目录未多文件，修好元数据后重试成功且源文件被删除；A / B 之后临时目录无 `import-*` 残留；D 的 600 MB `addClip` 耗时 0.060 s → 0.002 s，可用空间变化 −32768 B → 0 B。移除探针后重新构建，并跑现有 `ShotStoreTests` 28 项全部通过（复制退路由 `BlockingCopyFileManager` 强制走到）。**未验证：** 相册选择器界面与相机录制没有走（模拟器控制未获授权、模拟器无摄像头），以上是同一条 `addClip` 代码路径而不是界面操作；真机 iOS 沙盒下 `linkItem` 是否可用、数据保护分级的影响未测——链接失败时会自动退回原有复制路径，行为与修复前一致。

**修复 commit：** b6024a1404fc800450643318a80ec7980ac94647

<a id="p2-44"></a>

### P2-44 · 历史页影片下拉面板不滚动，影片较多时底部的影片与「重制」落到屏幕外点不到

**验证状态：** 模拟器实测（iPhone 17 Pro · iOS 26.5，25 部影片：面板越过标签栏、超出屏幕下沿，第 14 部之后的影片与「重制」不可见；阈值随机型与动态字号变化，本清单不给出精确数字）

**代码位置：** ShotList/Views/FilmBar.swift · `dropdownPanel`（第 105–132 行）、悬浮锚定（第 43–49 行）；ShotList/Views/HistoryView.swift · `FilmBar(allowsRename: false)` 所在的 `ScrollView`（第 46–57 行）（行号为修复前；修复后对应 `dropdownPanel` / `filmList` / `filmListHeight` / `outsideTapCatcher`）

**问题详情**

**预期行为：** 影片库里有多少部，下拉面板就应该都能选到；面板末尾的「重制」也始终可点。

**实际行为：** `dropdownPanel` 是一个没有滚动容器的 `VStack`，`ForEach(store.sortedFilms)` 有多少部就排多少行（每行 `minHeight: SLSize.minTouchTarget` = 44pt），末尾再加分隔线与「重制」。它挂在白条的 `.overlay` 上——overlay **不参与父视图的布局尺寸**，所以外层 `ScrollView` 的内容高度不会因为面板展开而增加。面板一旦比「白条下沿到屏幕底」这段距离更高，超出的行既画不出来，也没法靠滚动够到。

**根因证据：**

- `FilmBar.swift:43-49` 用 `.overlay(alignment: .topLeading)` + `padding(.top, cardHeight + tiny)` 定位，`.overlay` 不撑开父视图；
- `FilmBar.swift:106-123` 面板内没有 `ScrollView`，也没有任何高度上限；
- `HistoryView.swift:46-57` 外层是 `ScrollView`，会把超出自身 bounds 的内容裁掉。

**影响范围：** 按每行 44pt、面板上方约占 70pt 估算，常见 iPhone 竖屏大约 16 行以后开始溢出；具体阈值随机型与动态字号变化，**未实测**，本清单不给出精确数字。影响的是「切换影片」这条找回路径与「重制」入口；分镜页的三点菜单提供了同样的两个动作（`FilmSwitcherMenu`，系统 `Menu` 自带滚动），有合理绕行方式，因此定为 P2 而不是 P1。

**复现方法**

1. 隔离测试数据：用 `Tools/seed-simulator.py` 或手动「重制」造出 20 部以上影片（每部起一个可区分的标题，避免空壳被 `discardBlankFilm` 回收）。
2. 打开历史页，点影片条右侧的下拉箭头展开面板。
3. 尝试向下滚动面板，并尝试点击列表最后几部影片与末尾的「重制」。
4. 观察：面板底部被屏幕下沿截断，页面滚动也不会把它们带上来；预期正确结果是面板内可滚动，或面板有高度上限并在内部滚动。
5. 验证完成后删除隔离影片。

**修复状态：** 已修复

**修复说明：** 根因是面板挂在 `.overlay` 里，不参与外层 `ScrollView` 的内容高度，而面板自身既没有高度上限也没有滚动容器，行数一多就整体越过滚动区下沿。修复把影片清单放进面板内部的 `ScrollView`，高度取「内容高度」与「面板上限」中较小的一个（做法同 `CameraChrome.noteBody`：`ScrollView` 会占满给它的高度，所以先量内容再定高，影片少时面板不会被撑空）；「修改标题」/「重制」与分隔线固定在清单下面，不随清单滚走，任何数量下都可点。面板上限 = 白条下沿到滚动区可视区下沿的距离，减去与白条的间隙和 `SLSpacing.medium` 的底部留白，再扣掉面板内边距与固定区高度；这段距离由 `outsideTapCatcher` 用 `proxy.bounds(of: .scrollView)` 顺带量出（该层只在展开时存在，收起时滚动不产生额外布局），量不到时（不在滚动视图里）不设上限；空间小到极端时清单至少保留一行（44pt）。只改 `FilmBar.swift`，`FilmSwitcherMenu`（分镜页三点菜单，系统 `Menu` 自带滚动）不受影响。

**验证结果：** Xcode 27.0，专用模拟器 `iPhone 17 Pro`（iOS 26.5，竖屏、浅色），用临时脚本往该模拟器容器的 `films.json` 写入 25 部影片（每部一个可区分的标题「测试影片01」…「测试影片25」，各含一个镜头、无素材），触摸由模拟器输入注入；测试数据与脚本不入库，模拟器验证后已删除。构建 `xcodebuild -project ShotList.xcodeproj -scheme ShotList -destination 'id=<UDID>' build`：BUILD SUCCEEDED，未出现 `FilmBar.swift` 相关告警。

- **修复前（复现）：** 25 部影片，历史页展开下拉：面板一路铺到标签栏下面并超出屏幕下沿，只能看到前 13 部与半行第 14 部，之后的影片与「重制」不可见。
- **修复后（同一路径）：** 面板下沿止于标签栏之上，清单末行被裁半行（提示可滚动），「重制」固定在面板底部可见；在面板内上滑 3 次滚到底，第 25 部完整可见、页面本身不被带动；点第 25 部：切换成功，白条标题变为「测试影片25」、面板收起；重新展开后点「重制」：弹出「重制当前影片？」确认（已点面板外取消，未真正重制）。
- **回归：** 3 部影片：面板高度贴合内容，无多余空白；点面板外空白处收起，与修复前一致。辅助字号「特大」（accessibility-extra-large）下：面板下沿仍止于标签栏之上，「重制」可见（仅看截图，未在此字号下做滚动交互）。面板展开时上滑页面（第一部影片带 20 个镜头使页面可滚）：面板顶部随白条上移，下沿仍守在标签栏之上、「重制」可见，无跳动或错位。

**未验证：** 真机；VoiceOver；辅助字号下的面板内滚动交互；横屏（应用仅支持竖屏，未测）。

**修复 commit：** d09cd931a6071959ad4b0ad1cd61318ca76de23d

<a id="p2-45"></a>

### P2-45 · 任何一次分镜编辑都会立刻删掉已生成的导出包，分享还没完成时会被中断

**验证状态：** 模拟器实测（iPhone 17 Pro · iOS 26.5；真实导出包生成与失效路径已验收；系统分享传输完成未在真机验证）

**代码位置：** ShotList/Views/ExportView.swift · `.onChange` 一组失效触发、`resultSection` 的 `ShareLink`；ShotList/Support/ExportSession.swift · `invalidate()`、`build()` 的旧包回收

**问题详情**

**预期行为：** 已经生成的导出包在用户把它交给别的应用之前应当一直可用；输入变化时把它标成「已过期」即可，不必立刻从磁盘上删掉。

**实际行为：** 修复前 `invalidate()` 除了置 `isStale`，还会 `discard(package)`，删除 zip 及其 `ShotListExport/<uuid>/` 目录。触发条件包括 `store.shots` 的任何变化（改一个字的描述也算）、切影片、改风格描述、改范围、改格式；分享面板收起后仍可能在后台读取这个文件。

**根因证据：** `ExportSession.invalidate()` 把「结果过期」和「文件回收」放在同一条路径，调用 `discard(package)` 后立即清空 `package`；`discard(_:)` 不判断文件是否仍被系统分享引用。

**影响范围与定级依据：** 包文件会在编辑后立即从磁盘消失，界面上的分享入口也随之消失；系统在传输进行中被删源文件后的具体表现未在真机实测，因此不声称一定传输失败。界面有重新生成的绕行方式，定为 P2。

**复现方法**

1. 在导出页生成一个较大的导出包（几百 MB 级，使用隔离测试影片）。
2. 点「分享 / 存储到「文件」」，选隔空投送到一台 Mac，在传输进度条还没走完时收起分享面板。
3. 切到「分镜」标签页，在任意镜头的描述里加一个字，等 400 毫秒落盘。
4. 观察 Mac 上的接收结果，以及导出页「刚刚生成的导出包」一节是否消失。
5. 预期正确结果：已经交出去的包在本次传输结束前不被删除；界面可以标成「已过期」，但不应让进行中的分享失败。

**修复状态：** 已修复

**修复说明：** `invalidate()` 现在只取消在途构建、更新失效代次、置 `isStale` 并保留已生成的 `package` 与 zip；旧包在下一次成功生成新包时由既有 `discard(package)` 回收，应用启动时仍由 `ExportPackageBuilder.cleanUp()` 清理临时目录。迟到的旧构建结果仍会被丢弃，不会重新成为可分享结果。

**验证结果：** iOS 26.5 / iPhone 17 Pro 模拟器，使用 `Tools/seed-simulator.py` 灌入隔离演示素材；Debug 版本通过 `build_run_sim` 构建、安装并启动。临时 Debug 探针复用真实 `ExportSession.build()` 生成 `分镜导出_夏日vlog_20260919_003242.zip`，再调用真实 `invalidate()`：导出页同时保留「刚刚生成的导出包」、`ShareLink` 的「分享 / 存储到「文件」」入口，并显示「分镜、素材或导出范围已变化，请重新生成导出包。」；模拟器容器内 zip 仍存在，大小 535485 bytes。移除探针后再次构建运行通过，构建诊断无警告。**未验证：** 真机执行中的隔空投送/「存储到文件」传输完成时序。

**修复 commit：** 2cc14ebc0e723c82dc9cf9d37aa6164cebf65abe

<a id="p2-46"></a>

### P2-46 · 删除补偿日志清理失败时整个应用停在「无法读取分镜记录」，标题与实际原因不符

**验证状态：** 模拟器实测（iPhone 17 / iOS 26.5 模拟器，隔离测试数据；故障注入用临时探针与文件系统层故障，探针已在提交前移除）

**代码位置：** ShotList/Models/ShotStore.swift · `commit()`、`writeLibrary()`、`loadStoredLibrary()`；ShotList/Views/RootTabView.swift · `loadError` 分支（原第 861–866、982–985、35–43 行为修复前行号；修复后对应 `LoadFailure`、`loadStoredLibrary()`、`loadLegacyLibrary()`、`startEmptyLibrary()`、`commit()` 与 `LoadFailure.Kind.title` / `retryTitle`）

**问题详情**

**预期行为：** 错误页的标题应当说明真正发生了什么，并给出用户能执行的下一步。

**实际行为：** `commit()` 末尾调 `recoverPendingDeletions()`，它失败时把 `loadError` 置成「删除尚未完成，已保留恢复记录和剩余素材。请检查存储后重试。」；而 `RootTabView` 只要看到 `loadError != nil` 就整页换成 `ContentUnavailableView`，标题固定写「**无法读取分镜记录**」。于是用户看到的是一个说「读不出记录」的标题，配一段说「删除没完成」的正文，两者说的不是一回事——而记录其实读得好好的。

**根因证据：**

- `ShotStore.swift:864-866` 用 `loadError` 这一个通道表达了两类完全不同的状态（读不出来 / 删除收尾没完成）；
- `RootTabView.swift:36-38` 的标题写死为「无法读取分镜记录」，不区分 `loadError` 的来源；
- 副作用：`writeLibrary()`（第 983 行）在 `loadError != nil` 时直接抛错，因此这一状态下所有编辑都会失败，而错误页的「重新读取」按钮调的是 `retryLoad()` → `load()`，恰好会重走一次 `recoverPendingDeletions()`，有可能自愈——但标题没告诉用户这一点。

**影响范围：** 需要「文件删不掉」这类存储异常才会触发；触发后应用整体不可编辑，但数据完好、重试可恢复，定为 P2。

**复现方法**

1. 隔离测试数据：使用专门的模拟器与其应用容器，不要对用户真实数据操作。
2. 故障注入（修复时补正，见下）：让 `recoverPendingDeletions()` 末尾的 `removeItem` 抛错。
3. 在应用里做任意一次会落盘的编辑（分镜页右上角「＋」加一个镜头即可）。
4. 观察整页错误界面：标题为「无法读取分镜记录」，正文为「删除尚未完成……」。
5. 预期正确结果：标题与正文说的是同一件事，并明确「记录没有丢、重试即可」。
6. 验证完成后移除注入与隔离数据。

**复现条件补正（修复时核对）**

- 原步骤 2「把 `deletionRecoveryURL` 替换成不可删的目录」到不了目标路径：`commit()` 的第一步是 `write(to: deletionRecoveryURL, options: .atomic)`，目标是目录时原子写入直接失败（实测 `NSCocoaErrorDomain 4`，目录原样保留），走的是 `commit()` 的回滚分支，界面只出「未能保存本次更改」的内联提示，根本到不了 `recoverPendingDeletions()`。
- 实际可用的注入有两种：① 临时探针——在 `recoverPendingDeletions()` 末尾 `removeItem` 之前，容器里出现 `probe-fail-recovery` 文件就抛错，然后在应用里点「＋」加镜头，走的正是 `commit()` 末尾这条路径；② 文件系统层、无需改代码——把一份有效的 `pending-deletions.json`（复制 `films.json` 即可）放进容器并 `chflags uchg`，冷启动时恢复清理在最后的 `removeItem` 处失败。
- 补充一个原条目没有列出的入口（同一根因）：`loadStoredLibrary()` 用同一个 `catch` 兜住「解码 `films.json`」与「`recoverPendingDeletions()`」，清理失败也被记成「无法读取分镜记录」，还把读好的影片从内存里清空。因此冷启动遇到残留日志清理失败、以及在错误页点「重新读取」重试时，页面都会写成「无法读取分镜记录……请恢复可用的分镜记录后重试」——记录其实读得好好的，「恢复可用的分镜记录」这一步用户既做不到也不需要。

**根因（修复时定位）**

`ShotStore.loadError` 是一个 `String?`，同时承担「读不出记录」「旧版升级失败」「删除收尾没做完」三种状态，界面只能对所有状态写死同一个标题和同一个「重新读取」按钮；`loadStoredLibrary()` 又把「读取失败」与「清理失败」并进一个 `catch`，同一种失败在 `commit()`、冷启动、重试三条路径上被贴了不同的标签。

**修复状态：** 已修复

**修复说明：** `loadError` 改为 `LoadFailure?`（类别 `Kind` + 正文 `message`）：`unreadable`（`films.json` 读不出，或素材还在却一份记录都没有）、`upgradeFailed`（旧版 `shots.json` 升级失败）、`deletionPending`（记录读得好好的，只是删除补偿日志没清理完）。所有 `loadError == nil` 的门闩不动。`RootTabView` 的标题与重试按钮按类别给：「无法读取分镜记录 / 重新读取」「无法升级旧版分镜记录 / 重试升级」「删除尚未完成 / 重试清理」；`deletionPending` 的正文明确「分镜记录没有丢失，只是删除后的收尾清理没有完成」，并保留系统错误描述。`loadStoredLibrary()` 拆成「解码 `films.json`」与「恢复清理」两段：只有前者失败才算 `unreadable`（沿用原文案并清空内存）；后者失败记 `deletionPending` 且不再清空内存，与 `commit()` 里同一种失败一致——「重试清理」调用的 `retryLoad()` 会重读 `films.json` 并重走一次清理，所以这一态可以自愈。不动：`writeLibrary()` 在 `loadError != nil` 时拒写的保护（防止覆盖尚未恢复的关联）、`unreadable` 与 `upgradeFailed` 的正文。

**验证结果：** Xcode 27.0，专用的 `iPhone 17` 模拟器（iOS 26.5，浅色、竖屏），演示数据来自 `Tools/seed-simulator.py`，触摸由模拟器输入注入；修复前后同一条路径各走一遍。构建：`xcodebuild -project ShotList.xcodeproj -scheme ShotList -destination 'id=<UDID>' -configuration Debug clean build`，BUILD SUCCEEDED，源码告警 0 条（仅有与代码无关的 `appintentsmetadataprocessor` 提示）。

*修复前（复现，探针版构建）：*

- 原路径：容器里放 `probe-fail-recovery`，点「＋」加镜头 → 整页错误，标题「无法读取分镜记录」，正文「删除尚未完成，已保留恢复记录和剩余素材。请检查存储后重试。」+ 探针错误，按钮「重新读取」——标题与正文对不上；此时 `films.json` 已含新镜头（6 个），日志文件仍在。
- 重试入口：故障未撤时点「重新读取」，正文翻成「无法读取分镜记录，已暂停编辑和文件清理……请恢复可用的分镜记录后重试」。
- 冷启动入口（文件系统层）：`pending-deletions.json` 设 `chflags uchg` 后启动 → 标题与正文都写「无法读取分镜记录……请恢复可用的分镜记录后重试」，附「未能移除 pending-deletions.json，因为你没有访问许可」。

*修复后（同一路径）：*

- 原路径：点「＋」→ 标题「删除尚未完成」，正文「分镜记录没有丢失，只是删除后的收尾清理没有完成……」+ 探针错误，按钮「重试清理」。
- 故障未撤时点「重试清理」：仍是同一类页面，不再翻成「无法读取分镜记录」；撤掉故障后再点：回到分镜页，镜头 7 个（触发这次失败的那次编辑也保住了），`pending-deletions.json` 已被消费。
- 冷启动入口（探针版构建一次，去掉探针的最终构建再用 `uchg` 一次）：残留日志 + 探针 / `uchg` → 「删除尚未完成」；撤掉故障后点「重试清理」→ 分镜页，日志被清掉。
- 回归：把 `films.json` 写成非法内容 → 标题「无法读取分镜记录」、按钮「重新读取」，还原文件后点重试 → 分镜页；写旧版 `shots.json` 并把 `ShotList` 元数据目录设为只读 → 「无法升级旧版分镜记录」/「重试升级」，恢复写权限后点重试 → 升级成功（`films.json` 与 `shots.json.migrated` 都在）。

**未验证：** 真机；「文件删不掉」的自然故障（探针与 `uchg` 执行的是同一段恢复代码，不是自然触发）；特大辅助字号与 VoiceOver 下的错误页；`unreadable` 里「素材还在却一份记录都没有」入口（`startEmptyLibrary()`，仅代码级：与其余 `unreadable` 共用同一类别与文案）；按 AGENTS 约定未构建 / 运行 Unit Test。

**发现的独立问题：** 补偿日志本身损坏（非法 JSON）时，「重试清理」永远失败、没有出口，见 [P2-48](#p2-48)（已修复）；该问题在 P2-46 修复前就存在，不属于同一根因。

**修复 commit：** 8550a74b7ce7677c5e38000f1cbd6230a499cc29

<a id="p2-47"></a>

### P2-47 · 影片条的点击收起层用已在 iOS 26 废弃的 UIScreen.main 取屏幕尺寸，是全项目仅有的编译告警

**验证状态：** 模拟器实测（修复前后各一次干净构建对比告警；交互在 iPhone 17 Pro / iOS 26.5 模拟器上验收）

**代码位置：** ShotList/Views/FilmBar.swift · `outsideTapCatcher`（第 205–217 行）、`outsideTapBleed`（第 219 行）

**问题详情**

**预期行为：** 全项目零告警；取屏幕尺寸走当前上下文（`GeometryReader` 已经在手边）。

**实际行为：** `outsideTapCatcher` 用 `UIScreen.main.bounds` 铺一块全屏透明层。`UIScreen.main` 在 iOS 26.0 已被废弃（SDK 声明：`API_DEPRECATED("Use a UIScreen instance found through context instead (i.e, view.window.windowScene.screen)…", ios(2.0, 26.0))`），而本项目的部署目标正是 iOS 26.0（project.yml），因此每次构建都会产生 4 条告警。

**根因证据：** 一次干净构建的告警列表里只有这 4 条（另有一条与代码无关的 `appintentsmetadataprocessor` 提示），全部指向 `FilmBar.swift:206/207/210/211`。

**影响范围与维护成本：** 当前是 iPhone 竖屏单场景，取值正确，功能不受影响；代价是把项目从「零告警」拉成「有告警」，后续新告警会淹没在噪声里，并且这条 API 在后续系统版本移除时会直接编译失败。定为 P2（有明确影响的非最佳实现）。

**复现方法**

1. 在仓库根目录执行：`xcodebuild -project ShotList.xcodeproj -scheme ShotList -destination 'generic/platform=iOS Simulator' -configuration Debug clean build`。
2. 过滤输出中的 `warning:` 行。
3. 观察：4 条 `'main' was deprecated in iOS 26.0` 全部指向 `ShotList/Views/FilmBar.swift`。
4. 预期正确结果：构建输出中没有来自本项目源码的告警。

**根因（修复时定位）**

`UIScreen.main` 只是取「整屏有多大」的一种写法；收起层真正要的是「覆盖整个可触摸的滚动区，含被导航栏、标签栏压住的那两带」。SwiftUI 里能在 `FilmBar` 内部拿到的几个候选，实测都不等于整屏（iPhone 17 Pro，屏高 874pt，页面顶部内容边距 121pt，底部标签栏区 83pt）：

- `proxy.bounds(of: .scrollView)` 与 `containerRelativeFrame`：得到 402×670，即**扣掉上下内容边距后的可视区**（874 − 121 − 83），位置随滚动跟随。只用它会漏掉导航栏与标签栏那两带——内容延伸到标签栏下面，那里的卡片在面板展开时仍可点开，且滚到底后内容下方的空白也点不到收起。这是第一版实现的实际回归，被验收时与旧构建对拍发现。
- 在 `ScrollView` 上声明具名坐标空间再 `bounds(of:)`：得到 402×675，是 SwiftUI 给 `ScrollView` 的布局框（约 116–791pt），同样不含两带。
- 在 `NavigationStack` 外声明具名坐标空间：`bounds(of:)` 返回 nil，坐标空间不跨过 `NavigationStack` 的承载边界。
- `safeAreaInsets`：在滚动内容里恒为 0，算不出栏高。

**修复状态：** 已修复

**修复说明：** 收起层改为读外层 `ScrollView` 的可视区（`bounds(of: .scrollView)` 直接换算到本视图坐标系，随滚动自动跟随，不在滚动视图里时退化为白条自身），再向四周多铺 `outsideTapBleed = 200pt` 盖住导航栏、标签栏那两带；超出滚动视图的部分收不到触摸，所以这个值只需大于栏高、不是测量值。只改 `FilmBar.swift`，不需要调用方（`HistoryView`）配合，也不引入 UIKit 全局查询。

**验证结果：** Xcode 27.0，`iPhone 17 Pro` 模拟器（iOS 26.5，竖屏、深色），演示数据来自 `Tools/seed-simulator.py`，触摸由模拟器输入注入。

*构建*：同一条命令 `xcodebuild -project ShotList.xcodeproj -scheme ShotList -destination 'generic/platform=iOS Simulator' -configuration Debug clean build`。修复前 4 条 `'main' was deprecated in iOS 26.0`（`FilmBar.swift:206/207/210/211`）；修复后 BUILD SUCCEEDED，源码告警 0 条，仅剩与代码无关的 `appintentsmetadataprocessor` 提示。

*交互*（均在最终构建上执行；面板展开态下点面板外，应「只收起、不触发被点的控件」）：

- 点筛选条上的「未拍」：面板收起，筛选仍停在「全部」；
- 点镜头卡片正文：面板收起，不打开镜头面板；
- 点落在标签栏那一带的镜头 04 卡片右侧（x=365, y=812）：面板收起，不打开镜头面板；
- 向上滚动约 178pt（到底）后：点镜头 03 卡片、点内容下方的空白（y=808）均收起；旧构建对内容下方空白的同一点击也会收起，两者一致；
- 内容较短、无法滚动的影片（咖啡店探店，3 个镜头）：点内容下方空白收起；
- 点下拉里的另一部影片：切换成功且面板收起；点镜头卡片：收起态下正常打开镜头面板，说明收起层只在展开时存在；
- 点导航栏区域（y=108）：面板不收起，旧构建同样不收起（导航栏先接触摸），无变化。

**未验证：** 应用仅支持竖屏、单场景，未测横屏与多窗口；未测特大辅助字号与 VoiceOver 下的收起。真机未验证。

**修复 commit：** 5fb0a623c522c1cfe6c4b27b3ff368e2e3a422a3

<a id="p2-48"></a>

### P2-48 · 删除补偿日志本身损坏时应用永远停在「删除尚未完成」，重试无法自愈也没有出口

**验证状态：** 模拟器实测（iPhone 17 / iOS 26.5 模拟器，隔离测试数据；把容器里的 `pending-deletions.json` 写成非法 JSON）

**代码位置：** ShotList/Models/ShotStore.swift · `recoverPendingDeletions()`（先 `try?` 解 `FilmLibrary`、再 `try` 解 `[Shot]`）、`loadStoredLibrary()`；ShotList/Views/RootTabView.swift · `loadError` 分支

**问题详情**

**预期行为：** 补偿日志读不懂、而 `films.json` 完好时，应用不该因此整个不可用：要么识别出这份日志已损坏并丢弃（或改名留存）后继续，要么至少给用户一个能走出错误页的办法。

**实际行为：** `recoverPendingDeletions()` 对日志先按 `FilmLibrary` 解、失败再按旧版 `[Shot]` 解，两种都解不开时第二次解码抛错；`loadStoredLibrary()` 把它记成 `deletionPending`（P2-46 修复前是「无法读取分镜记录」）并整页拦住。「重试清理」（`retryLoad()`）重走同一段代码、读到同一份坏日志，每次都失败。日志在 Application Support 下，用户碰不到，只有卸载重装能出去，而那会丢掉全部记录。

**根因证据：** `recoverPendingDeletions()` 里 `Data(contentsOf:)` 与两次解码都没有「日志损坏」的分支，抛出的任何错误一律按「这次没清完」处理；错误页的正文与按钮（「请检查设备存储后重试」/「重试清理」）又暗示重试会有用。

**影响范围：** 日志由原子写入产生，正常路径下不会写出坏文件，只有磁盘层损坏、备份只恢复了半份文件、或将来日志格式变更没做兼容才会遇到，触发概率很低；触发后 `films.json` 完好、数据不丢，但应用整体不可用且无自助出口。P2-46 修复前同样如此（只是被标成别的标题），所以不是该修复引入的回归。定为 P2（触发条件苛刻、数据不丢）。

**复现方法**

1. 隔离测试数据：专用模拟器，装好应用并用 `Tools/seed-simulator.py` 灌入演示数据。
2. 终止应用，把容器里 `Library/Application Support/ShotList/pending-deletions.json` 写成内容 `not json`。
3. 启动应用。
4. 观察：整页错误「删除尚未完成」，附「未能读取数据，因为它的格式不正确。」；点「重试清理」页面不变，`films.json` 完好。
5. 预期正确结果：损坏的日志被识别并丢弃（或改名留存），应用正常进入分镜页；至少要有明确出口。
6. 验证完成后删除该文件与隔离数据。

**根因（修复时定位）**

`recoverPendingDeletions()` 对日志先用 `try?` 按 `FilmLibrary` 解，失败后按旧版 `[Shot]` 解时却用的是 `try`：内容损坏时这里抛错，而上层（`loadStoredLibrary()` / `loadLegacyLibrary()`）把「恢复清理里任何一处抛错」一律当作「这次没清完、可重试」。可「日志读不懂」是确定性的，重试读到的还是同一份内容，永远失败。

**修复状态：** 已修复

**修复说明：** `[Shot]` 解码改为 `else if let legacy = try? …`，其后新增最终 `else`：两种格式都解不开就判为损坏，直接 `removeItem` 丢弃这份日志并 `return`，应用继续启动。只改 `ShotStore.swift` 的 `recoverPendingDeletions()`，冷启动、错误页重试、旧版升级（`loadLegacyLibrary()` 里同样先消费日志）三个入口一并受益。边界不变：`Data(contentsOf:)` 读取失败（不是内容损坏，可能是暂时的）与 `removeItem` 失败仍按清理失败走 `deletionPending`，可重试。取舍：丢弃日志就放弃了找回「JSON 已删、文件没删成」那几个片段的关联，但日志只是上一次提交前的旧快照，这些文件仍留在磁盘上，会出现在导出页的「未使用的文件」里，是否清理由用户点击决定（`removeOrphanFiles()` 只被导出页按钮调用，不会被自动删除）；对比之下，应用整体不可用且只能卸载重装（丢掉全部记录）代价大得多。

**验证结果：** Xcode 27.0，专用的 `iPhone 17` 模拟器（iOS 26.5，浅色、竖屏），演示数据来自 `Tools/seed-simulator.py`（占位素材为本地生成的 5 段纯色视频）。构建：`xcodebuild -project ShotList.xcodeproj -scheme ShotList -destination 'id=<UDID>' -configuration Debug build`（增量构建，重编译 `ShotStore.swift`），BUILD SUCCEEDED，源码告警 0 条。

- **修复前（复现，即 P2-46 修复后的基线构建 `8550a74`）：** 日志写成 `not json` 后启动 → 整页「删除尚未完成」，附「未能读取数据，因为它的格式不正确。」；点「重试清理」页面不变，`films.json` 完好。
- **修复后（同一路径）：** 同样写入 `not json` 后启动 → 直接进入分镜页，「夏日vlog」5 个镜头，`pending-deletions.json` 已被删除，`films.json` 的 md5 与启动前一致（0d113756…，没有多写盘）。
- **旧版升级入口：** `seed-simulator.py --legacy` 写旧版 `shots.json` 再放入损坏日志 → 启动后升级成功（`films.json` 与 `shots.json.migrated` 都在），日志已被丢弃，分镜页 5 个镜头。
- **回归 · 有效日志照常恢复：** 构造「日志 = 完整快照、`films.json` 里少了镜头 3（其片段文件仍在磁盘上）」→ 启动后镜头 3「手冲壶」被找回，顺序为 1–5，日志被消费。
- **边界 · 损坏且删不掉：** 损坏日志加 `chflags uchg` → 启动仍是「删除尚未完成」，附「未能移除 pending-deletions.json，因为你没有访问许可」（没有被当成已处理）；`nouchg` 后点「重试清理」→ 进入分镜页，日志被清掉。
- **回归 · 正常编辑：** 点右上角「＋」加镜头 → 弹出「镜头 06」面板，`films.json` 落盘为 6 个镜头，目录里没有残留日志，没有出现错误页。

**未验证：** 真机；真实的磁盘层损坏（模拟器里用写入非法文本代替）；日志为空文件、被截断的 JSON 等其它「解不开」形态（走同一个解码失败分支，未逐一构造）；按 AGENTS 约定未构建 / 运行 Unit Test。

**修复 commit：** 045b378a9a421bbb3da90b3ee0bdff0eb6123a84

<a id="p2-49"></a>

### P2-49 · 体积上限随画质放宽后磁盘可能被录满，而写满时仍然只是「自己停了」且没有任何说明

**验证状态：** 代码级确认（未做磁盘写满的故障注入实测）；用户于 2026-09-21 指示按已修复收口，未验证项以用户的验收结论为准

**代码位置：** ShotList/Camera/CameraRecorder.swift · `updateMaximumFileSize()`（第 614–629 行）、`stopReason(matching:)`（第 1033–1041 行）、`configureIfNeeded`（第 690–694 行，未设置 `minFreeDiskSpaceLimit`）

**问题详情**

**预期行为：** 单条录制不会把设备磁盘录到写满；真的写满时，界面要像撞上限那样明确说明「为什么停了、这条保没保住」。

**实际行为：** [P2-36](#p2-36) 的修复把 `maxRecordedFileSize` 从固定 600 MB 改成按当前格式码率估算（`码率 ÷ 8 × 600 秒 × 1.15`，再与 600 MB 取 `max`）。这一步本身是对的——体积上限不该早于时长上限生效——但它同时拿掉了原先那道「单条最多 600 MB」的磁盘保险，而没有补上替代品：

1. `AVCaptureMovieFileOutput.minFreeDiskSpaceLimit` 全项目没有设置过（已全仓检索确认），所以录制不会在剩余空间见底前主动收尾；
2. 真的写满时，AVFoundation 返回 `AVError.diskFull` 并把 `recordingSuccessfullyFinished` 置为 true（素材完好）。`stopReason(matching:)` 只把 `.maximumFileSizeReached` / `.maximumDurationReached` 记为 `.reachedLimit`，`.diskFull` 落进 `default` 分支被记为 `.userRequested`，于是走静默成功路径：直接进回看，不弹任何提示。

**根因证据：**

- `CameraRecorder.swift:628` `movieOutput.maxRecordedFileSize = max(Int64(estimatedBytes), Self.maximumFileSize)`——高码率格式下这个值远大于 600 MB，实际约束退化为 10 分钟时长上限；
- `CameraRecorder.swift:1036-1039` `case .maximumFileSizeReached, .maximumDurationReached: return .reachedLimit` / `default: return .userRequested`；
- `grep -rn "minFreeDiskSpaceLimit" ShotList/` 无结果。

**影响范围与定级依据：** 这正是 P2-36 原本要解决的症状（「突然自己停了、没有解释」），只是触发原因换成了磁盘写满；而 P2-36 的修复让单条录制可以写到 GB 级，使写满这条路径**更容易**碰到而不是更难。素材本身完好、可回看可保存，不丢数据，用户清理空间后可继续拍，有绕行方式，因此定为 P2 而不是 P1。**未实测**：具体在剩余多少空间时触发、以及 `.diskFull` 在本项目配置下的确切返回形态没有做故障注入验证，本条目不给出未经测量的数值。

**复现方法**

1. 隔离测试环境：真机，先用无关的大文件把可用空间压到很小（不要删除用户真实素材）。
2. 进入任意镜头的取景页，在拍摄设置里选「4K」+「60 fps」。
3. 开始录制，持续拍到磁盘写满。
4. 观察：录制自行停止并直接进入回看，界面没有任何说明；对照 P2-36 修复后的行为，撞体积/时长上限会弹「已达录制上限」，而写满不会。
5. 预期正确结果：要么在剩余空间见底前主动收尾并说明原因，要么在 `.diskFull` 路径上同样给出提示。
6. 验证完成后清理测试用的占位文件。

**修复状态：** 已修复

**修复说明：** 根因两处，均在 `ShotList/Camera/CameraRecorder.swift`：`movieOutput` 没有设 `minFreeDiskSpaceLimit`，而 P2-36 放宽 `maxRecordedFileSize` 之后它不再是磁盘的保险；`stopReason(matching:)` 把 `.diskFull` 落进 `default` 记成 `.userRequested`，走静默成功路径。

修复：

- 新增 `minimumFreeDiskSpace`（500 MB），在 `configureIfNeeded` 里与 `maxRecordedDuration` / `maxRecordedFileSize` 同处赋给 `movieOutput.minFreeDiskSpaceLimit`：剩余空间低于它时 AVFoundation 主动收尾，留出空间给 `films.json` 与系统。
- `StopReason` 新增 `.diskFull`，`stopReason(matching:)` 把 `AVError.diskFull` 归到它；其余 code 仍归 `.userRequested`，不误报。
- `CameraCaptureView` 把原来的「已达录制上限」提示与新增的「存储空间不足」（正文「已停止录制。请释放空间后再拍。」）合并成一个由 `RecordingNotice` 驱动的说明弹层，避免同一视图上叠第三个 `.alert`；照常进入回看，素材不受影响。

**验证结果：** - 构建：Xcode 27.0，iPhone 17 Pro / iOS 26.5 模拟器，Debug 构建成功、无新增告警；既有 107 项测试全部通过。
- 隔离的代码级验证：把 `stopReason(matching:)` 的判定原样搬到仓库外的独立 Swift 脚本，用真实 `AVError` 构造样本，`nil` → `userRequested`、`.maximumFileSizeReached` / `.maximumDurationReached` → `reachedLimit`、`.diskFull` → `diskFull`、其他 `AVError` 与非 `AVError` → `userRequested`，6 项全部通过；验证后已删除脚本，未纳入提交。
- 界面：模拟器没有摄像头，用临时探针（启动参数直接触发，验证后已移除）分别弹出两种说明：「存储空间不足 / 已停止录制。请释放空间后再拍。」与只有标题的「已达录制上限」，版式与「好」按钮正常。
- 待验证（需要真机摄像头，无法在模拟器执行）：`minFreeDiskSpaceLimit` 在真实设备上收尾时返回的确切 error（预期为 `AVError.diskFull` 且 `recordingSuccessfullyFinished` 为 true）；把可用空间压到 500 MB 以下后录制的实际停止时机；500 MB 这个预留量是否合适；停止后回看页说明弹层的真实弹出时机。

用户验收（2026-09-21）：用户指示将本条移入已修复。真机阶段未能覆盖的项——① `minFreeDiskSpaceLimit` 在真机上收尾时返回的确切 error；② 可用空间压到 500 MB 以下后的实际停止时机与预留量是否合适；③ 停止后回看页说明弹层的真实弹出时机——以用户的验收结论为准；用户侧的设备与逐步观察未在此逐项记录。

**修复 commit：** `85382798cbe75097d734c8c950678d84124a2fab`

**来源：** 审查 [P2-36](#p2-36) 的修复实现时发现，属于独立根因（磁盘保险缺失 + `.diskFull` 未被识别），按 AGENTS.md 单独编号，不并入 P2-36 条目。

<a id="p2-50"></a>

### P2-50 · 录制失败时残缺的临时视频不会被删除，一直占着磁盘到下次启动

**验证状态：** 代码级确认（未在真机实测触发）；用户于 2026-09-21 指示按已修复收口，未验证项以用户的验收结论为准

**代码位置：** ShotList/Camera/CameraRecorder.swift · `fileOutput(_:didFinishRecordingTo:from:error:)`；ShotList/Views/CameraCaptureView.swift · `toggleRecording()` 的 `.failure` 分支

**问题详情**

**预期行为：** 录制没有成功结束时，AVFoundation 已经写到临时目录里的 `shot-*.mov` 是残缺的，没有任何路径会再用它，应当随失败一起删掉。

**实际行为：** `didFinishRecordingTo` 在 `error != nil` 且 `recordingSuccessfullyFinished == false` 时只把 `.failure(error)` 交给回调；回调里没有 `outputFileURL`，界面也只弹「录制失败」，没有人删这个文件。只有「没人等这条视频」（相机已关）与「重拍 / 关页面」两条路径会删临时文件，失败路径漏了。残缺文件要等到下次启动时 `cleanUpTemporaryRecordings()` 统一回收。

**根因证据：** `CameraRecorder.swift` 的 `didFinishRecordingTo`：`.failure` 分支不删 `outputFileURL`；`CameraCaptureView.toggleRecording()` 的 `.failure` 只置 `errorMessage`。

**影响范围与定级依据：** 残缺文件的大小取决于失败前已经写出多少（高码率下可达 GB 级）；触发失败的常见原因之一就是磁盘写满（见 [P2-49](#p2-49)），此时留着它会让本来就紧张的空间更紧张，直到用户重启应用才回收。不丢用户数据，有绕行方式（重启应用），因此定为 P2。未实测：真机上失败时文件是否一定存在、大小多少，本条目不给未经测量的数值。

**复现方法**

1. 隔离测试环境：真机，用无关的大文件把可用空间压到很小（不要删除用户真实素材），拍摄设置选「4K」+「60 fps」。
2. 进入任意镜头的取景页开始录制，一直录到因写入失败而停止并弹出「录制失败」。
3. 观察：用「文件」App 或调试命令查看应用临时目录，`shot-*.mov` 仍在；退出并重新启动应用后才被清掉。
4. 预期正确结果：弹出「录制失败」的同时，这个残缺文件已被删除。
5. 验证完成后清理测试用的占位文件。

**修复状态：** 已修复

**修复说明：** 根因是失败路径漏了临时文件回收。修复：`didFinishRecordingTo` 回到主线程后，若结果是 `.failure` 就删除 `outputFileURL`，再走原有的「没人等这条视频就删掉」判断与回调；成功与撞上限的路径不变，文件照常交给回看。

**验证结果：** Xcode 27.0 构建成功、无新增告警，既有 107 项测试全部通过。改动落在真机录制回调上，模拟器没有摄像头，无法触发录制失败，故只做代码级确认。待验证（真机）：按复现方法把可用空间压到很小后录制到失败，确认弹出「录制失败」时临时目录里的 `shot-*.mov` 已被删除；正常录制并保存、撞上限、点「重拍」三条路径的文件处理不受影响。

用户验收（2026-09-21）：用户指示将本条移入已修复。真机阶段未能覆盖的项——① 真机把可用空间压到很小、录制到失败时临时目录里的 `shot-*.mov` 是否已被删除；② 正常保存、撞上限、点「重拍」三条路径的文件处理是否不受影响——以用户的验收结论为准；用户侧的设备与逐步观察未在此逐项记录。

**修复 commit：** `eafafc99176dff36f10b21fba524b677b3b13274`

**来源：** 复查 P2-36 / P2-49 相关的录制收尾回调时发现，属于独立根因（失败路径漏了临时文件回收），按 AGENTS.md 单独编号。

<a id="p2-51"></a>

### P2-51 · 打包途中输入变化使结果被丢弃后，切换影片仍显示「需重新打包」，而新影片从未打过包

**验证状态：** 模拟器实测

**代码位置：** ShotList/Support/ExportSession.swift · `invalidate()`、`build(_:isCurrent:builder:)` 中「输入已变化」的丢弃分支、`isStale`；ShotList/Views/ExportView.swift · `buildSection` 里的 `export.package == nil && export.isStale`

**问题详情**

**预期行为：** 「需重新打包」是对「已经生成的导出包不再对应当前设定」的说明。没有导出包可言的影片（新建的空影片、换到一部从没打过包的影片）不应该显示它。

**实际行为：** `isStale` 只在一次打包成功时被复位为 `false`（`build` 的成功分支），`invalidate()` 只会把它置为 `true`、从不清除。打包途中改了范围或格式时，这次结果被丢弃，`package` 仍为 `nil` 而 `isStale` 被置 `true`；此后只要没有再成功打出一个包，标记就一直留着：切换影片、改分镜、改风格都清不掉它，新建的空影片上也显示「需重新打包」，而此时「打包导出」按钮是灰的，用户无从「重新打包」。

**根因证据：** `ExportSession.invalidate()` 里 `if package != nil || isBuilding { isStale = true }` 没有对应的清除分支；`build` 的丢弃分支再显式 `isStale = true`；唯一的 `isStale = false` 在成功分支。旧版本里同一个标记驱动的是独立小节，现象一致（[R-2](../requirements/delivered-requirements.md#r-2) 验证时发现，不是它引入的）。

**影响范围与定级依据：** 只是一处不准确的界面提示，不影响数据与导出功能，成功打出一个包后自动消失，因此定为 P2。

**复现方法**

1. 模拟器，一部含多段 4K 素材的影片（转码每段耗时数秒，例如 4 个镜头、每镜一条 24 秒 4K HEVC），导出「格式」选「H.264 · 1080p」。
2. 点「打包导出」，进度显示「转码 1 / 4」时把范围改为「全部」。
3. 观察：进度收起、没有结果，打包一节末尾出现「需重新打包」（这次结果被丢弃，符合预期）。
4. 回「分镜」页，影片菜单 →「新建影片」→ 确认，再回到「导出」页。
5. 观察：新影片没有任何导出包，「打包导出」置灰，但「需重新打包」仍在。预期不再显示。

**修复状态：** 已修复

**修复说明：** 根因在 `ExportSession.invalidate()`：`isStale` 只会被置 `true`，唯一的复位在一次打包成功之后。打包途中输入变化使这次结果被丢弃时，`package` 仍为 `nil`、`isStale` 被置 `true`；此后没有成功打出新包，标记就一直留着，切换影片、新建空影片都清不掉。

最小修复：`invalidate()` 里改为赋值 `isStale = package != nil || isBuilding`——只有「有已生成的包」或「有一次刚被作废的在途打包」才算过期。`build` 的丢弃分支仍在 `invalidate()` 之后显式置 `true`，所以「这次打包被丢弃」的提示照常出现，只是之后再有输入变化（例如切换影片）就会把它清掉；有结果时的行为不变（输入变化后照常标为需重新打包）。

**验证结果：** 环境：Xcode 27.0，新建的独立 iPhone 17 / iOS 26.5 模拟器；《重素材》影片 4 个镜头、每镜一条 24 秒 4K HEVC 片段，格式「H.264 · 1080p」（转码每段约十几秒）。Debug 构建成功，无新增告警。没有跑单元测试（按仓库规范只做集成验证）。

按复现方法重走：

- 步骤 2、3：点「打包导出」，进度行显示「打包中」时把范围改为「全部」：立即出现「需重新打包」；等这次被丢弃的打包返回（目录 `tmp/ShotListExport` 清空）后，提示仍在，没有结果一节——符合预期。
- 步骤 4、5：回「分镜」，影片菜单 →「新建影片」→ 确认，再回到「导出」：新影片的打包一节只有「无可导出视频」，**没有**「需重新打包」，「打包导出」置灰。修复前同一路径实测会显示它。
- 回归（有结果）：《夏日vlog》打出结果后改范围，结果一节第一行照常出现「需重新打包」（[R-2](../requirements/delivered-requirements.md#r-2) 的验证覆盖）；已有结果时点「停止」不置过期（[R-3](../requirements/delivered-requirements.md#r-3) 的验证覆盖，该路径不经过 `invalidate()`）。

未验证：无（本条只涉及界面标记的复位）。

**修复 commit：** 712dace4a8d892c8cfd6e50655960f9e470d6901

**来源：** 验证 [R-2](../requirements/delivered-requirements.md#r-2) 时发现，属于独立根因（`isStale` 只清于成功打包），按 AGENTS.md 单独编号。

<a id="p2-52"></a>

### P2-52 · 片段文件已不在磁盘上时点播放，进入一块没有任何出口的黑屏，只能强杀应用

**验证状态：** 模拟器实测（临时探针强制 `clipURL(for:)` 返回 nil）

**代码位置：** ShotList/Views/ShotFlowModifier.swift · `fullScreenCover` 的 `.player` 分支（第 107–117 行，第 116 行 `Color.black.ignoresSafeArea()`）；ShotList/Views/ClipPlayerScreen.swift（关闭按钮只在这里面）；ShotList/Views/ClipOptionsSheet.swift · `clipRow`（第 298 行起，片段行的播放按钮）

**问题详情**

**预期行为：** 要播放的片段文件不存在时，要么不进入播放页，要么进入后能看到原因并关掉。

**实际行为：** `.player` 分支在 `store.clipURL(for: clip)` 为 nil 时渲染 `Color.black.ignoresSafeArea()`。关闭按钮（`xmark`）写在 `ClipPlayerScreen` 里面，这个 else 分支里**一个可点的元素都没有**；而 `fullScreenCover` 不支持下滑关闭。用户看到的是一整屏黑色，点哪里都没有反应，只能从多任务界面强杀应用。

**根因证据：**

- `ShotFlowModifier.swift:108-117`：`if let url = store.clipURL(for: clip) { ClipPlayerScreen(...) } else { Color.black.ignoresSafeArea() }`，else 分支没有任何按钮或 `dismiss`；
- `ClipPlayerScreen.swift:14/32/34`：`@Environment(\.dismiss)` 与关闭按钮只存在于正常分支的视图里；
- `store.clipURL(for:)` 只认 `existingClipFileNames` 缓存，文件不在时返回 nil；而 `ClipOptionsSheet.clipRow` 按 `live.clips`（JSON 记录）逐条画行，记录在、文件不在时这一行照样可点（缩略图此时已显示 `video.slash`）。

**影响范围与定级依据：** 前提是「JSON 里还有记录、文件已经不在磁盘上」——`reconcileClipsWithDisk` 每次切回前台都会清理这类记录，所以触发窗口窄；但一旦触发，界面完全卡死、没有任何出口，属于局部场景下的严重交互异常。不丢数据、重启后恢复，定为 P2。**未实测**：没有在模拟器上构造这一状态验证实际表现。

**复现方法**

1. 隔离测试数据：模拟器上准备一部测试影片，给某个镜头导入一段视频（不要使用用户真实素材）。
2. 代码级验证：在 `ShotFlowModifier` 的 `.player` 分支临时加探针，强制让 `store.clipURL(for: clip)` 返回 nil（或在应用运行期间从模拟器容器里删除该片段文件，并阻止 `refreshStorageStats()` 在此之前运行）。
3. 打开该镜头的面板，点「片段」里那一条的播放。
4. 观察：进入整屏黑色，没有关闭按钮，下滑无效。预期正确结果：不进入播放页并提示文件不存在，或播放页给出说明与关闭按钮。
5. 验证完成后移除探针与隔离数据。

**修复状态：** 已修复

**修复说明：** 在 `ShotFlowModifier.optionsSheet(for:)` 的 `onPlay` 回调里，排队 `.player` 之前先判断 `store.clipURL(for: clip) != nil`：文件已不在磁盘上时不再进播放页，改为关掉镜头面板并弹出提示「视频文件不存在，无法播放」。原先只服务导入失败的 `importError`/`导入失败` 弹层泛化为 `flowError`/「操作未完成」，两类失败共用同一条提示通道。`fullScreenCover` 的 `.player` else 分支不再是裸的 `Color.black.ignoresSafeArea()`，改为 `missingClipScreen`：黑底上加一个与 `ClipPlayerScreen` 关闭按钮同款式的圆形 `xmark` 按钮，点击把 `cover` 置 nil 关闭整屏——即使以后别的路径漏检、带着 nil URL 落进这个分支，也有出口。

**验证结果：** 环境：Xcode 27.0，独立启动的 iPhone 17 Pro Max / iOS 26.5 模拟器（`BF450796-F6EF-49BB-906E-0532203E4CEB`），`Tools/seed-simulator.py` 写入《夏日vlog》等两部演示影片（镜头 1 有 3 段片段）。Debug 构建 `BUILD SUCCEEDED`，无新增告警。

验证方式：在 `ShotStore.clipURL(for:)` 顶部临时加一行按启动参数 `-debugForceMissingClip` 强制返回 nil 的探针（对应复现方法第 2 步的等效做法，覆盖全部片段而不必真的从容器删文件/赛跑 `refreshStorageStats()`），验证完立即移除：

1. 用该参数启动 App，打开镜头 01 的面板，点第 1 条片段的播放：面板关闭，立即弹出「操作未完成 / 视频文件不存在，无法播放。」，**没有**进入黑屏；点「好」后回到正常可交互的分镜列表——与修复前的「整屏黑色、点哪里都没反应」相比，行为已改变，问题描述的异常消失。
2. 额外用第二个临时探针（同样验证完移除）在 `onPlay` 的 guard 上加一个 `-debugForcePlayerFallback` 旁路，让 `.player` 即使在 URL 为 nil 时也被排队，专门触达 `fullScreenCover` 的兜底分支：画面确认为黑底 + 右上角圆形 `xmark` 按钮（无 `ClipPlayerScreen` 的视频与文字，符合预期），点击按钮后正确关闭整屏、回到正常分镜列表，没有卡死。

两个临时探针均已在提交前移除（`git diff` 确认 `ShotList/Models/ShotStore.swift` 无残留改动）。未覆盖：真实的「外部删除文件后 `existingClipFileNames` 缓存滞后」时序竞态本身未在磁盘层面复现（如复现方法所述触发窗口很窄），但探针直接构造了 `clipURL` 返回 nil 这一根因条件，修复逻辑与触发方式无关，因此不影响验证结论。

**修复 commit：** 3d2a66f2ebfde0fd45592342ed4c177670fa5b00

<a id="p2-53"></a>

### P2-53 · 应用内没有任何删除整部影片的入口，影片记录只增不减

**验证状态：** 模拟器实测

**代码位置：** ShotList/Models/ShotStore.swift · `deleteFilm(_:)`（第 462 行）、`deleteCurrentFilm()`（第 472 行）、`discardBlankFilm(_:)`（第 482 行）；ShotList/Views/FilmBar.swift · `dropdownPanel` / `FilmSwitcherMenu` / `FilmActionDialogs`（影片列表、影片菜单与共用确认弹层）；ShotList/Views/ShotListView.swift（`FilmSwitcherMenu` 的接线）

**问题详情**

**预期行为：** 用户能删除一部不再需要的影片（连同它的镜头与素材）。

**实际行为：** `deleteFilm` / `deleteCurrentFilm` 在生产代码里**零调用点**，只有 `ShotListTests` 在调。原先唯一的入口是导出页的「删除影片」，已在 [R-8](../requirements/delivered-requirements.md#r-8)（`0b3572a feat(R-8): 导出页精简…`，2026-09-20）中按需求移除；R-8 交付记录的「遗留」一栏写明「应用内不再有删除整部影片的入口，影片库只增不减；该入口放到哪里需要用户另行决定」。此后影片条下拉面板（重命名 / 新建影片）、分镜页影片菜单（重命名 / 模板 / 剪辑风格 / 新建影片）与影片列表行都没有补上删除入口，也没有滑动删除或长按菜单。这项遗留只记在已交付需求的备注里，不在任何未关闭清单上，因此在此登记跟踪。

**根因证据：**

- `grep -rn "\.deleteFilm\|\.deleteCurrentFilm" ShotList ShotListWidget` 无结果；
- `grep -n "swipeActions\|contextMenu\|role: .destructive\|删除" ShotList/Views/FilmBar.swift ShotList/Views/HistoryView.swift` 无结果；
- `discardBlankFilm` 只会在切换影片 / 新建影片时静默回收**完全空白**的影片（`Film.isBlank`：无镜头、无标题、无剪辑风格、无模板），写过任何一样的影片永远不会被回收。

**影响范围与定级依据：** 素材占用的磁盘**可以**回收——逐个删除镜头或清空片段即可，`commit()` 会删掉不再被引用的文件；删不掉的是影片记录本身。影片下拉面板与影片菜单会随使用一直变长。唯一的绕行方式是把该影片的所有镜头、标题、剪辑风格和模板逐项清空，再切换影片触发静默回收，不可发现且要求用户先亲手删掉自己写的内容。拍摄到导出的主路径不受影响，定为 P2。修复需要先由用户决定入口放在哪里（R-8 遗留的未决问题）。

**复现方法**

1. 模拟器安装 Debug 构建，新建两部影片并各起一个标题。
2. 依次检查：分镜页右上角影片菜单、历史页影片条下拉面板中的每一行（尝试左滑、长按）、导出页。
3. 观察：任何位置都没有删除影片的操作。
4. 预期正确结果：至少有一处可以删除当前影片（或指定影片），并有确认弹层说明镜头与素材会一起删除。

**修复状态：** 已修复

**修复说明：** 入口位置由用户明确指定：分镜页影片菜单（`FilmSwitcherMenu`）与历史页影片条下拉面板（`FilmBar.dropdownPanel`）末尾各加一项「删除影片」，`FilmSwitcherMenu` 用系统 `Button(role: .destructive)`，`FilmBar` 的自绘 `actionRow` 新增 `isDestructive` 参数把图标与文字着红。两处新增的 `isConfirmingDelete` 绑定都接到它们已共用的 `FilmActionDialogs`，新增一个「删除影片？」确认弹层：影片有镜头时写「N 个镜头与视频将被删除，无法恢复。」，没有镜头时只写「无法恢复。」（不出现「0 个镜头」）。确认后调用已有的 `ShotStore.deleteCurrentFilm()`（内部已有完整实现、测试与删除补偿日志），删空影片库时由已有的 `ensureFilmExists()` 自动补一部空白影片；`ExportView` 已有的 `.onChange(of: store.currentFilmID)` 会在切换后自动使旧导出包失效，未改动该文件。

**验证结果：** 环境：Xcode 27.0，独立启动的 iPhone 17 Pro Max / iOS 26.5 模拟器（`BF450796-F6EF-49BB-906E-0532203E4CEB`），`Tools/seed-simulator.py` 写入《夏日vlog》（5 镜头）与《咖啡店探店》（3 镜头）两部演示影片。Debug 构建 `BUILD SUCCEEDED`，无新增告警。

按复现方法对照验证，并覆盖两个入口与边界：

1. 分镜页右上角「影片」菜单：末尾出现红色「删除影片」；点击后弹出「删除影片？ / 5 个镜头与视频将被删除，无法恢复。」，点「删除」后《夏日vlog》从库中移除，当前影片自动切到剩余的《咖啡店探店》（其 3 个镜头完整保留），与 `deleteFilm` 已有实现一致。
2. 历史页影片条下拉面板：末尾同样出现红色「删除影片」行；点击后弹出「删除影片？ / 3 个镜头与视频将被删除，无法恢复。」（此时只剩《咖啡店探店》），确认删除后影片库变空，`ensureFilmExists()` 自动补一部「未命名影片」，历史页与分镜页都正确显示这部新空白影片的「无记录」/「无镜头」空状态，应用没有崩溃或卡死。
3. 边界：对着这部新的空白影片（0 个镜头）再次走分镜页菜单 →「删除影片」，确认弹层文案为「删除影片？ / 无法恢复。」——确认没有出现「0 个镜头」的表述，随后点「取消」结束验证（不再继续删除以保留可用状态）。

未验证：真机上的表现（模拟器验证已覆盖交互与文案，行为不依赖设备能力，判断真机结果一致）；导出页失效提示未在界面上重新截图确认，仅代码核对 `ExportView.swift:48` 的 `.onChange(of: store.currentFilmID)` 逻辑仍在、未被本次改动触及。

**修复 commit：** d68b8bac44f8ba902a6f2499224d0541e276017c

<a id="p2-55"></a>

### P2-55 · 快速拍摄在相机页改走「导入」后取消选片，新建的空镜头不会被撤销

**验证状态：** 模拟器实测

**代码位置：** ShotList/Views/ShotFlowModifier.swift · `drainQueue()`、`.onChange(of: pickerItems)`、新增的 `.onChange(of: isPickerPresented)`、`finishQuickShoot()`；ShotList/Views/CameraCaptureView.swift · `requestImport()`（第 519 行）及其入口（第 376、392、403 行）

**问题详情**

**预期行为：** 快速拍摄会先新建一个空镜头再开相机；用户最终没有拍到、也没有导入任何片段就离开时，这个空镜头应被撤销（`finishQuickShoot` 的注释：「点了快速拍摄又退出」是取消，不该在列表末尾留一个没人要的空镜头）。

**实际行为：** 在相机页点「导入」时，`drainQueue()` 走 `queuedImportShot` 分支，把「是否接着开描述页」记进 `pickerOpensDescription` 后**立刻清空 `quickShotID`**，收尾改由导入完成后接管。但用户若在系统选片器里点取消，`pickerItems` 保持为空，`.onChange(of: pickerItems)` 的 `guard !newValue.isEmpty` 直接返回，导入流程从未开始；`quickShotID` 又已经是 nil，`finishQuickShoot()` 永远不会撤销这个镜头。结果列表末尾多出一个没有内容、没有片段的镜头。

**根因证据：** `ShotFlowModifier.swift:230-231` 在选片器呈现**之前**就清空了 `quickShotID`；选片被取消这条路径上没有任何回调去补做 `finishQuickShoot()` 的撤销逻辑。

**影响范围与定级依据：** 相机页的「导入」按钮出现在未授权摄像头、首次请求权限、相机不可用三种页面上（`CameraCaptureView.swift:376/392/403`），模拟器没有摄像头，走的正是「相机不可用 → 导入」这条路，因此在模拟器上可稳定触发。后果是列表里多一个空镜头，用户可以手动删除，定为 P2。**未实测**。

**复现方法**

1. 在模拟器上（没有摄像头）打开分镜页，长按右上角加号，点「快速拍摄」。
2. 相机页显示「相机不可用」，点「导入」。
3. 在系统选片器里点取消。
4. 观察分镜列表末尾：多出一个空镜头。预期正确结果：列表恢复到快速拍摄之前的样子。

**修复状态：** 已修复

**修复说明：** `quickShotID` 的清空推迟到 `.onChange(of: pickerItems)` 真正拿到非空选择、导入即将开始时才做；`drainQueue()` 不再提前清空它。新增 `.onChange(of: isPickerPresented)` 监听选片器关闭：选中过内容时 `quickShotID` 已经被前一个 `onChange` 清空，这里的 `finishQuickShoot()` 是空操作；没有选择就直接取消时 `quickShotID` 还在，据此调用 `finishQuickShoot()` 撤销这次快速拍摄新建的空镜头。非快速拍摄发起的普通导入全程 `quickShotID` 为 nil，`finishQuickShoot()` 恒为空操作，不受影响。

**验证结果：** 环境：Xcode 27.0，独立启动的 iPhone 17 Pro Max / iOS 26.5 模拟器（`BF450796-F6EF-49BB-906E-0532203E4CEB`），`Tools/seed-simulator.py` 写入《夏日vlog》（5 镜头）演示影片；用 `ffmpeg` 生成一段 1 秒 H.264 占位视频，`xcrun simctl addmedia` 加入模拟器相册用于测试「选中视频」分支。Debug 构建 `BUILD SUCCEEDED`，无新增告警。真实长按手势在自动化输入下无法稳定触发系统的 `onLongPressGesture`（同一枚举实测无论 `duration` 参数还是显式 `touch_path` 带延时都被识别成普通点击），因此用一个临时的、按启动参数 `-debugQuickShoot` 触发 `quickShoot()` 的探针代替长按这一步，探针只影响触发方式，不改变之后的状态机路径；验证完成后已移除（`git diff` 确认 `ShotListView.swift` 无残留改动）。

按复现方法重走，并额外覆盖成功导入的对照路径：

1. 步骤 1–4（取消路径）：触发快速拍摄 → 相机页「需要访问摄像头」页点「导入」→ 系统选片器（无视频时先确认为空，验证「相机不可用 / 未授权 / 首次请求」三种入口共享同一条路径）→ 点左上角「×」取消：分镜列表精确保持 5 个镜头（1–5），**没有**出现第 6 个空镜头——与复现方法描述的修复前行为（多出一个空镜头）相反，问题消失。
2. 对照（成功导入路径，确认未破坏既有功能）：重新触发快速拍摄 → 同一入口点「导入」→ 这次选中相册里的占位视频并确认：新建的「镜头 06」保留、片段列表显示 1 段 0:01 的片段，且自动弹出该镜头的面板并把光标聚焦到内容输入框（`opensDescription` 生效）——与「导入成功后接着开描述页」的既有路径完全一致，未被本次改动破坏。

**修复 commit：** 6bc3ea38cdc6abf28b275334309d8f9dd7cc327f

<a id="p2-56"></a>

### P2-56 · 批量添加入口移除后，ShotStore.addShots(count:) 只剩测试在调用

**验证状态：** 代码级确认（全仓检索调用点）

**代码位置：** ShotList/Models/ShotStore.swift · `addShots(count:)`（原第 427 行，已删除）；ShotListTests/ShotStoreTests.swift（原第 51、183 行，两处断言已删除）

**问题详情**

**预期行为：** 生产代码里的公开写入接口都有实际调用方；需求删除一个功能时，相应的接口一并移除。

**实际行为：** [R-11](../requirements/delivered-requirements.md#r-11)（`772ea00 feat(R-11): 长按加号去掉批量添加，只保留「快速拍摄」`，2026-09-20）按需求移除了「一次添加 3 / 5 / 10 个镜头」，但 `ShotStore.addShots(count:)` 保留了下来。现在它在生产代码中零调用点，只有 `ShotStoreTests` 的两处断言在用。

**根因证据：** `grep -rn "\.addShots" ShotList ShotListWidget` 无结果；`grep -rn "addShots" ShotListTests` 命中第 51、183 行。

**影响范围与维护成本：** 不影响任何用户可见行为。代价是 `ShotStore` 保留一条没有入口的写路径：它和 `addShot` / `insertShot` 走同一套 `normalize()` + `persist()`，今后改动编号或文件名规则时仍需同步维护它和它的测试，而它已经不对应任何功能。定为 P2（有明确维护成本的无用代码）。

**复现方法**

1. 在仓库根目录执行 `grep -rn "\.addShots" ShotList ShotListWidget`，确认无结果。
2. 执行 `grep -rn "addShots" ShotListTests`，确认只剩测试调用。
3. 预期正确结果：功能移除后接口与其测试一并删除，或该接口重新有调用方。

**修复状态：** 已修复

**修复说明：** 删除 `ShotStore.addShots(count:)` 与 `ShotStoreTests` 中对应的两处断言（原第 51、183 行）。两处分别测的是「读不出记录（`loadError`）」与「写盘失败（`saveError`）」时写入被拦截；同一块里紧挨着的 `XCTAssertNil(…addShot())`（第 50、182 行，保留）已覆盖同类拦截，删除后覆盖不减。只删这两行断言，没有新增任何测试。

**验证结果：** 环境：Xcode 27.0。`xcodebuild -scheme ShotList -destination 'generic/platform=iOS Simulator' -configuration Debug build` → `BUILD SUCCEEDED`，无新增告警；另跑 `xcodebuild -scheme ShotList -destination 'id=<模拟器>' build-for-testing` 确认改动后的 `ShotStoreTests.swift` 仍能正常编译（`TEST BUILD SUCCEEDED`），未运行测试。全仓检索 `grep -rn "addShots" ShotList ShotListTests ShotListWidget` 无任何结果，确认接口与其测试已一并删除、没有遗留引用。

**修复 commit：** 1a6318a9b141cb45c5d5d690164dc091ef14bb18

<a id="p2-58"></a>

### P2-58 · 选片器关闭时的快速拍摄收尾依赖两个 onChange 的执行顺序，顺序颠倒时导入会被静默丢弃

**验证状态：** 模拟器实测（iPhone 17 Pro Max / iOS 26.5）；「回调顺序颠倒」本身在当前系统上无法人为触发，其鲁棒性由推迟执行的构造保证，见验证结果

**代码位置：** ShotList/Views/ShotFlowModifier.swift · `.onChange(of: pickerItems)`（第 144–156 行）、`.onChange(of: isPickerPresented)`（第 157–165 行，第 163 行 `pickerTarget = nil`、第 164 行 `finishQuickShoot()`）、`finishQuickShoot()`（第 300–310 行）；ShotList/Models/ShotStore.swift · `addClip`（第 614 行 `targetMissing`）

**问题详情**

**预期行为：** 用户在选片器里选中视频时，无论系统先通知「选择变了」还是先通知「选片器关了」，导入都照常进行；只有真正取消选片时才撤销快速拍摄新建的空镜头。

**实际行为：** [P2-55](resolved-issues.md#p2-55) 的修复新增了 `.onChange(of: isPickerPresented)`：选片器一关就把 `pickerTarget` 置空并调用 `finishQuickShoot()`。它能正确区分「选中」与「取消」，完全依赖一个前提——选中视频时 `.onChange(of: pickerItems)` 必须**先于**它执行，先清空 `quickShotID`、取走 `pickerTarget` 开始导入。这个先后顺序是 SwiftUI 的实现细节，没有写进文档。一旦顺序颠倒：

1. **普通导入**（镜头面板里的「导入」）：第 163 行先把 `pickerTarget` 置空，随后 `pickerItems` 的 handler 在 `guard let target = pickerTarget` 处直接返回——**选中的视频被静默丢弃**，没有任何提示。这一句对修复 P2-55 本身并不需要，却把影响面扩大到了所有导入。
2. **快速拍摄接力的导入**：`finishQuickShoot()` 先执行，此时 `quickShotID` 仍在、镜头还没有片段，于是 `store.delete(shot)` 把镜头删掉；随后导入因 `ShotStore.addClip` 找不到目标镜头而失败，提示「镜头已被删除」。

**根因证据：** 第 157–165 行在选片器关闭的同一次更新里同步做出「是否取消」的判断，而判断所依据的 `quickShotID` / `pickerTarget` 要等另一个 `onChange` 执行后才是最终值。

**影响范围与定级依据：** P2-55 修复时已在 iOS 26.5 模拟器上实测两条路径都正常，说明当前系统里 `pickerItems` 的回调先执行，**目前不会触发**；风险在于系统版本变化后回调顺序改变，届时所有导入都可能静默失效。属于有明确影响的实现缺陷，定为 P2。

**复现方法**

1. 代码级验证：在 `.onChange(of: isPickerPresented)` 与 `.onChange(of: pickerItems)` 里各加一条带时间戳的探针，模拟器上选中一段视频，确认当前的执行顺序是 `pickerItems` 在先。
2. 顺序颠倒的推演：把第 157–165 行的逻辑挪到 `pickerItems` 的 handler 之前执行（或临时交换两个 `onChange` 的读写时机），观察普通导入被丢弃、快速拍摄导入报「镜头已被删除」。
3. 预期正确结果：两种顺序下导入都照常进行，只有取消选片时才撤销空镜头。
4. 验证完成后移除探针。

**修复状态：** 已修复

**修复说明：** 去掉 `.onChange(of: isPickerPresented)` 里的 `pickerTarget = nil`，并把 `finishQuickShoot()` 改为 `DispatchQueue.main.async` 推迟到下一轮主线程执行。前者是修复 P2-55 用不到、却会在顺序颠倒时让**所有**导入静默丢弃的那一句——陈旧的 `pickerTarget` 本来就无害：下次呈现选片器前由 `drainQueue` 重新赋值，`pickerItems` 的 guard 也要求非空选择。后者让「是否取消」的判断总在同一次更新的全部 `onChange` 回调之后执行：选中视频时 `pickerItems` 的回调必然已清空 `quickShotID`，`finishQuickShoot()` 是空操作；取消时 `quickShotID` 仍在，照常撤销空镜头。只改这一个 handler，P2-55 的行为不变。

**验证结果：** 环境：Xcode 27.0，iPhone 17 Pro Max 模拟器 / iOS 26.5（无摄像头，相机页走「需要访问摄像头 → 导入」），`ffmpeg` 生成 3 秒测试视频并 `simctl addmedia` 加入相册。在两个 `onChange` 与推迟块里各加一条带标记的临时 `NSLog`，用 `log stream` 记录实际执行顺序；三条路径均通过界面实际操作完成（长按加号的真实手势在本次可以触发，未使用启动参数探针）：

1. **快速拍摄 → 导入 → 取消选片**：列表保持原来的 6 个镜头，没有残留空镜头（P2-55 行为保持）。顺序：`dismiss-sync(target=1, quick=1)` → `dismiss-deferred(quick=1)` → 撤销；`pickerTarget` 不再被置空。
2. **快速拍摄 → 导入 → 选中视频**：新建「镜头 07」并带上 0:03 片段（片段 1 · 3 秒），描述页自动打开且光标在「内容」框。顺序：`pickerItems(count=1, target=1, quick=1)` → `dismiss-sync(target=0, quick=0)` → `pickerItems(count=0)`（回调里清空选择） → `dismiss-deferred(quick=0)`，推迟块排在最后、读到的是最终状态。
3. **镜头面板普通「导入」（原先那句 `pickerTarget = nil` 威胁的路径）**：镜头 4 从虚线加号变为带 0:03 片段。顺序：`pickerItems(target=1, quick=0)` → `dismiss-sync` → `dismiss-deferred(quick=0)`，收尾为空操作。

当前系统上「选择变了」先于「选片器关了」通知，所以旧写法也能跑通；本修复消除的是对这一先后顺序的依赖。**验证限制**：无法在当前系统上人为制造「顺序颠倒」，这部分的正确性来自构造——推迟块在本次更新的全部回调之后执行，上面三组日志都显示它排在最后。临时 `NSLog` 探针已全部移除（`grep -n "PROBE58\|NSLog" ShotList/Views/ShotFlowModifier.swift` 无结果），移除后 `generic/platform=iOS Simulator` 构建 `BUILD SUCCEEDED`、无新增告警。模拟器里的「夏日vlog」测试影片因此多了镜头 07、镜头 4 多了一段片段，属于种子测试数据，可用 `Tools/seed-simulator.py` 重置。

**修复 commit：** 406ee2933fb5bb63fbc8292948c903c9a16abd4b
