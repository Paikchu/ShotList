# 分镜助手 · 未完成需求

更新日期：2026-09-20。共 **7 项未完成**：**P0 0 项、P1 2 项、P2 5 项**。保留原需求编号，按优先级及编号排序。

未完成需求保留在优先级分组；完成项移至 [delivered-requirements](delivered-requirements.md)，保留验收与实现提交以便追溯。边界条件或验收标准缺失时，先按现有产品行为和代码补全；无法判断且影响实现方向时再向用户澄清。

## 汇总 Checklist

| 完成 | 编号 | 需求标题 |
|:--:|---|---|
| ☐ | R-4 | [用户停止录制后应当立刻看到回看画面，而不是先等界面卡一下](#r-4) |
| ☐ | R-6 | [用户点分镜页里还没拍的镜头的虚线加号时，应直接进入该镜头的拍摄，而不是先进镜头面板再点「拍摄」](#r-6) |
| ☐ | R-1 | [用户在任何一屏看到的界面语言都应是标准的软件提示语，能用图标或版式表达的就不写说明文字](#r-1) |
| ☐ | R-2 | [用户打开导出页时应先看到「这次导什么、结果在哪」，说明性内容按当前影片实际会产出的东西给](#r-2) |
| ☐ | R-3 | [用户在导出转码进行中改主意时，应当能直接停下这次导出](#r-3) |
| ☐ | R-7 | [用户要让下一个分镜沿用上一个分镜的字幕与角标版式时，应能一步带过来再改，而不是重新敲一遍](#r-7) |
| ☐ | R-8 | [用户在导出页应只看到导出所需的设置，「打包导出」是固定在底部的强调色独立按钮](#r-8) |

☐ 未完成；☑ 已交付并验证通过。

## P0

暂无未完成需求。

## P1

<a id="r-4"></a>

### R-4 · 用户停止录制后应当立刻看到回看画面，而不是先等界面卡一下

**验证状态：** 模拟器实测（构建与既有测试）+ 代码级确认；停止录制的实际手感待真机实测

**代码位置：** ShotList/Views/CameraCaptureView.swift · `enterReview` / `cancelReviewTasks` / `resolvedDuration`；ShotList/Camera/CameraRecorder.swift · `activatePlaybackAudioSession`

**预期结果：** 点「停止」之后回看界面与第一帧立即出现，界面不出现卡顿；音频稍后接上开始播放。点「保存」之后回到取景（或关闭相机）同样没有可感知的等待。「回看 → 重拍」回到取景后仍然录得到声音。

**需求详情**

**当前行为：** 用户反馈每次停止录制都要等较长时间才能继续。停止到可继续拍摄之间串了三件事：

1. AVFoundation 收尾写文件（`movieOutput.stopRecording()` → `didFinishRecordingTo`），不在应用控制范围内；
2. `enterReview` 在**主线程同步**把音频会话从录制模式切到播放模式（`setCategory` + `setActive`）。这两个调用要等音频路由真的切过去才返回，期间主线程画不了任何东西；
3. 点「保存」之后才开始解析视频时长（`AVURLAsset` 加载），用户要为这一步再等一次。

**目标行为：** 第 2、3 两件事都不再占用「用户正在等」的那段时间。

- 音频会话切换挪到 `CameraRecorder.sessionQueue` 上异步执行，回看界面与播放器先立起来，切完再 `play()`；
- 时长解析提前到进入回看时就在后台开始，用户看回放的这段时间正好把它算完，点「保存」时直接取结果。

**范围：** 仅相机取景页的「停止 → 回看 → 保存 / 重拍」这条路径。

**明确不做：**

- **不引入 iOS 27 的 ProVideoStorage。** 核对过 iOS 27.0 SDK 头文件（`AVProVideoStorage.h`、`AVCaptureFileOutput.h` 的 `usesProVideoStorage`）：它保的是高码率捕获**过程中**的写入吞吐确定性（面向 ProRes），不缩短停止时的收尾；而且容量由用户在系统设置里配、会随录制消耗、`busyReasons` 非空时启动录制直接失败。当前录制为 H.264/HEVC 且有 600MB / 10 分钟上限，够不上适用场景，引入它只会给主路径新增失败分支。等接入 ProRes 或长时间持续录制时再单独立项。
- **不改 `MediaFileCopy` 的落盘方式。** 曾假设 `ShotStore.addClip` 里那次 `temporaryCopy` 是全量字节拷贝、按文件大小收费，并据此改成硬链接。实测推翻：在 iOS Foundation 下对 600MB 文件调 `FileManager.copyItem`，耗时 0.000 秒、卷可用空间零增长——APFS 会做 COW 克隆，这一步本来就不按大小收费。改动已回退，`copyItem` 保留（克隆语义比硬链接更安全：硬链接会让源与目标共享同一个 inode）。

**对其他功能的影响：** 音频会话切到播放模式与 `resumeSession()` 里切回录制模式排在同一条串行队列上，先后顺序由调用顺序决定，「回看 → 重拍」不会出现播放模式后到、把录制模式盖掉导致回到取景录不到声音。

**验收方法**

1. 前置环境：真机安装 Debug 构建（模拟器没有摄像头，这条路径走不到），准备一部至少含 1 个镜头的影片，分别在外放和蓝牙耳机两种输出下各走一遍。
2. 进入取景页，录制约 10 秒，点「停止」。观察：回看界面与第一帧是否立即出现，中间有没有可感知的卡顿。
3. 在回看页立即点「保存」，确认回到取景（或关闭相机）没有可感知等待，且片段时长在分镜卡片上显示正确、非 0。
4. 重复第 2 步，这次在回看页点「重拍」，立刻再录一条并确认**录得到声音**（验证音频模式没有被播放模式盖住）。
5. 停止录制后立刻下拉关闭相机页，确认没有崩溃、没有残留 `shot-*.mov` 临时文件。
6. 录满 10 分钟上限让 AVFoundation 自动收尾，确认同样进入回看且时长正确。
7. 失败判据：停止后界面出现肉眼可见的冻结；重拍后录不到声音；保存后片段时长为空或为 0。

**实现状态：** 已实现，待真机验收

**实现说明：** `enterReview` 不再在主线程切音频会话，改为先建立回看界面与 `AVPlayer`，再由 `playbackTask` 等 `activatePlaybackAudioSession()` 完成后 `play()`；`durationTask` 在进入回看时即开始解析时长，`save` 经 `resolvedDuration` 取结果，任务被取消时当场重新解析兜底。两条在途任务在重拍、关页面、进入下一次回看前统一由 `cancelReviewTasks()` 取消。

**验证结果：** 模拟器（iPhone 17 Pro，iOS 26）上 107 项既有测试全部通过，构建无新增告警；停止录制的实际手感与第 4 步的录音回归待真机验证。

**实现 commit：** 待提交；完成后填写实际实现提交的完整 SHA。

<a id="r-6"></a>

### R-6 · 用户点分镜页里还没拍的镜头的虚线加号时，应直接进入该镜头的拍摄，而不是先进镜头面板再点「拍摄」

**验证状态：** 待验证

**代码位置：** 待实现。现状为 ShotList/Views/ShotCardView.swift · `body`（整张卡片是一个 `Button`）；ShotList/Views/ClipThumbnailView.swift · 未拍时的虚线框与 `plus`（第 83–89 行）；ShotList/Views/ShotListView.swift · `shotRow`（第 118–143 行，点击只会 `sheet = .options(shot)`）；ShotList/Views/ShotFlowModifier.swift · `cover` / `queuedCover`（相机 `fullScreenCover` 是这个 modifier 的私有状态，目前只有镜头面板里的「拍摄」能触发它）

**预期结果：** 分镜页上，还没有片段的镜头，卡片左侧的虚线加号可以直接点：点一下就进入这个镜头的相机取景页，中间不经过镜头面板。卡片其余区域的点击行为不变，仍打开镜头面板。

**需求详情**

**当前行为：** 镜头还没拍时，卡片左侧缩略图位置是一个虚线框加号，视觉上就是「在这里加视频」的按钮；但点它与点卡片任何位置一样，只是打开镜头面板（`ClipOptionsSheet`），用户还要再点一次「拍摄」才进相机。一部影片有多个未拍镜头、逐个拍摄时，每个镜头都要多走「点卡片 → 等面板弹出 → 点拍摄 → 等面板收起」这几步。

**目标行为：**

1. 没有任何可用片段的镜头（判据与缩略图走到虚线加号那一支一致，即磁盘口径的「未拍」）上，虚线加号是一个独立的点击目标；点击后直接以该镜头进入相机取景页（效果等同镜头面板里点「拍摄」），并给一次 `Haptics.impact(.light)`。
2. 拍完（保存 / 关闭相机）回到分镜页，不经过镜头面板；该镜头卡片立即显示缩略图，虚线加号消失。相机页里既有的「从相册导入」入口照旧。
3. 卡片上加号以外的区域（描述、占位线、空白处）点击行为不变，仍打开镜头面板——写描述、导入、插入 / 复制仍走面板。
4. 已拍过的镜头没有加号，行为完全不变：点卡片仍打开镜头面板。
5. VoiceOver：卡片目前是一个合并的无障碍按钮（`accessibilityElement(children: .ignore)`），加号不会作为独立元素被读到。为未拍镜头增加一个自定义无障碍操作「拍摄」，效果与点加号一致；默认激活动作仍是打开镜头面板。已拍镜头不提供该操作。

**范围：** 分镜页（`ShotListView`）未拍镜头卡片上的虚线加号。

**推断（未经用户确认）：** 历史页的卡片（`HistoryView.card(for:)`）与分镜页共用 `ShotCardView` 和 `ShotFlowModifier`，「全部 / 未拍」筛选下同样会出现虚线加号。为避免同一个加号在两个标签页行为不一致，历史页同步生效；用户原话只提到分镜页，如只想改分镜页，需在实现前说明。

**明确不做：**

- 不改「新增分镜」（右上角加号、空状态按钮）的流程——新增仍是建一个空镜头并打开面板、光标落在描述框；本条只管**已存在的**未拍镜头上的那个加号。
- 不改相机页本身与导入流程；不在加号上加长按菜单或「拍摄 / 导入」二选一。
- 不做「拍完一条自动跳到下一个未拍镜头」，那是另一条需求。

**对其他功能的影响：**

- 整张卡片是一个 `Button`，加号要成为独立点击目标，就不能在它里面再嵌一个 `Button`（命中会被外层吞掉或双触发）。实现时需要把卡片改成「整卡点击区域 + 加号覆盖层」的结构，并确认长按 `contextMenu`、左滑删除、`onMove` 拖动排序都不受影响。
- 相机的 `fullScreenCover` 目前只有「面板 → 拍摄」能触发，需要给它增加一个从列表直接触发的入口，并沿用 `drainQueue` 的串行呈现约束，避免在别的弹层还没收起时弹相机。
- 镜头面板点「拍摄」前会先 `flushDraft()` 保存描述草稿；直接进相机没有草稿，不受影响。相机页对「描述为空」已有处理，未写描述的镜头也可以直接拍。
- 直接进相机后，「从相册导入」只剩相机页内的入口，或返回后点卡片其余区域走镜头面板。

**定级依据：** 由用户指定为 P1。「拍摄」是主路径，逐个拍未拍镜头时每个镜头都多一次面板往返；绕行方式是走镜头面板，但每个镜头都要多几步。

**验收方法**

1. 前置环境：真机安装 Debug 构建；模拟器没有摄像头，只能验证到「点加号 → 直接出现相机页」，录制、保存、回到分镜页要真机。准备一部含 4 个镜头的影片：镜头 1、2 未拍且描述为空，镜头 3 未拍且写过描述，镜头 4 已拍。
2. 进入「分镜」页，点镜头 1 的虚线加号：观察是否**直接**进入相机取景页，中间没有镜头面板弹出；取景页对应的是镜头 1。
3. 录一小段，点「保存」，再关闭相机：回到分镜页，镜头 1 卡片显示缩略图与时长，虚线加号消失；镜头面板没有出现。
4. 点镜头 2 卡片上加号以外的位置（描述占位线、卡片右侧空白）：确认仍是镜头面板弹出；在面板里点「拍摄」仍可正常进入相机。
5. 点镜头 3 的加号，确认取景页显示镜头 3 的描述；点镜头 4（已拍）的缩略图，确认打开镜头面板，行为与改动前一致。
6. 长按镜头 2 卡片弹出上下文菜单、左滑出现「删除」、拖动排序：三者仍可用，且不误触发相机。
7. 打开 VoiceOver，聚焦镜头 2：自定义操作里有「拍摄」，触发后进入相机；聚焦镜头 4：没有「拍摄」操作。
8. 切到「历史」页的「全部」筛选，点未拍镜头的加号，行为与分镜页一致。
9. 在系统设置里关闭本应用的相机权限后再点加号：走与「面板 → 拍摄」相同的权限说明页，不是无反应或崩溃。
10. 失败判据：点加号后仍先出现镜头面板；点卡片其余区域进了相机；已拍镜头的点击行为变化；相机关闭后停在镜头面板而不是分镜页；一次点击同时触发面板与相机。

**实现状态：** 未开始

**实现说明：** 待实现；完成后记录卡片点击区域的拆分方式、从列表直接触发相机的入口放在哪里，以及历史页是否同步。

**验证结果：** 待验证；完成后记录环境、逐步观察结果与验证限制。

**实现 commit：** 待提交；完成后填写实际实现提交的完整 SHA。

## P2

<a id="r-1"></a>

### R-1 · 用户在任何一屏看到的界面语言都应是标准的软件提示语，能用图标或版式表达的就不写说明文字

**验证状态：** 模拟器实测（除下列未验证项）；无障碍标签为代码级确认，回看页与失败提示待真机

**代码位置：** 现状分布在 ShotList/Views/ 全部界面文件、ShotList/Support/DesignSystem.swift（进度环）、ShotList/Models/ShotStore.swift（存储错误）、ShotList/Models/Shot.swift 与 Film.swift（状态、无障碍与进度文本）、ShotList/Support/ExportPackageBuilder.swift（`ExportScope.title`、`ExportTranscodeOption.detail`、`ExportPackage.summary`、`ExportProgress.text`、`ExportError`、`导出说明.txt`）、ShotList/Camera/CameraRecorder.swift（相机错误）。典型落点见「需求详情」。

**预期结果：** 全应用的按钮、标题、空状态、确认弹层、错误提示统一成软件界面惯用的短语（动词 / 名词 / 图标 + 数字），不再是解释给用户听的自然语言句子。图标、数字、版式、虚线占位已经能表达的信息，不再另写文字。无障碍标签与提示同步跟着改，不留两套说法。

**需求详情**

**当前行为：** 界面上大量文案是「解释型长句」——把设计取舍、实现原因、边界条件都写进了用户能看到的位置，读起来像旁白或说明书，而不是软件界面的提示语；有图标或版式能说清的地方也还在配一句文字。抽样（全部为当前代码中的原文）：

- `ClipOptionsSheet`：脚注「描述写「拍什么」，字幕与角标写「画面上显示什么」。这两样会被剪辑侧原样使用，不会替你改写；留空就是这一镜不出。」；提示「打开相机，给这个镜头再录一条，之前拍的会保留」；按钮「从相册再添加」「清空这个镜头的 N 段片段」。
- `ExportView`：「只会导出《X》的镜头，别的影片不会被一起带走。」「还没有可导出的视频。先到「分镜」里拍一段，再回来导出。」；「压缩包里有什么」每个文件下一行说明；数据线取到电脑的两段路径说明。
- `FilmBar` / `FilmActionDialogs`：「点这里给影片起个名字」「留空也可以，之后在影片菜单里按日期找得到。」「《X》会留在影片库里，随时可以载入继续拍。现在打开一部空白影片。」
- `CameraChrome` / `CameraCaptureView`：「这个分镜还没有写描述」；录制按钮提示「拍完可以回看，确认后再保存……」；权限页整段说明；「正在保存到分镜…」「正在启动相机…」。
- `CameraSettingsSheet`：分辨率、帧率脚注各一段解释。
- `FilmStyleView`：两段脚注与「可以写这些」下每条带例句的清单。
- `ShotStore` 的错误文案：「无法读取分镜记录，已暂停编辑和文件清理，原文件不会被覆盖。请恢复可用的分镜记录后重试。」——「请恢复可用的分镜记录」对用户不可执行，「已暂停编辑和文件清理」是内部状态。

**目标行为：** 先定一份写法约定，再按约定逐屏重写：

| 场景 | 写法 |
|---|---|
| 按钮、菜单项 | 动词或动宾短语，一般不超过 4 个字：拍摄、导入、分享、删除、完成；不解释后果（后果放确认弹层）。 |
| 有通用图标的操作 | 图标为主：关闭、更多、展开 / 收起等只画图标，名称写进无障碍标签。列表与菜单里的动作图标 + 短动词并列。 |
| 标题、分组名 | 名词，不超过 4 个字；同一屏里分组标题与行标题不重复。 |
| 状态与统计 | 图标 + 数字（✓ 3/5、时长、体积），不写「已拍 3 / 5 个镜头」这类句子；图标含义不明确的地方保留 1–2 字标签。 |
| 空状态 | 图标 + 一个名词短语（「无镜头」）+ 至多一个动作按钮，不写说明句与鼓励语。 |
| 确认弹层 | 标题为「动作 + 对象 + ？」；正文只写不可逆后果或波及数量（「N 段视频将被删除，无法恢复。」），无后果可写时不写正文；按钮为动词与「取消」。 |
| 错误提示 | 「动作 + 失败」加用户能执行的一步（重试、释放空间、改用原片）；不把内部状态与设计取舍写给用户；做不到时写「已保留原内容」。 |
| 输入框 | 字段名靠图标与短标签常驻，占位只留字段名或一个短示例，不写「例如：…」引导语。 |
| 脚注、提示、无障碍 hint | 默认不写。只有会改变用户决定的取舍（导出格式的兼容与体积）保留一行；hint 只补标签没说清的操作结果。 |
| 设计理由 | 不进界面，留在代码注释里。 |

**范围：** ShotList/Views/ 下的全部用户可见字符串及其无障碍标签，加上 `ShotStore` 的 `loadError` / `saveError` 文案、`ExportError` 的 `errorDescription`、`ExportScope.title`、`ExportTranscodeOption.detail`、`ExportPackage.summary` 与导出进度文本、相机错误文案、`导出说明.txt`。允许为替换文字而新增或调整 SF Symbols，但不改布局结构、功能与信息架构。

**明确不做：** 不改导出包内的 `分镜文字内容指南.md` 与 `剪辑风格.md`，也不改 `分镜清单.csv`——那几份是写给剪辑工具 / AI 读的，长句与显式约束是它们的功能。不改「剪辑风格」页输入框里的整段示例（它是可编辑内容的样本，不是界面说明）。不做多语言（`AppLocale` 仍固定简体中文）。不改功能、导航与数据；需要重排结构的部分归 [R-2](#r-2)。

**对其他功能的影响：** `ExportPackageBuilder.writeReadme` 里的 `导出说明.txt` 对用户可见，属于本需求范围，但它与包内给 AI 的两份文件共用一些变量，改写时不要连带改到那两份的措辞。少数现有测试断言了可见文本（`ExportProgress.text`、`ExportError` 的描述），随文案同步更新断言。

**验收方法**

1. 前置环境：模拟器安装 Debug 构建，`Tools/seed-simulator.py` 灌入演示数据（含 5 个镜头、3 个已拍、写过标题与风格描述的影片）。
2. 按「分镜 → 镜头面板 → 取景页（`-chromeDemo` / `-cameraDemo`；真实拍摄需真机）→ 历史 → 导出 → 剪辑风格」的顺序逐屏走一遍，对每一屏截图，含空状态、确认弹层与错误提示。
3. 逐条核对约定表：屏幕上是否还存在解释设计取舍或复述操作步骤的句子；按钮是否只剩动词或图标；状态是否是图标 + 数字；空状态是否只剩「图标 + 名词 + 一个按钮」。
4. 打开 VoiceOver（或读无障碍树）再走一遍同样的路径，确认朗读内容与屏幕文字一致，图标按钮都有名称，没有遗留旧说法。
5. 用隔离测试数据触发一次保存失败（例如把 Application Support 副本设为只读）与一次导出失败，确认提示里是「失败 + 可执行的一步」。
6. 失败判据：任一屏仍出现解释实现取舍的句子（例如「别的影片不会被一起带走」「这是有意的」）；图标按钮没有无障碍名称；可见文案与无障碍朗读不一致；文字被替换成图标后无法判断该控件做什么。

**实现状态：** 已实现，待验证

**实现说明：** 写法约定的对照表在本条「目标行为」，一览版放在 ShotList/Support/DesignSystem.swift 文件头注释。新增 `IconValue`（图标 + 数值），用于分镜卡片、镜头面板头部、导出概览、影片条、取景页与播放页；分镜、镜头面板、历史、导出、取景页、拍摄设置、剪辑风格、错误页的按钮、标题、空状态、确认弹层与错误文案按表重写，解释性脚注与无障碍 hint 全部删除，只留一两个字的操作结果。

保留下来的较长文案及理由：剪辑风格页输入框里的整段示例（可编辑内容的样本，不是界面说明）；导出「格式」下的一行取舍（会改变选择）；相机权限前置页「用于录制分镜视频，视频仅存于本机。」（权限说明含隐私承诺）；确认弹层正文「N 段视频将被删除，无法恢复。」（波及数量与不可逆）；「新建影片」弹层「《X》将保留在影片库。」（说明不是覆盖）；存储错误保留系统给出的原因一行。

顺带调整：导出页「分享」一节在没有已拍镜头时不再显示（原空状态文字与「无可导出视频」重复）；`ExportScope` 新增 `label`（界面短标签「已拍 / 全部」），`title` 保持原样，因为导出包内的文档仍在用它；删除无引用的 `Shot.displayCaption`、`Film.subtitleText`；`Film.progressText` 改为「3/5」；系统权限弹窗说明（Info.plist 与 project.yml）各改为一句用途。README 里引用的界面用词同步更新，README 里的截图仍是旧文案，未重拍。

未改：`分镜文字内容指南.md`、`剪辑风格.md`、`分镜清单.csv`，以及剪辑风格页示例。既有测试有 3 处断言随文案更新（`ExportProgress.text`、`ExportError` 两条描述），未新增测试。

后续调整（[R-8](#r-8)）：导出页的「包内文件」「分享」「电脑」三节、「格式」下方的说明脚注与「删除影片」入口已按用户指示移除，上文对应的文案与验证观察以 R-8 为准。

**验证结果：** 环境：Xcode 27.0，iPhone 17 Pro / iOS 26.5 模拟器，`Tools/seed-simulator.py` 灌入的演示数据（两部影片、5 + 3 个镜头，均为隔离副本）。Debug 构建成功，无新增告警；既有 107 项测试全部通过。

模拟器逐屏核对（截图，临时目录，未入库）：分镜页列表、卡片长按菜单、清空片段确认、影片菜单、重命名弹窗、新建影片确认、空状态；镜头面板顶部、三个输入框、编号与文件名、片段列表、清空与删除镜头确认；历史页列表、影片下拉、空状态；导出页素材概览、剪辑风格（已设置 / 未设置）、打包与结果、包内文件、分享、电脑、删除影片确认、无可导出视频；剪辑风格页；取景页 `-chromeDemo` 1 / 3 / 4 / 5（描述折叠、拍摄中加对焦锁定、拍摄设置、无描述）；相机权限前置页、权限被拒页、无摄像头页（`-cameraDemo` 配合 `simctl privacy`）；加载失败页（把隔离副本的 `films.json` 写坏后启动）；保存失败横幅（把元数据目录设为只读后点添加）。以上均未再出现解释设计取舍的句子，统计与状态为图标 + 数字。

未验证：① VoiceOver 朗读与无障碍树——模拟器控制工具的 `inspect` 不可用，仅代码级确认每个图标按钮都有 `accessibilityLabel`、图标对读屏隐藏；② 取景页回看页（「重拍 / 使用 / 保存并再拍」）——只在录制完成后出现，模拟器没有摄像头；③ 录制失败、保存失败、导入失败与导出失败的弹层文案——没有安全的触发方式，仅代码级确认。以上待真机或 VoiceOver 走查后补记，通过后再移入已交付。

**实现 commit：** d79a034257a44f972406dd1e5d1bebd958a63f61

<a id="r-2"></a>

### R-2 · 用户打开导出页时应先看到「这次导什么、结果在哪」，说明性内容按当前影片实际会产出的东西给

**验证状态：** 待验证

**代码位置：** 待实现。现状为 ShotList/Views/ExportView.swift · `body`（第 44–105 行）的九个 Section 顺序、`contentsSection`（第 302–313 行）、`exampleAlternateFileName`（第 345–353 行）、`computerSection`（第 428–450 行）

**预期结果：** 导出页一屏之内能回答「我要导什么 / 导出结果在哪 / 现在能不能导」。参考性内容（包里有什么、怎么取到电脑）收起为可展开的一节，且列出的文件与当前影片真的会产出的文件一致。

**需求详情**

**追加说明（[R-8](#r-8)）：** 「包内文件」「单独分享」「电脑」三节与「删除影片」已按用户指示移除，本条目标行为 2、3 及验收 2、3、5 随之作废；结果紧随打包设置、过期提示紧跟结果仍有效。是否关闭本条待用户确认。

**当前行为：** 导出页把九个 Section 平铺在一列里：素材概览、剪辑风格、打包导出、结果、已过期提示、压缩包里有什么（6 行固定说明）、单独分享某个镜头（每个已拍镜头一行）、用数据线取到电脑（2 行静态说明）、维护。其中：

- 「刚刚生成的导出包」插在「打包导出」与「压缩包里有什么」之间，生成后要往回找；
- 「压缩包里有什么」与「用数据线取到电脑」加起来 8 行，是一次性读的参考资料，却常驻在主路径中间；
- `contentsSection` 无条件列出 `备用片段/…`（`exampleAlternateFileName`）与 `剪辑风格.md`，但当前影片可能没有任何拍了多条的镜头（包里不会有「备用片段」目录），也可能没写风格描述（`writeStylePrompt` 在描述为空时不产出文件，见 ExportPackageBuilder.swift:751）。列出来的文件解压后找不到。

**目标行为：**

1. 顺序改为：素材概览 → 打包导出（范围 / 格式 / 生成按钮 / 进度）→ 生成结果（生成后置顶到概览之下）→ 剪辑风格 → 单独分享 → 参考资料（可展开）→ 维护。
2. 「压缩包里有什么」与「用数据线取到电脑」合并成一个默认收起的「参考资料」小节（`DisclosureGroup`）。
3. `contentsSection` 按当前影片实际会产出的文件列：没有多条片段的镜头就不列「备用片段/」，没写风格描述就不列「剪辑风格.md」。判据要与 `ExportPackageBuilder` 的产出条件同源，不另写一套。
4. 「已过期」提示紧跟在结果一节之后，不单独占一个无标题 Section。

**范围：** 只动 `ExportView` 的版式与条件展示。

**明确不做：** 不改导出包的目录结构、命名规则与包内文件内容；不改 `ExportScope` / `ExportTranscodeOption` 的取值；文案改写归 [R-1](#r-1)，本条只管结构与条件。

**对其他功能的影响：** `contentsSection` 改成条件展示后，它与 `ExportPackageBuilder` 的产出条件必须保持同源，后续改包内结构时两处要一起改；在实现说明里记录这条耦合。

**验收方法**

1. 前置环境：模拟器安装 Debug 构建。准备三部测试影片：A 有多条片段且写过风格描述；B 每镜只有一条片段、没写风格描述；C 一个镜头都没拍。
2. 切到影片 B，打开导出页：确认「压缩包里有什么」里**没有**「备用片段/」和「剪辑风格.md」两行。
3. 切到影片 A，打开导出页：确认两行都在，且示例文件名与随后真实生成的包内文件名一致（解压比对）。
4. 在影片 A 上点「生成导出包」，生成完成后不滚动屏幕，确认结果一节出现在首屏可见范围内。
5. 确认「参考资料」默认收起，展开后包含原「压缩包里有什么」与「用数据线取到电脑」的全部内容。
6. 切到影片 C：确认生成按钮禁用且给出原因，维护一节的删除入口按现有规则可用。
7. 失败判据：列出的文件在实际导出包里不存在；生成完成后结果一节仍需要向下滚动才能看到。

**实现状态：** 未开始

**实现说明：** 待实现；完成后记录条件展示的判据放在哪里，以及与 `ExportPackageBuilder` 的耦合点。

**验证结果：** 待验证；完成后记录环境、三部测试影片的实际观察结果与解压比对结论。

**实现 commit：** 待提交；完成后填写实际实现提交的完整 SHA。

<a id="r-3"></a>

### R-3 · 用户在导出转码进行中改主意时，应当能直接停下这次导出

**验证状态：** 待验证

**代码位置：** 待实现。现状为 ShotList/Views/ExportView.swift · `buildSection`（第 189–247 行）、`buildProgress`（第 251–262 行）；ShotList/Support/ExportSession.swift · `invalidate()`（第 20–29 行）、`cancellation`（第 18 行）；ShotList/Support/ExportPackageBuilder.swift · `ExportCancellation`（第 159–165 行）、`run.checkCancellation()`（第 411、426、509 行）

**预期结果：** 打包进行中，进度条旁边有一个「停止」入口；点它之后打包在下一段素材之前停下，界面回到可以重新选范围 / 格式的状态，临时目录里不留半成品。

**需求详情**

**当前行为：** 取消能力在底层已经完整实现——`ExportCancellation` 是原子标志，`build` 在每个镜头、每条片段与打包前都调 `run.checkCancellation()`，`ExportSession.invalidate()` 会置位它。但界面上没有任何入口调用 `invalidate()`：生成按钮在 `export.isBuilding` 时被禁用，进度区只有一条 `ProgressView` 和一行文字。唯一能触发取消的是去改「导出范围」或「视频格式」Picker（它们的 `onChange` 会调 `invalidate()`），这是副作用，不是取消，用户也不会知道。

选了「H.264 · 1080p」或「HEVC · 1080p」时每段素材要重编码，素材多时整个过程是分钟级；期间用户只能等，或退出应用。

**目标行为：**

1. `export.isBuilding` 为真时，在进度区右侧显示「停止」按钮，点击调 `export.invalidate()`。
2. 停止后不弹错误（取消不是失败——`ExportPackageBuilder` 已经把 `CancellationError` 与转码失败分开，见第 478 行），进度区收起，界面回到未生成状态。
3. 停止后不要显示「分镜、素材或导出范围已变化，请重新生成导出包」——那句话说的是另一回事；为「用户主动停止」给一句独立说明，或什么都不显示。
4. 确认临时目录里不留 `ShotListExport/<uuid>/`（`buildChecked` 的 `defer` 已经负责回收工作目录，验收时核对）。

**范围：** `ExportView` 加入口，`ExportSession` 按需要区分「被用户停止」与「输入变化作废」两种失效原因。

**明确不做：** 不做断点续传；不做后台继续导出；不改转码实现。

**对其他功能的影响：** `isStale` 目前一个布尔值同时表达两种失效原因，拆分时要确认 `build()` 完成后的 `guard generation == token, isCurrent()` 分支（ExportSession.swift:54–59）仍然把「迟到的结果」正确丢弃。

**定级依据：** 导出是主路径的最后一步，但默认档位「原片」不转码、耗时短，且用户可以退出应用终止，有绕行方式，因此定为 P2 而不是 P1。

**验收方法**

1. 前置环境：模拟器安装 Debug 构建，准备一部至少 6 个镜头都已拍、素材较大的测试影片（隔离数据）。
2. 在导出页把「视频格式」选为「H.264 · 1080p」，点「生成导出包」。
3. 等进度文字走到「正在转码 2 / 6 段」左右，点「停止」。
4. 观察：进度区在下一段素材开始前收起；不弹「导出失败」；「刚刚生成的导出包」一节不出现；界面上的范围 / 格式 Picker 与生成按钮恢复可用。
5. 用「文件」App 或调试命令检查应用临时目录，确认没有残留的 `ShotListExport/<uuid>/` 工作目录。
6. 再点一次「生成导出包」，确认能正常走完并产出可分享的包。
7. 失败判据：停止后弹出错误提示；或停止后进度仍继续推进到下一段；或临时目录留下工作目录。

**实现状态：** 未开始

**实现说明：** 待实现；完成后记录「用户停止」与「输入变化作废」在 `ExportSession` 里如何区分。

**验证结果：** 待验证；完成后记录环境、停止时机、临时目录检查结果。

**实现 commit：** 待提交；完成后填写实际实现提交的完整 SHA。

<a id="r-7"></a>

### R-7 · 用户要让下一个分镜沿用上一个分镜的字幕与角标版式时，应能一步带过来再改，而不是重新敲一遍

**验证状态：** 待验证

**代码位置：** 待实现。现状为 ShotList/Views/ClipOptionsSheet.swift · `previousBadgeText`（第 89 行）与「屏幕文字」一节里的「沿用上一镜」按钮（第 184–190 行，只抄角标）；ShotList/Models/Shot.swift · `caption` / `badgeText` 及 `trimmedCaption` / `hasCaption` / `trimmedBadgeText` / `hasBadgeText`（第 208–217 行）

**预期结果：** 在镜头面板的「屏幕文字」一节，点一下「沿用上一镜」就把上一镜的字幕和角标一起带到当前镜头，两个输入框立刻出现内容，用户只改变化的部分（比如把 62.5KG 改成 65KG）。只补当前还没写的那一项，已经写好的内容不会被覆盖。

**需求详情**

**当前行为：** 上一镜写了「辅助引体 ⌄ 62.5KG * 4 * 10」这样的字幕时，下一镜要把整句重新敲一遍，只为改一两个数字。面板里已经有「沿用上一镜」，但它只抄**角标**，字幕不在内；而且只有上一镜的角标非空、与当前角标不同时才出现，字幕这一半没有任何快捷入口。绕行方式是「在下方复制一个」（会连描述一起复制，还得回头改描述）或手敲。

**目标行为：**

1. 「沿用上一镜」改为一次带过来**字幕与角标两样**（合称「屏幕文字版式」）。不带描述、片段、编号——描述是「拍什么」，每镜不同，不属于版式。
2. **只补空项，不覆盖已写内容**：当前字幕为空才补字幕，当前角标为空才补角标；已写的那一项原样保留。
3. 出现条件：当前镜头不是影片里的第一个镜头；上一镜的字幕或角标至少有一项非空；并且至少有一项「上一镜有、当前为空」。三条有一条不满足就不出现（无可补时不显示一个点了没反应的按钮）。空白与否沿用全应用口径（`hasCaption` / `hasBadgeText`，去首尾空白后判断）。
4. 「上一镜」＝当前影片里编号排在前一位的镜头，与现有 `previousBadgeText` 一致，不是「上一个拍过的」；历史页的「未拍 / 已拍」筛选不影响它。每次取仓库里的最新值，不缓存。
5. **一次性拷贝，不联动**：带过来之后两镜的文字各自独立，改当前镜头不会反过来改上一镜。这是沿用现有原则——「留空」只有一种含义，即这一镜不出，不做「留空即沿用」这类隐式规则。
6. 带过来的文字进入现有草稿（`draftCaption` / `draftBadgeText`），走既有的 400 毫秒攒批落盘与关闭面板立即落盘，不另加保存动作。
7. 可以链式使用：镜头 3 沿用镜头 2（镜头 2 已经改过数字），镜头 2 沿用镜头 1。
8. 无障碍提示同步改成新说法（例如「把上一镜的字幕和角标抄过来，只补还没写的，抄完可以再改」），按钮文案保持「沿用上一镜」。

**范围：** 镜头面板（`ClipOptionsSheet`）。分镜页与历史页共用这个面板，两处同时生效。

**推断（未经用户确认）：**

- 用户说的「版式」按现有数据模型解读为字幕 + 角标这两段文字：位置、行数等真正的版式属性是影片级的（见「剪辑风格」），本来就对所有镜头生效；镜头级只有这两段文字。如果用户要的是别的东西（例如只要「有字幕」这个结构而不带文字，或连描述一起带），需在实现前说明。
- 覆盖规则选「只补空项」是为了不丢用户已经敲的字；代价是现有「沿用上一镜」能把一个已写的角标覆盖成上一镜的，改后要先清空再点。

**明确不做：**

- 不做分镜页长按菜单里的快捷入口：卡片上不显示字幕与角标，一步操作后在列表里看不到任何结果，只剩一次震动，用户无从确认。面板里两个输入框就在眼前，结果立刻可见。
- 不做占位符 / 模板变量（不自动把数字挖空）；不做「只抄字幕」「只抄角标」的分开按钮，不想要的那一项清掉即可；不做批量套用到多个镜头。
- 不新增数据字段，不改「在下方复制一个」与「在下方插入新镜头」的行为，不改导出。

**对其他功能的影响：**

- 现有「沿用上一镜」的行为变化：由「只抄角标、可覆盖已写的角标」变为「字幕与角标一起、只补空项」。实现说明里记录这一变化。
- 导出无影响：`分镜清单.csv`、`分镜文字内容指南.md` 读的仍是每个镜头自己的 `caption` / `badgeText`。
- 草稿落盘、编号变更（`numberEdit`）与这个按钮互不相关；按钮只改两个文字草稿。

**定级依据：** 定为 P2（用户未指定级别，按规则判定，如需上调请说明）。不阻断从拍摄到导出的主路径，且有绕行方式：手敲，或用「在下方复制一个」带走文字再改描述；属于逐镜写字幕时的效率增强。

**验收方法**

1. 前置环境：模拟器安装 Debug 构建（不涉及相机），用隔离数据准备一部含 5 个镜头的影片：镜头 1 字幕「辅助引体 ⌄ 62.5KG * 4 * 10」、角标「热量缺口：1758千卡」；镜头 2、3 字幕与角标全空；镜头 4 字幕「A」、角标空；镜头 5 字幕「B」、角标「C」。
2. 打开镜头 1 的面板：「屏幕文字」一节里没有「沿用上一镜」（第一个镜头没有上一镜）。
3. 打开镜头 3：上一镜（镜头 2）字幕与角标都空，没有「沿用上一镜」。
4. 打开镜头 2，点「沿用上一镜」：字幕、角标两个输入框同时出现镜头 1 的内容，按钮随即消失。关闭面板后再打开，内容仍在；镜头 1 的内容没变。
5. 把镜头 2 字幕里的 62.5 改成 65，关闭再打开：改动保留，镜头 1 不受影响（不联动）。
6. 打开镜头 3：现在出现「沿用上一镜」，点后带入镜头 2 改过的字幕与角标（链式）。
7. 打开镜头 4（字幕「A」已写、角标空，上一镜有内容）：点「沿用上一镜」，角标补上，字幕仍是「A」。
8. 打开镜头 5（两项都已写）：没有「沿用上一镜」，已写内容原样。
9. 清空镜头 2 的角标：「沿用上一镜」重新出现；点后只补角标，字幕不变。
10. 在「历史」页切到「未拍」筛选，打开镜头 3 的面板：按钮的有无与带入内容，仍以影片内的前一镜（镜头 2）为准，不是筛选列表里的前一项。
11. 导出「原片」并解压 `分镜清单.csv`：镜头 2、3 的屏幕字幕与角标列与界面一致；镜头 1 未被改动。
12. 打开 VoiceOver 聚焦「沿用上一镜」：朗读提示是新说法，与实际行为一致（「字幕和角标、只补没写的」）。
13. 失败判据：改当前镜头反过来改了上一镜；已写的字幕或角标被覆盖；第一个镜头或上一镜两项都空时仍出现按钮；描述被一并带过来；关闭面板后带入的内容丢失。

**实现状态：** 未开始

**实现说明：** 待实现；完成后记录「只补空项」与出现条件的判据放在哪里，以及对原「沿用上一镜」行为的改动。

**验证结果：** 待验证；完成后记录环境、逐步观察结果与验证限制。

**实现 commit：** 待提交；完成后填写实际实现提交的完整 SHA。

<a id="r-8"></a>

### R-8 · 用户在导出页应只看到导出所需的设置，「打包导出」是固定在底部的强调色独立按钮

**验证状态：** 模拟器实测

**代码位置：** ShotList/Views/ExportView.swift · `body`（`safeAreaBar` 挂底部按钮）、`exportButton`、`buildSection`、`maintenanceSection`；已删除 `contentsSection`、`clipsSection`、`computerSection`、「删除影片」入口与 ShotList/Support/ExportPackageBuilder.swift 的 `ExportTranscodeOption.detail`。

**预期结果：** 导出页只保留素材概览、剪辑风格、导出设置（片名、范围、格式、进度与「无可导出视频」原因）、导出包（生成后出现，含「分享」）与过期提示。「包内文件」「分享」（逐镜头）「电脑」三节、「格式」下方的说明脚注、「删除影片」入口不再出现。「打包导出」是强调色实心按钮，独立于列表，固定在页面底部、标签栏之上，不随列表滚动。

**需求详情**

**当前行为：** 导出页把「包内文件」（6 行文件名）、「分享」（每个已拍镜头一行）、「电脑」（两条路径）平铺在设置与结果之后，页面很长；「格式」下方有一行取舍说明；末尾是「删除影片」。生成按钮是设置区里的一行普通文字按钮，不突出。

**目标行为：**

1. 移除「包内文件」「分享」「电脑」三个 Section，移除「格式」下方的说明脚注（`ExportTranscodeOption.detail` 随之删除）、移除「删除影片」按钮及其确认弹窗。
2. 「打包导出」（四个字）从设置区拿出来，做成独立按钮固定在页面底部：强调色实心、通栏（左右留边距）、带 `shippingbox` 图标。无已拍镜头时置灰不可点，原因文字「无可导出视频」仍留在设置区；打包进行中置灰，图标换成转圈。
3. 设置区的进度行、生成后的「导出包」一节（含「分享」）、「需重新打包」提示位置与行为不变。

**范围：** 只动 `ExportView` 的版式与入口。

**明确不做：** 不改导出包内容与命名；不改打包流程与取消逻辑（停止按钮归 [R-3](#r-3)）；不改 `ShotStore.deleteCurrentFilm`；不动「清理未使用文件」——它只在磁盘上确有无归属文件时才出现，保留在列表末尾。

**对其他功能的影响：**

- 「删除影片」此前是全应用唯一的删除整部影片入口，移除后应用内没有删除影片的地方；`ShotStore.deleteCurrentFilm` 保留但界面无调用方。新入口放哪里由用户另行决定。
- 逐镜头分享入口仍可在分镜页卡片长按菜单的「分享」、镜头面板每条片段的「更多 › 分享」中使用；导出页的「分享」只保留对整个导出包的那一个。
- 「用数据线取到电脑」的路径提示随「电脑」一节移除，文件共享本身（`UIFileSharingEnabled`）不变。
- 与 [R-2](#r-2) 的关系：R-2 讨论的「包内文件」「电脑」两节已在本条移除，其目标行为 2、3 及验收 2、3、5 随之作废；结果紧随打包设置、过期提示紧跟结果仍有效。R-2 是否关闭待用户确认。

**定级依据：** P2。不改变导出主路径的能力，属于导出页的版式与入口调整；删除影片入口的去向是后续需要另行决定的问题。

**验收方法**

1. 前置环境：模拟器安装 Debug 构建，`Tools/seed-simulator.py` 灌入演示数据（《夏日vlog》含 3 个已拍镜头）。
2. 打开「导出」页：没有「包内文件」「分享」「电脑」三节，「格式」下没有说明文字，页面里没有「删除影片」。
3. 页面底部、标签栏之上有强调色实心的「打包导出」按钮；上下滚动列表，按钮位置不动，列表最后一行不被它遮挡。
4. 点「打包导出」：按钮置灰并出现转圈，完成后「导出包」一节出现并可「分享」；按钮恢复可点。
5. 改「范围」或「格式」：出现「需重新打包」，按钮仍可点。
6. 切到没有已拍镜头的影片：按钮置灰，设置区里仍有「无可导出视频」。
7. 分镜页长按已拍镜头卡片：仍有「分享」；镜头面板的片段「更多」里仍有「分享」。
8. 失败判据：按钮随列表滚走；按钮被标签栏遮住或遮住最后一行；无已拍镜头时按钮可点；被移除的任一项仍出现；打包完成后没有「导出包」。

**实现状态：** 已实现，待合并

**实现说明：** 底部按钮用 iOS 26 的 `safeAreaBar(edge: .bottom)` 挂在列表上：按钮固定不动，列表末行自动让出它的高度，滚到它下面的内容由系统柔化。按钮为 `.borderedProminent` + `.controlSize(.large)`，文案「打包导出」（四个字，沿用原分组标题的说法；如想用别的四个字只需改一处），图标 `shippingbox`；打包中图标换成小号转圈，高度不变。原设置区里的生成按钮删除，进度行、「无可导出视频」原因与「导出包」一节不动。

删除的代码：三个 Section 及其示例文件名辅助属性、`ExportTranscodeOption.detail`、删除影片的确认弹窗与 `showClearConfirm` / `deleteMessage`。`ShotStore.deleteCurrentFilm` 保留（既有测试在用），但界面上已无调用方。「清理未使用文件」保留，仅在有无归属文件时才出现，不再与「删除影片」同属一个分组。

遗留：应用内不再有删除整部影片的入口，影片库只增不减；该入口放到哪里需要用户另行决定。README 里「单独分享」的说法同步改为分镜页长按卡片的「分享」。本分支基于 `feature-R-1`，随其一并合并。

**验证结果：** 环境：Xcode 27.0，iPhone 17 Pro / iOS 26.5 模拟器，《夏日vlog》演示数据。Debug 构建成功，无新增告警；既有 107 项测试全部通过。

逐步观察（按验收方法）：步骤 2——导出页只剩素材、剪辑风格、打包导出设置与导出包，没有「包内文件」「分享」「电脑」，「格式」下没有说明，没有「删除影片」；步骤 3——强调色通栏按钮位于标签栏之上，列表上滑时按钮不动，标签栏收起后按钮仍在，滚到底时「导出包」一节的「分享」完整可见、没有被按钮遮挡；步骤 4——点「打包导出」，「格式」选 HEVC 时截到按钮置灰加转圈、进度行显示「转码 1 / 5」，完成后出现「导出包」一节；步骤 5——打包中改「范围」，出现「需重新打包」，按钮可点；步骤 6——切到新建的空白影片，按钮置灰，「无可导出视频」仍在。

未验证：步骤 7（分镜页长按「分享」与镜头面板片段「更多 › 分享」）未在本次重走，两处代码未改动；打包完成后在系统分享面板里的实际分享未执行。

**实现 commit：** 0b3572a069711be7ab444288c0aaee032795166b
