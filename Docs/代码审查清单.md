# 分镜助手 · 代码审查清单

审查时间：2026-09-15（第一轮）+ 2026-09-15 01:00 前后（第二轮，见第七节）
审查范围：第一轮 = 全部 22 个 Swift 源文件 + 当时工作区里未提交的那份改动；
第二轮 = 全部 22 个 Swift 源文件 + `project.yml` / `Info.plist` / `README.md` / `Tools/`
构造状态：`BUILD SUCCEEDED`，零 error 零编译器告警

分级口径：

- **P0** — 绝对不能进仓库，或会直接影响用户数据 / 设备空间
- **P1** — 真 bug，能稳定复现，用户看得见
- **P2** — 条件触发或隐患，暂不致命，但要排期

复选框含义：`[x]` 已修复并验收，`[ ]` 待处理。

标「第二轮」的条目是 2026-09-15 凌晨那次复查新发现的，**均未修复**。

---

## 一、P0 — 必须先处理

- [ ] **P0-2 · `load()` 把「读不到」当成「不存在」，一次启动就能把全部片段记录永久删掉（第二轮，已实测复现）**
  - 位置：`ShotList/Models/ShotStore.swift:327-348`（`load()`），关键在 337-343 的
    `fileManager.fileExists(atPath:)` 判定与 347 行的 `if didRepair { persist() }`
  - 现象：`fileExists` 对「文件确实不在」和「目录暂时读不到（权限 / 数据保护未解锁 /
    备份恢复中 / 目录创建失败）」返回的都是同一个 `false`。`load()` 不加区分地把这些片段
    从内存里剔掉，并且**立刻把结论写进 `shots.json`**。实测：把片段目录 `chmod 000`
    后启动一次，JSON 里的 5 条片段记录变成 0 条，而磁盘上 10 个视频文件一个不少——
    记录没了，文件还在，从此在应用里看不见、不计入占用空间、也不会被导出带走。
  - 为什么会这样：这条「校正立刻落盘」是上一轮 P2-4 的修法引入的。修 P2-4 之前
    `load()` 只改内存、不落盘，同样的误判要等下一次数据变更才写进 JSON；改成立刻落盘后，
    误判变成了「启动一次即生效、且不可逆」。
  - 修法：
    1. 用 `try fileManager.contentsOfDirectory(at:)` 是否抛错来区分「目录不可访问」与
       「目录可访问但文件不在」，**目录不可访问时直接跳过整段校正**（连编号都不要动）；
    2. 单个文件的存在性判定改用 `URL.resourceValues(forKeys: [.isRegularFileKey])` 的
       抛错语义，`EACCES` 一律按「未知」处理，不当作「不存在」；
    3. 剔除记录时留一份可恢复的副本（例如写 `shots.repaired.json`，或在日志/界面上告知），
       避免静默永久丢失。
  - 验收：重复上面的 `chmod 000` 实测——启动后 JSON 仍是 5 条；恢复权限后再启动，5 条完好。

- [x] **P0-1 · 临时验收钩子没删，且每次冷启动跑一次全量导出**
  - 位置：`ShotList/App/ShotListApp.swift:13-27`
  - 现象：`.task` 里每次启动都执行一次 `.everything` 完整导出（复制全部视频 → 打 zip）。代码注释自己写着「用完删掉」。一旦 `git add -A` 就会进仓库，装到真机上等于每次开 App 都白搬一遍全部素材。
  - 修法：删除该 `.task`，改为启动时清理上一次会话留在临时目录的导出包。
  - 验收：文件内不再出现 `TEMP_EXPORT_*` 字样；启动后临时目录不含上一轮的包。

---

## 二、P1 — 真 bug，可复现

- [x] **P1-1 · CSV 没有转义换行，一条记录被拆成两行**
  - 位置：`ShotList/Support/ExportPackageBuilder.swift:320-323`（`csvField`）
  - 现象：分镜描述用的是 `TextField(axis: .vertical)`，可以输入多行。`csvField` 只对 `,` 和 `"` 加引号，含换行的描述会直接把一行拆成两行，后面整列错位（实测 2 条数据输出 4 行），导出的 `分镜清单.csv` 在 Excel 里对不上号。
  - 修法：`\n` / `\r` 也走引号分支（标准 CSV 允许引号内含换行）。
  - 验收：多行描述的一条记录在 CSV 里仍只占一行，Excel / Numbers 打开列对齐。

- [x] **P1-2 · 「待拍」有两套互相矛盾的口径**
  - 位置：`ShotList/Models/ShotStore.swift:61`（`todayPendingCount`）vs `ShotList/Views/TodayView.swift:53,96,101`
  - 现象：标签栏徽标用「今天没拍」（`todayPendingCount`），今日页的「还没拍」统计块与「下一个 / 今天的分镜都拍完了」用「从没拍过」（`pendingShots`）。昨天把 3 个镜头全拍完、今天打开 App：徽标显示 `3`、进度环 `0%`、正文写着「今天已经拍了 0 个镜头」，旁边却打出绿色的「今天的分镜都拍完了」。
  - 修法：今日页的进度环、徽标、「下一个」提示统一取「今天还没拍」这一个口径，并把口径收进 `ShotStore` 作为唯一来源；统计块「还没拍」保留「从没拍过」的含义（它与「今日已拍」「往日已拍」构成三分，不能混）。
  - 验收：全拍完昨天的数据下，徽标、进度环、提示三者自洽，不再出现 0% 却「都拍完了」。

- [x] **P1-3 · `cleanUp()` 是死代码，临时目录只增不减**
  - 位置：`ShotList/Support/ExportPackageBuilder.swift:230-233`
  - 现象：全项目零调用。每个导出包体积≈全部视频，留在 `tmp/ShotListExport/` 里从不回收；一次会话里导出几次就堆几份全量体积。
  - 修法：启动时调一次 `cleanUp()`；另外每次生成新包后只保留最新一份 zip。
  - 验收：一次启动内连续导出两次，临时目录里只留 1 个 zip；重启后上一轮的包被清掉。

- [x] **P1-4 · 主素材有两个真相源，时间戳并列时互相打架**
  - 位置：`ShotList/Support/ExportPackageBuilder.swift:155,164,257,473`
  - 现象：写进根目录的那条由 `clip.id == latest.id` 决定；而清单 / 指南里的「主素材」由 `takeIndex != takeTotal` 反推。`max(by:)` 在时间戳完全相同时返回**第一个**最大值（已实测），于是两条路径立刻不一致——指南会把真正在根目录的那个文件列进「备用片段」，把备用片段写成「主素材文件」。
  - 修法：`ManifestRow` 直接存 `isMain`，由 `build()` 在落盘时传入，其余地方一律引用它；同时把「并列时取哪一条」定死为数组里靠后的一条（片段按拍摄先后追加，靠后的才是更晚拍的）。
  - 验收：时间戳完全并列的 3 条片段，指南里的「主素材文件」与实际在根目录的文件一致。

- [ ] **P1-5 · 「今日已拍 / 往日已拍」两个统计块的数字与点击后的筛选结果对不上（第二轮，已实测截图）**
  - 位置：`ShotList/Views/TodayView.swift:118-135`（`statRow` 里两个已拍块都传 `filterTarget: .recorded`）
    ↔ `ShotList/Views/TodayView.swift:59-65`（`filteredShots` 的 `.recorded` = `store.shots.filter(\.hasClip)`）
  - 现象：两个块的数字各自只数**一个子集**（今日已拍 = `todayRecorded.count`，往日已拍 =
    `earlierRecorded.count`），但点下去都切到「已拍」这一个**并集**筛选。实测数据
    （镜头 01、02 今天拍过，03 是昨天拍的）：点「今日已拍 2」后列表列出 3 张卡片。
    与上一轮修掉的 P1-2 是同一族问题——数字与它指向的列表不是同一个口径。
  - 证据：`Docs/screenshots/审查-01-统计块数字与筛选结果对不上.png`（块上写 2，列表 3 条）
  - 修法：把筛选值拆到与统计块一一对应（`全部 / 今日 / 往日 / 未拍` 四段，或点块时切到对应的
    精确筛选），保证「点哪个块，列表就是那个块数的那些镜头」。
  - 验收：四段筛选各自的条数与同名的统计块数字相等。

- [ ] **P1-6 · 录制中直接关掉相机：临时片段没人回收，音频会话可能停在播放态（第二轮，代码级）**
  - 位置：`ShotList/Views/CameraCaptureView.swift:111-147`（`topBar` 的关闭按钮在录制中没有被禁用）、
    `:57-62`（`onDisappear` 只 `recorder.stop()`，不处理 `reviewURL`）、`:465-477`（`enterReview`）、
    `ShotList/Camera/CameraRecorder.swift:192-218`（`completion` 一直留着，直到 `didFinishRecording` 才清）
  - 现象：录制中那个「✕」是可点的，点下去 → `dismiss()` → `onDisappear` 里 `recorder.stop()`
    让 AVFoundation 收尾 → 录制回调把 `completion` 交回主线程 → 已经被关掉的页面又执行一次
    `enterReview(url)`：把音频会话切成 `.playback` 并 `setActive(true)`。
    两个后果：
    1. 这段刚录好的 `tmp/shot-<uuid>.mov` **不会有任何人删**（只有「重拍」和「使用这条」两条
       出口会处理它），一直躺在临时目录里；
    2. `stop()` 在队列侧还会 `deactivateAudioSession()`，与上面那次 `setActive(true)`
       谁先谁后不确定——若 `enterReview` 后到，音频会话就停在 `.playback + active` 上，
       而唯一会归还焦点的 `onDisappear` 早就跑完了。
  - 影响面：`cleanUp()` 只清 `ShotListExport`（`ExportPackageBuilder.swift:238-241`），
    `tmp/shot-*.mov` 与相册导入失败时留下的 `tmp/import-*` 从来没有回收路径，
    只能靠系统清临时目录。
  - 修法：录制中禁用关闭按钮（或先 `stopRecording()` 再关）；`stop()` 里把 `completion`
    清空并删除它对应的临时文件；`cleanUp()` 扩成「清 `ShotListExport` + 清 `tmp/shot-*`、`tmp/import-*`」；
    `enterReview` 前加一道「页面还在不在」的判断。
  - 验收：真机录制中直接关相机 → 临时目录不残留 `shot-*.mov`，控制中心里的音频焦点正常归还。
    模拟器无摄像头，只做代码级核对。

---

## 三、P2 — 条件触发 / 隐患

- [x] **P2-1 · 相册导入一律改名为 `.mov`**
  - 位置：`ShotList/Support/MediaImport.swift:16`、`ShotList/Models/ShotStore.swift:238`（`addClip`）、`ShotList/Support/ExportPackageBuilder.swift:308`（`mainFileName`）
  - 问题：任何相册视频都落成 `import-<uuid>.mov`，接着 `addClip` 又把片段命名成 `.mov`，导出时 `mainFileName` 也写死 `.mov`——三处都在丢扩展名，mp4 被改名的同时容器没变，导出后交给剪映存在扩展名与容器不符的风险。
  - 修法：三处一起改成跟随源文件扩展名：导入保留原扩展名；片段命名与编号改名沿用原扩展名（`token` 与扩展名分开存）；导出命名取片段扩展名；编辑页的文件名预览读该镜头主素材的扩展名。
  - 验收：mp4 片段在库内、改名后、导出包里都仍是 `.mp4`。

- [x] **P2-2 · 从「设置」授权后回到 App，相机页仍停在旧状态**
  - 位置：`ShotList/Views/CameraCaptureView.swift:22`（`@State` 的 `authorization`）
  - 问题：`authorization` 只在 `.task` 与 `requestAccess()` 里更新；用户去系统设置里打开相机权限再切回来，界面还显示「权限已关闭」，必须关掉相机重进。
  - 修法：`scenePhase` 变为 `.active` 时调 `refreshAuthorization()` 重读权限，按新状态切到取景或对应的说明页。

- [x] **P2-3 · 播完片段不归还音频焦点**
  - 位置：`ShotList/Views/ClipPlayerScreen.swift:73-76`
  - 问题：切成 `.playback` 且 `setActive(true)` 后从不 `setActive(false)`，退出后别的 App 的音乐不恢复。
  - 修法：`onDisappear` 里 `setActive(false, options: .notifyOthersOnDeactivation)`。

- [x] **P2-4 · 读取时校正了编号却不落盘，极端情况下片段会「凭空消失」**
  - 位置：`ShotList/Models/ShotStore.swift:285`（`load()`）
  - 问题：`load()` 会过滤已消失的文件、并调 `normalize()` 改名磁盘文件，但从不 `persist()`。磁盘文件名与 JSON 记录因此长期不一致，下次启动按「文件不存在」过滤就会把片段从界面上抹掉（文件其实还在磁盘上）。
  - 修法：`normalize()` 改为返回「是否改动过」，`load()` 里只要过滤掉了片段或重排过编号就立刻 `persist()`。

- [x] **P2-5 · 解析视频时长留在主线程**
  - 位置：`ShotList/Support/Haptics.swift:30`（`VideoMetadata.duration`）
  - 问题：默认主协程隔离下，裸的 `nonisolated async` 会留在调用方所在的主协程上，`AVURLAsset` 的构造与解析都压在主线程。同文件 `decodeThumbnail` 已经标了 `@concurrent`，这里漏了。
  - 修法：补 `@concurrent`。

- [x] **P2-6 · 视图 body 里做同步文件 I/O，且同一份计算做了两遍**
  - 位置：`ShotList/Models/ShotStore.swift`（`totalClipBytes`、`clipURL(for:)`、`clipURLs(for:)`）；`ShotList/Views/ExportView.swift:65,76`
  - 问题：`totalClipBytes` 每次读都跨 N 个片段发系统调用，`clipURL(for:)` 也要 `fileExists`；导出页在无障碍值里把同样的计算又算了一遍，等于每次重绘 2×N 次系统调用。
  - 修法：新增 `refreshStorageStats()`，一次目录枚举（预取 `fileSize`）同时刷新「占用空间」与「磁盘上存在哪些片段」；在启动、数据变更、从后台回到前台时各调一次；`clipURL(for:)` 改为查缓存。视图读的都是缓存值。
  - 备注：`totalClipBytes` 因此从计算属性变成 `@Published` 存储属性，`ExportView` 的无障碍值与正文读同一份。

- [x] **P2-7 · 相机录制在错误的队列上碰会话状态**
  - 位置：`ShotList/Camera/CameraRecorder.swift:181-200`（`startRecording` / `stopRecording`）
  - 问题：注释写明「只在 `sessionQueue` 上访问」的 `movieOutput`，实际被主线程读写，与 `sessionQueue` 上的 `switchCamera` / `updateRotation` 并发改同一个连接。
  - 修法：`movieOutput` 的读写整体挪进 `sessionQueue`（含 `stop()` 与中断通知里的收尾）；`isRecording` 仍在主线程同步置位，队列侧确认失败再回滚，保住「连点两次不会启动两段录制」。

- [x] **P2-8 · 缩略图陈旧回调会覆盖新图**
  - 位置：`ShotList/Views/ClipThumbnailView.swift:88-101`、`ShotList/Support/Haptics.swift:58`（`thumbnail(for:completion:)`）
  - 问题：回调无条件写 `image`，不校验请求时的 url 是否仍是当前 url；加载器也没有 in-flight 去重，同一张图可能并发解码多次。
  - 修法：加载器改成 `async` 并在 `await` 之后由调用方判断 `Task.isCancelled`（`.task(id: url)` 在 url 变化时本就会取消上一个任务，迟到的结果直接丢掉）；同时加「同一个 url 只跑一次」的在途任务表。
  - 顺带：`invalidate(for:)` 原本是死代码，现在接到片段删除与「清空重来」上——文件名带时间戳，删掉后同一路径可能被新片段复用，缓存里的旧图不清就会显示上一个视频的首帧。

- [ ] **P2-9 · 片段被外部删掉后，当次会话里界面「半死不活」，要等下次启动才自愈（第二轮，已实测截图）**
  - 位置：`ShotList/Models/ShotStore.swift:300-319`（`refreshStorageStats()` 只刷新缓存，
    不动 `shots`）↔ `:327-348`（`load()` 只在启动时跑）↔ `ShotList/Views/ShotCardView.swift:118-144`
    （`metaRow` 仍按 `shot.hasClip` 判断，而缩略图按 `clipURL` 判断）
  - 现象：实测把镜头 03 的片段从容器里删掉 → 回前台（`refreshStorageStats()` 确实跑了）后，
    卡片变成空心的虚线加号占位（看起来是「未拍」），却仍挂着「昨天 00:49」的拍摄时间、
    也不显示「点击拍摄或导入视频」；标签栏徽标、今日页的进度环与统计块依旧把它算成已拍。
    重启后 `load()` 才把这条记录剔掉，界面恢复自洽。
  - 证据：`Docs/screenshots/审查-03-外部删除后当次会话的混合态.png`（会话内）、
    `Docs/screenshots/审查-04-重启后自愈.png`（重启后）
  - 修法：让「磁盘上有没有这个文件」成为界面口径的一部分——`refreshStorageStats()` 里
    把磁盘上不存在的片段从 `shots` 里摘掉（同一套校正逻辑抽成函数，`load()` 与它共用），
    或者在卡片上明确表达「视频文件已不在」。
  - 验收：删掉外部文件 → 回前台即恢复自洽，不需要重启。

- [ ] **P2-10 · 导出包的「N 个待拍」与包内说明的「仍待补拍的镜头」会给出不同的结论（第二轮，代码级）**
  - 位置：`ShotList/Support/ExportPackageBuilder.swift:127`（`pending = included.filter { !$0.hasClip }`
    按 JSON 算）、`:228`（`pendingCount: pending.count`，界面摘要用它）、
    `:155-158`（清单行按**磁盘可用性**判待拍）、`:389` + `:432-437`（`导出说明.txt` 用 `exportedPath == nil` 列待补拍）
  - 现象：镜头在 JSON 里有片段、但文件已经不在磁盘上时（= 上面 P2-9 那个状态），
    应用里导出摘要不会出现「待拍」，`导出说明.txt` 却把该镜头列进「仍待补拍的镜头」。
    解析代码：`pendingCount` 与 `pendingRows` 来自两个不同的判据。
  - 修法：统一按磁盘可用性算待拍（`build()` 里顺手收集 `available.isEmpty` 的镜头，
    用它喂 `pendingCount`），或者反过来让清单行也用 `hasClip`。二者只能留一个。
  - 验收：构造「有记录、无文件」的镜头导出一次，摘要里的待拍数与 `导出说明.txt` 的待补拍条数相等。

- [ ] **P2-11 · 孤儿片段文件既不计入「占用空间」，也没有任何回收路径（第二轮，已实测截图）**
  - 位置：`ShotList/Models/ShotStore.swift:316-318`（只累加 `shots` 里引用到的片段）
  - 现象：`Documents` 目录对「文件」App 开放（`UIFileSharingEnabled`），用户完全可以把视频
    拖进来；这些文件不进 JSON，于是（1）永远不会出现在界面上，（2）不计入「占用空间」，
    （3）不会被导出带走，（4）也没有任何清理入口。实测：往目录里放一个 3.05 MB 的 mp4，
    回前台后界面仍是「占用空间 14.3 MB」，而 `du` 已经是 18.2 MiB。
  - 证据：`Docs/screenshots/审查-02-占用空间不含孤儿文件.png`
  - 修法：`refreshStorageStats()` 里顺便识别「目录里有、JSON 里没有」的文件，
    在导出页给一个「清理未使用的文件 N 个 / X MB」，或直接把它们计入占用空间并说明。
  - 验收：放入孤儿文件后，占用空间与实际 `du` 一致，或界面上明确提示存在未使用文件。

- [ ] **P2-12 · `CameraRecorder` 里触碰主线程资源的方法缺 `@MainActor`，与类文档的隔离约定不一致（第二轮）**
  - 位置：`ShotList/Camera/CameraRecorder.swift:144`（`stop()`）、`:165`（`attachPreviewLayer`）、
    `:172`（`detachPreviewLayer`）、`:345`（`updateRotation`）、`:360`（`installRotation`）、
    `:420`（`stopTimer`）——全是 `nonisolated`；对照 `:404` 的 `startTimer()` 是 `@MainActor`，
    类注释（`:30-35`）又写着「主线程资源（`previewLayer`、旋转协调器、计时器）只在主线程访问」
  - 问题：这条约束现在靠「调用方碰巧都在主线程」维持，编译器不检查。`stop()` 会读写
    `timer` / `recordingStart`，`updateRotation()` 开头就清 `rotationCoordinator` 与两个
    KVO 观察——一旦将来有人从 `sessionQueue` 侧或后台任务里调一下，就是静默的数据竞争。
    同类不一致还有：`startTimer()` 是 `@MainActor` 而 `stopTimer()` 不是。
  - 修法：给这几个方法补 `@MainActor`（`stop()` 内部已经自己跳队列，标注不影响它的语义），
    或把主线程资源收进一个 `@MainActor` 的小类型里，由签名而不是注释来保证。
  - 验收：给这几个方法补标注后仍能 `BUILD SUCCEEDED`（顺带能发现现在有哪些调用点其实不在主线程）。

- [ ] **P2-13 · 三处死代码 / 无用逻辑（第二轮，全仓 grep 确认零调用）**
  - `ShotList/Models/ShotStore.swift:226` `deleteShots(withIDs:)` —— 全仓唯一出现处是定义本身。
    多选编辑时代的遗留（现在工具栏那个编辑按钮已经去掉了）。
  - `ShotList/Models/Shot.swift:299` `ShotStatus.isRecorded` —— 零调用。注意别和
    `NumberBadge.isRecorded`（按钮参数）、`ClipThumbnailView.isRecorded`（`url != nil`）弄混，
    那两个在用，只有状态枚举上的这个没人读。
  - `ShotList/Views/ClipThumbnailView.swift:15` `showsPlayGlyph` —— 三个调用点全部用默认值 `true`，
    这个开关从来没被关过；要么接上（例如列表里的紧凑缩略图关掉播放标识），要么删掉。
  - 修法：删掉前两处；第三处决定接上还是删掉。

- [ ] **P2-14 · README 与界面示例文案落后于实现（第二轮）**
  - `README.md:70-80` 的导出包结构图只列到 `分镜清单.csv` / `导出说明.txt`，
    少了两处已经存在的东西：`分镜文字内容指南.md`、以及根目录主素材与「备用片段」的扩展名
    现在跟随源文件（区里还写死 `.mov`）。
  - `README.md:42`「卡片下方补一行『共 3 段 · 总时长 18 秒』」——现在的实现是先给不用这句话，
    条数收在缩略图角标上，卡片只剩「总时长 18 秒」。
  - `ShotList/Views/ExportView.swift:156-166`「压缩包里有什么」两个示例写死 `.mov`，
    而相册导入的 mp4 导出后就是 `.mp4`。
  - `README.md:133-171` 的目录结构没有提 `DesignSystem.swift` 里的时间线导轨（`TimelineRail`）。
  - 修法：把这几处对齐实现。示例文案可改用「扩展名跟随源文件」的说法，避免下次又过期。

- [ ] **P2-15 · 演示数据脚本不清理旧数据，且前置条件没写（第二轮，已实测）**
  - 位置：`Tools/seed-simulator.py:29-32`（`makedirs(..., exist_ok=True)`，不清空目录）、
    `:60`（`shutil.copyfile(f"/tmp/slseed/v{source}.mov", ...)`）
  - 现象：连跑两次后目录里有 10 个视频文件，只有 5 个被 `shots.json` 引用——剩下 5 个就是
    P2-11 说的孤儿，脚本自己打印的「磁盘文件: 10 个」正好自证。
    另外脚本硬依赖 `/tmp/slseed/v1..v5.mov`，换台机器没有这些素材就直接 `FileNotFoundError`，
    而 README 里只写了「往模拟器里灌演示数据」，没写要先准备素材。
  - 修法：跑之前清空「分镜视频」目录（或只删自己上次生成的文件）；素材缺失时给一句
    明确的提示（而不是抛栈），或在脚本头部注释/README 里写清素材从哪来。

---

## 四、实测确认**不是** bug 的

- 日期格式化：同年输出 `9月14日`、跨年输出 `2025年9月14日`，`.month(.wide)` 没有把年份带出来。
- 文字指南的按编号并组正确；编号连续性正确。
- `update(_:)` 改编号后的 remove + insert 两个方向都对。
- `SLTimecode.text(for:)` 对 `nil` / 0 / 3599 等边界不崩溃。
- （第二轮）`todayPendingShots` 这一套口径在真实数据上自洽：镜头 03 是昨天拍的、04/05 没拍过，
  徽标与「今天还没拍」都是 3，进度环 `2/5 = 40%`，「下一个：镜头 03」——三者对得上，
  上一轮 P1-2 的修法成立（同一张截图里这四项都在：
  `Docs/screenshots/审查-01-统计块数字与筛选结果对不上.png`）。
- （第二轮）`filteredShots` 的「未拍」筛选用的是 `neverShot`，与「还没拍」统计块同源，
  数字与列表一致；P1-5 只影响「今日已拍 / 往日已拍」这两个块。
- （第二轮）时间线导轨首行不画上段、末行不画下段，5 行卡片接成一条不断不错位的竖线
  （截图中导轨与编号节点对齐正确）。

---

## 五、验收记录

验收时间：2026-09-15 00:36（iPhone 17 模拟器 · iOS 26.5）

构建：从零新建派生目录 `xcodebuild ... -derivedDataPath /tmp/finalbuild` →
`** BUILD SUCCEEDED **`，**零 error 零编译器告警**；`grep -rn "TEMP_" ShotList/` 无输出。

验收手法：验收用的临时导出钩子只在核对期间存在，核对完已删除；
演示数据用构造脚本灌入，专门造出下面这些边界。

| 项 | 验收手法 | 结果 |
|---|---|---|
| P0-1 | 全仓搜 `TEMP_`；启动后查容器 `tmp/` | 无残留；启动只清临时目录，不再自动导出 |
| P1-1 | 描述含换行 + 逗号 + 双引号的镜头，导出后按 CSV 规则解析 | 每条记录都是 7 列；多行描述完整留在引号内，没有被拆行 |
| P1-2 | 造「3 个镜头昨天全拍完、今天一条没拍」的数据截图今日页 | 进度环 0%、`0/3`、徽标 3，提示「下一个：镜头 01」；不再出现 0% 却「今天的分镜都拍完了」。混合数据下今日已拍 1 / 往日已拍 2 / 还没拍 1，与进度环自洽 |
| P1-3 | 一次启动内连导两次 + 重启一次，看容器 `tmp/ShotListExport/` | 两次导出后只剩 1 个 zip；重启后上一轮的包名消失 |
| P1-4 | 3 条拍摄时间**完全相同**的片段导出后对比磁盘与指南 | 根目录的 `01_….mov` 与指南「主素材文件」一致；两条备用片段落在 `备用片段/01-1`、`01-2`，不再出现「主素材被列进备用片段」 |

顺带确认的行为变化：

- 时间戳并列时主素材改取**靠后**一条，缩略图上的时长从 take 1 的 `0:06` 变为 take 3 的
  `0:05`，与导出包里的主素材保持一致。
- 分镜页、今日页、空描述卡片截图复核，没有引入界面回归。

---

## 六、P2 验收记录

验收时间：2026-09-15 00:45-00:50（iPhone 17 模拟器 · iOS 26.5）

构建：`-derivedDataPath /tmp/p2final` 从零构建 → `** BUILD SUCCEEDED **`，
**零 error 零编译器告警**，22 个目标文件全部重新编译（用 `.o` 数量核对过，
避免增量缓存把问题文件挡掉）。

验收数据：`/tmp/p2-seed.py` 造出三种边界——镜头 1 有 3 条片段（第 2 条刻意用 `.mp4`，
三条拍摄时间戳完全相同）、编号写成 `1,2,3,5,9` 的跳号、另有 mp4 片段挂在会被改号的镜头上。

| 项 | 验收手法 | 结果 |
|---|---|---|
| P2-1 | 库内 / 改名后 / 导出包三处核对 mp4 片段 | 种子文件 `镜头05_….mp4` → 启动校正改号后为 `镜头04_….mp4`；导出包根目录 `04_从背后跟拍走进地铁站.mp4`、备用片段 `01-2_….mp4`——容器与扩展名全程一致 |
| P2-2 | `simctl privacy revoke camera` → 打开相机页截图；再 `grant` + 切后台回前台截图 | 撤销时显示「摄像头权限已关闭」；授权并回前台后**自动**切到「无法使用相机」（模拟器无摄像头），不需要关掉相机重进 |
| P2-3 | 代码级（模拟器无法验证真实音频焦点） | `onDisappear` 已加 `setActive(false, .notifyOthersOnDeactivation)`；真实焦点归还需真机确认 |
| P2-4 | 灌入编号 `1,2,3,5,9` 的 JSON 后启动，读回磁盘 JSON | 启动后 `shots.json` 已被改写为 `1,2,3,4,5`，且片段文件名同步为 `镜头04_….mp4`——校正结果落盘，不会再出现「磁盘已改名、JSON 还是旧名」 |
| P2-5 | 临时钩子对 5 个片段逐个调 `VideoMetadata.duration(of:)` | `5.00 / 6.00 / 7.00 / 8.00 / 9.00` 秒，与素材实际时长一致，标 `@concurrent` 后解析链没有断 |
| P2-6 | 同一份数据的「占用空间」与宿主侧 `du` 对比 | `totalClipBytes = 14256440` 字节，与 5 个片段之和逐字节相等；改缓存后启动/变更/回前台各扫一次目录 |
| P2-6 | 后台期间从容器删除 `镜头02` 的片段，回前台截图 | 该卡片自动从缩略图变为虚线占位（`◀ Safari` 说明确实是从后台回来的），说明回前台确实重新扫了盘 |
| P2-7 | 代码级（模拟器无摄像头，录制链路跑不起来） | 会话操作已整体进 `sessionQueue`；连点两次的回归已在实现里挡回（见下） |
| P2-8 | 分镜页截图 + 代码级 | 缩略图全部正常渲染；陈旧回调改为 `Task.isCancelled` 判断后丢弃，加载器加了在途任务表 |

**过程中自己引入又修掉的一个回归**：P2-7 第一版把 `isRecording` 的置位也挪到了队列确认之后
（经 `onMain` 回主线程），结果主线程上那道「正在录制」的同步防线没了——快速连点两次录制按钮，
两次点击之间 `isRecording` 都还是 `false`，会通过守卫并连着启动两次录制，后一次还会把前一次的
完成回调覆盖成 `nil`，导致录完的东西进不了回看。改回「主线程同步置位 + 队列侧确认失败再回滚」。

**一处实测确认不是问题的**：`ShotListView` 右上角的「⋯」是仓库里原有的「更多添加方式」菜单
（`ShotListView.swift:212`），不是本轮改动引入的。

---

## 七、第二轮审查（2026-09-15 01:00 前后）

### 范围与方法

- 通读全部 22 个 Swift 文件，另加 `project.yml` / `ShotList/Info.plist` / `README.md` / `Tools/`
- 全仓 grep 逐个符号排查「定义了但没人调用」的情况（`for sym in ...; do grep -rn "$sym" ...`）
- 模拟器实机验证（iPhone 17 · iOS 26.5，UDID `D9962B2B-…`）：构建 →
  `simctl install` → 灌演示数据（`Tools/seed-simulator.py`）→ 截图；
  需要切标签 / 切筛选的地方用「临时改默认值 → 截图 → 立刻恢复」的老办法
  （`git diff --quiet ShotList/` 确认过源文件已回到 `58ca815` 状态，工作区只剩已知的
  `project.pbxproj` 里 Xcode 自动写入的 `DEVELOPMENT_TEAM`）

### 实测记录

| 项 | 验收手法 | 结果 |
|---|---|---|
| P1-5 | 造「镜头 01、02 今天拍过、03 是昨天拍的」数据，临时把今日页默认筛选切到「已拍」 | 统计块写「今日已拍 2」，列表里 3 张卡片（01、02、03）——数字与列表不是同一个口径 |
| P0-2 | `chmod 000` 片段目录 → 启动一次 → 读回 `shots.json` → `chmod 755` | 启动前 5 条片段记录 → 启动后 **0 条**；磁盘上 10 个视频文件一个没少。记录被永久抹掉，文件还在 |
| P2-9 | 删掉镜头 03 的片段文件 → 切后台再回前台 → 截图 | 卡片变成空心虚线占位，却还挂着「昨天 00:49」，也不显示「点击拍摄或导入视频」；徽标仍是 3、进度仍是 3/5。JSON 里那条记录当次会话仍在（`镜头03 ['镜头03_…mov']`） |
| P2-9 自愈 | 同一个容器直接重启应用 → 截图 + 读 JSON | 卡片恢复成完整「未拍」态（虚线 + 「点击拍摄或导入视频」），JSON 变成 `镜头03 []`——校正只在下次启动发生，且不可逆 |
| P2-10 | 代码级（`pendingCount` 与 `pendingRows` 两处判据不同） | 需要「有记录、无文件」的镜头才触发，正是 P2-9 验证过的那个状态；未单独打包验证 |
| P2-11 | 用户手动放一个 3.05 MB 的 mp4 进片段目录 → 回前台 → 截图 + `du` 对比 | 界面仍是「占用空间 14.3 MB」，`du` 已是 18.2 MiB（+3.05 MB）。孤儿文件不计入统计，也没有回收入口 |
| P2-12 | 代码级（逐个方法核对隔离标注） | 见清单条目；模拟器无法验证真实竞态 |
| P2-13 | 全仓 grep | `deleteShots(withIDs:)`、`ShotStatus.isRecorded`、`showsPlayGlyph` 三处确认零调用 / 零覆盖 |
| P2-15 | 连跑两次 `python3 Tools/seed-simulator.py <UDID>` | 脚本自报「已写入分镜: 5 条，片段: 5 段，磁盘文件: 10 个」——上一轮的文件没被清掉，正是 P2-11 那类孤儿 |
| 构建 | `-derivedDataPath build`，中途为验证改过 3 次文件 | 每次都 `** BUILD SUCCEEDED **`，零 error；源文件已恢复 |

### 复现 P0-2 的完整步骤（本次实测用的就是这几步）

```bash
UDID=D9962B2B-FAB3-4DC9-8D80-3C8DC0683C8B
C=$(xcrun simctl get_app_container $UDID com.max.ShotList data)
DIR="$C/Documents/分镜视频"
META="$C/Library/Application Support/ShotList/shots.json"

python3 -c "import json;d=json.load(open('$META'));print(sum(len(s['clips']) for s in d))"  # 5
xcrun simctl terminate $UDID com.max.ShotList
chmod 000 "$DIR"                      # 目录「读不到」——不是「文件不存在」
xcrun simctl launch $UDID com.max.ShotList && sleep 4
python3 -c "import json;d=json.load(open('$META'));print(sum(len(s['clips']) for s in d))"  # 0 ← 记录没了
chmod 755 "$DIR" && ls "$DIR" | wc -l                                                        # 10 ← 文件还在
```

### 没能自证的部分

- P1-6（录制中关相机）与 P2-12（隔离标注）都落在相机链路上，**模拟器没有摄像头**，
  只能做代码级核对；这两条要在真机上过一遍才算验收。
- `update(_:)` 改编号后的落位、`move(fromOffsets:toOffset:)` 的上下挪一位，本轮只做了代码推演
  （模拟器不支持合成点击，没法点 Stepper 和长按菜单），结论与实现一致但没有实机证据。
- P2-10 只做了代码级确认，没有单独构造导出包对比摘要与 `导出说明.txt`。

### 观察项（不判定为 bug，但值得知道）

- `ExportPackageBuilder.cleanUp()` 在主协程的 `.task` 里同步删目录；包体积≈全部素材，
  但 `removeItem` 只是摘目录项，实际耗时可忽略。
- `ThumbnailLoader` 的 `countLimit = 60`、单张最大 480×480（约 0.9 MB），理论上限约 55 MB；
  `NSCache` 会在内存压力下自动回收，暂不需要处理。
- `persist()` 每次数据变更都同步写 JSON + 枚举一次目录；`normalize()` 在编号变化时同步逐个
  重命名磁盘文件（都是主线程）。片段量级在几十条以内时无感，上百条时值得再看。
- 录制中下拉控制中心 / 收到横幅会把 `scenePhase` 变成 `.inactive`，当前按「中断」处理，
  会停止录制并跳到回看（`CameraCaptureView.swift:46-56` 的 `default:` 分支）。
  对录像是安全的选择，但和 README 里写的「切后台或来电」不完全是一回事，是否要收紧可以再定。

### 遗留

- 第一轮修完的 `9628783`（P0/P1）与 `58ca815`（P2）仍只在本地 `main`，没有推到 `origin`。
- 本轮新发现的问题**只记录、未修**（P0-2、P1-5、P1-6、P2-9 ~ P2-15）。

