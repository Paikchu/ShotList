# 项目开发规范

# Permission Needed：推送origin/main等待我确认，我没确认不能推送远程分支

## 基本原则

- 从第一性原理出发，先明确预期行为、实际行为及产生差异的根因，再选择最短、完整的解决路径。
- 善于完善需求：发现边界条件或验收标准缺失时，先结合现有产品行为和代码补全；无法判断且影响实现或修复方向时，再向用户澄清。
- 验证必须围绕问题复现路径或需求验收方法展开。构建成功不能代替验收。
- 开始工作前检查当前分支和工作区改动，保留用户及其他任务的未提交内容；不得覆盖、回退或混入无关修改。
- 不要构建Unit Test，只进行集成测试

### 开发新需求

未完成需求统一维护 [open-requirements](Docs/open-requirements.md)，已交付需求维护 [delivered-requirements](Docs/delivered-requirements.md)；沿用现有汇总 Checklist、优先级分组、标题和锚点格式，不另建平行清单。

#### 需求优先级

| 级别 | 判定标准 |
|---|---|
| P0 | 核心主路径缺失或走不通，不完成则无法完成一次拍摄到导出。 |
| P1 | 主要功能或关键交互，影响正常创作流程，缺少合理绕行方式。 |
| P2 | 增强、边界场景、体验或导出/无障碍等局部能力。 |

根据完成该需求对主路径的必要程度定级。不能因为实现简单就降低级别，也不能因为只是想法就定为 P0。

#### 编号与文档维护

- 需求号沿用 `R-1`、`R-2` 格式。新增编号使用历史最大序号加一；检查 open-requirements、delivered-requirements 及必要的 Git 历史，不填补空号，不复用旧号。
- 每个独立交付目标对应一个需求号；同一需求的多个入口或界面落点补充到原条目，避免拆成重复需求。
- 编号一经分配保持稳定。若调整优先级，更新所在分组并明确记录当前优先级及调整原因，保留原编号、锚点和追踪关系。
- 每个需求必须有明确标题。标题概括使用情境和要达成的结果；在该标题下完整记录需求详情及可执行的验收方法，不能只写「加个功能」「优化体验」。
- 接到需求后同步更新汇总 Checklist、对应详情、更新日期及未完成数量，按当前优先级和编号排序。
- 已交付需求移至 [delivered-requirements](Docs/delivered-requirements.md)，保留编号、详情和验收步骤，并登记验证结果与实现 commit。未完成统计不包含已交付项。

#### 需求条目模板

沿用与问题清单相同的字段结构，把复现换成验收、把修复换成实现：

```markdown
<a id="r-1"></a>

### R-1 · [谁在什么情境下要达成什么]

**验证状态：** 待验证 / 代码级确认 / 隔离测试实测 / 模拟器实测 / 真机实测

**代码位置：** 待实现 / 仓库相对路径 · 类型 / 方法 / 关键逻辑（必要时附行号）

**预期结果：** 可观察的完成态与验收标准。

**需求详情**

描述当前行为、目标行为、范围、明确不做的边界、对其他功能的影响。
边界条件或验收标准缺失时，先按现有产品行为和代码补全；未证实的推断单独注明。

**验收方法**

1. 前置环境、测试数据及初始状态。
2. 从明确页面入口开始的操作顺序。
3. 必要的等待时序、边界条件或失败路径。
4. 观察位置、实际应出现的结果；写清失败时不该出现什么。

**实现状态：** 未开始 / 进行中 / 待验证 / 已交付

**实现说明：** 待实现；完成后记录方案要点及对周边功能的取舍。

**验证结果：** 待验证；完成后记录环境、执行步骤或命令、实际结果及验证限制。

**实现 commit：** 待提交；完成后填写实际实现提交的完整 SHA。
```

#### 需求开发流程

1. **登记需求并建立分支**
   - 把用户原话补成可验收条目：预期结果、范围、不做的边界、对其他功能的影响。无法判断且影响实现方向时先澄清，不要带着缺口开工。
   - 检查当前分支、基线及工作区，保留他人未提交改动。在 [open-requirements](Docs/open-requirements.md) 按最大序号加一登记，更新 Checklist、日期和未完成数量。
   - 基于本次实现应使用的代码创建 `feature-[需求号]` 分支，例如 `feature-R-1`。需求分支使用此命名，不添加其他前缀。
   - 同名分支已存在时先核实是否属于该需求；属于本次实现则继续使用。有无关改动影响切换时使用独立 worktree 隔离。

2. **构建解决方案**
   - 从第一性原理对齐预期行为；深入分析 corner case、异常处理、空值与边界、对拍摄 / 导入 / 导出 / 影片切换等现有路径的影响。
   - 用最少改动、最高效率覆盖验收标准。将实现状态更新为「进行中」。若登记与产品现状不符，先补正条目再写代码。

3. **实现需求**
   - 只做本需求范围内的改动。不顺带无关重构、格式化、依赖升级或其他功能。
   - 实现中新发现的独立缺陷按问题规则登记到 [open-issues](Docs/open-issues.md)，不塞进本需求条目。
   - 必要的错误反馈、状态恢复和资源清理属于实现范围。

4. **沿验收方法验证**
   - 重走条目中的验收步骤，对照预期结果；同时验证与本需求直接相关的正常路径及必要边界。
   - 根据改动选择构建、模拟器或真机验证。交互与界面必须检查实际交互及界面；构建成功不能代替验收。
   - 不要构建 Unit Test，只进行集成测试。临时探针、调试代码在提交前移除。
   - 验证受阻时标记「待验证」，写清未验证内容及原因，不勾选完成，也不宣称已交付。

5. **提交实现并回填清单**
   - 检查差异，只暂存本需求相关代码、必要测试及文档，创建实现 commit；提交信息包含需求号，例如 `feat(R-1): 实现具体能力`。
   - 取得实际实现提交的完整 SHA，回填「实现 commit」，不得使用占位值、预计哈希或文档提交号代替。
   - 仅在实现已提交且验证通过后，将 Checklist 改为 `☑`，标记「已交付」，补齐实现说明和验证结果，将条目及对应 Checklist 行从 [open-requirements](Docs/open-requirements.md) 移至 [delivered-requirements](Docs/delivered-requirements.md)，更新日期和未完成统计。
   - 回填 SHA 后单独提交清单更新，例如 `docs(R-1): 登记实现提交和验证结果`；不要为把 commit 号写进其自身而反复 amend。
   - 直接合并到本地的 main 分支。Permission Needed：推送 origin/main 等待确认，未确认不能推送远程分支。
   - 最终报告需求号、方案要点、验证结果和实现 commit。


### 代码质量和Bug审查范围与证据

审查代码中潜在的逻辑异常、空值处理缺失、边界条件错误、逻辑冗余、无用代码、非最佳实现、用户交互异常、前端界面异常及其他逻辑 bug。重点关注数据读写、异步状态、错误处理、资源生命周期及用户完整操作链路。

- 沿实际调用链定位问题，说明触发条件、异常行为和具体影响，不凭局部代码或个人偏好下结论。
- 冗余、无用代码和非最佳实现也在审查范围内；登记时必须说明可核实的影响或维护成本，不能仅以“写法不够优雅”为依据。
- 区分实测结果、代码级确认和待验证假设。无法运行的场景明确写出限制，不得宣称已复现。
- 破坏文件、模拟低空间、故障注入等验证必须使用隔离测试数据，不能破坏用户真实素材。

#### Bug优先级

| 级别 | 判定标准 |
|---|---|
| P0 | 造成严重数据丢失、覆盖或损坏，应用无法启动，或核心流程大范围不可用，需要优先处理。 |
| P1 | 主要功能或关键交互明显异常，影响正常使用，缺少合理绕行方式，但未达到 P0 的严重程度。 |
| P2 | 局部功能、边界场景、界面、无障碍或性能问题，或有明确影响的冗余、无用代码及实现缺陷。 |

根据影响程度、触发范围和恢复成本定级，并在问题详情中说明依据。不能因为问题容易修复就降低级别，也不能因为仅是潜在风险就直接定为 P0。

#### 编号与文档维护

- 未关闭问题统一维护 [open-issues](Docs/open-issues.md)，已修复问题维护 [resolved-issues](Docs/resolved-issues.md)；沿用现有汇总 Checklist、优先级分组、标题和锚点格式，不另建平行清单。
- 问题号沿用 `P0-4`、`P1-8`、`P2-33` 格式。新增编号使用对应前缀下历史最大序号加一；检查 open-issues、resolved-issues 及必要的 Git 历史，不填补空号，不复用旧号。
- 每个独立根因对应一个问题号；同一根因的多个入口补充到原问题中，避免重复登记。
- 编号一经分配保持稳定。若调整优先级，更新所在分组并明确记录当前优先级及调整原因，保留原编号、锚点和追踪关系。
- 每个问题必须有明确标题。标题概括触发条件和异常结果；在该标题下完整记录问题描述及可执行的复现路径，不能只写“逻辑异常”“界面有 bug”。
- 发现问题后同步更新汇总 Checklist、对应详情、更新日期及未修复数量，按当前优先级和编号排序。
- 已修复问题移至 [resolved-issues](Docs/resolved-issues.md)，保留编号、详情和复现步骤，并登记验证结果与修复 commit。未修复统计不包含已修复项。

#### 问题条目模板

沿用现有字段，补充预期结果和修复追踪信息：

```markdown
<a id="p2-34"></a>

### P2-34 · [触发条件]导致[具体异常结果]

**验证状态：** 待验证 / 代码级确认 / 隔离测试实测 / 模拟器实测 / 真机实测

**代码位置：** 仓库相对路径 · 类型 / 方法 / 关键逻辑（必要时附行号）

**问题详情**

描述预期行为、实际行为、触发条件、根因证据、影响范围及定级依据。
未证实的推断或尚未覆盖的环境应单独注明。

**复现方法**

1. 前置环境、测试数据及初始状态。
2. 从明确页面入口开始的操作顺序，或可执行的代码级验证步骤。
3. 必要的等待时序、异常注入或边界条件。
4. 观察位置、实际异常结果及预期正确结果。

**修复状态：** 未修复 / 修复中 / 待验证 / 已修复

**修复说明：** 待修复；完成后记录根因及最小修复方案。

**验证结果：** 待验证；完成后记录环境、执行步骤或命令、实际结果及验证限制。

**修复 commit：** 待提交；完成后填写实际修复提交的完整 SHA。
```

#### Debug 流程

1. **读取问题并建立分支**
   - 根据问题号读取清单中的描述、代码位置、验证状态和完整复现步骤，明确验收结果。
   - 检查当前分支、基线及工作区，基于本次修复应使用的代码创建 `debug-[问题号]` 分支，例如 `debug-P2-34`。问题修复分支使用此命名，不添加其他前缀。
   - 同名分支已存在时先核实是否属于该问题；属于本次修复则继续使用，不强制覆盖或删除分支。有无关改动影响切换时使用独立 worktree 隔离。

2. **复现并定位根因**
   - 优先按清单路径复现，沿入口、状态变化、调用链及数据读写定位实际故障点。
   - 核对预期行为和关键约束，区分表面症状与根因。不能复现时如实记录，并通过有针对性的代码分析或受控测试补足证据。
   - 将问题状态更新为“修复中”。若记录与实际代码不符，先补正问题证据和复现条件。

3. **以最短完整路径修复**
   - 修复产生异常的根因，覆盖同一根因影响的必要入口。最短路径指最小必要改动，不是简单减少代码行数或掩盖错误。
   - 不顺带进行无关重构、格式化、依赖升级或功能扩展。新发现的独立问题另行编号登记。
   - 必要的错误反馈、状态恢复和资源清理属于修复范围，不可为了缩小 diff 而遗漏。

4. **沿原复现路径验证**
   - 重走清单中的复现步骤，对照预期结果确认异常已消失，同时验证与根因直接相关的正常路径及必要边界。
   - 根据改动选择有针对性的测试、构建、模拟器或真机验证。交互与界面问题必须检查实际交互及界面；纯代码检查不能替代这些验证。
   - 记录所用环境、测试步骤或命令和结果。临时探针、故障注入及调试代码在提交前移除。
   - 验证受阻时标记“待验证”，写清未验证内容及原因，不勾选完成，也不宣称已修复。

5. **提交修复并回填清单**
   - 检查差异，只暂存本问题相关代码、必要测试及文档，创建修复 commit；提交信息包含问题号，例如 `fix(P2-34): 修复具体异常`。
   - 取得实际修复提交的完整 SHA，回填清单的“修复 commit”，不得使用占位值、预计哈希或文档提交号代替。
   - 仅在修复已提交且验证通过后，将 Checklist 改为 `☑`，标记“已修复”，补齐修复说明和验证结果，将条目及对应 Checklist 行从 [open-issues](Docs/open-issues.md) 移至 [resolved-issues](Docs/resolved-issues.md)，更新日期和未修复统计。
   - 回填 SHA 后单独提交清单更新，例如 `docs(P2-34): 登记修复提交和验证结果`；不要为把 commit 号写进其自身而反复 amend。
   - 最终报告问题号、根因、修复内容、验证结果和修复 commit。合并、推送或发布按用户明确要求执行。
